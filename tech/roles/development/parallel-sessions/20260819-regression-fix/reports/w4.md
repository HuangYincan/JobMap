# w4 汇报(2026-08-19)

## 实际改动
- `server/src/lib/saved-overlay.ts` → `savedPlacesToOverlay` 新增 `mode?: MapMode` 参数,按当前模式过滤收藏:
  - work(含 internship)→ 只保留 `place.mode ∈ {work, internship}` 的工作收藏;
  - domain → 只保留 `place.mode === 'domain'` 的地点收藏;
  - mode 缺省 → 不过滤(向后兼容)。
- `server/src/lib/saved-overlay.ts` → `savedPlaceToOverlayPoi` 兜底改 kind 感知:recruitment 收藏未命中 live catalog 时,fallback 改为 `kind:'recruitment'`(空 positions,`company.name` + `scale:'startup'`),不再以 domain 形态出现在工作地图。
- `server/src/components/map-shell.tsx` → `overlayPois` 调用处传入当前 `mode`(1105-1112 段),markers 合并 `mergeMapPois` 只拿到本模式收藏。
- `server/src/lib/viewport-search.ts` → `loadWorkViewport` 起始对 `existing` 加 `isRecruitmentPoi` 过滤(kind 守卫):work 累计池只保留 recruitment 行,清掉被污染缓存/历史残留的 domain 行。
- `server/src/lib/mode-cache.ts` → `readModeCache` 增加按模式 kind 校验:`kindMatchesMode`,recruitment 模式缓存只接受 `kind:'recruitment'`,domain 只接受 `kind:'domain'`;任一不匹配整条缓存作废(read 返回 null → 重新拉取)。
  - **未 bump `MODE_CACHE_VERSION`**:读时校验已使旧污染缓存直接失效,无需强制全员重拉;干净缓存不受影响。
- 测试:`tests/saved-overlay.test.mjs` 新增 5 个用例(work/domain/internship 模式过滤 + recruitment 兜底 pin);`tests/mode-cache.test.mjs` 新增 3 个 kind 校验用例,并修正 work 缓存用例改用一个 recruitment 样本 POI;`tests/viewport-search.test.mjs` 新增 loadWorkViewport 清除 domain 行用例。

## 门禁结果
- npm test: 294 通过 / 0 失败(2 skipped)
- typecheck: 通过
- docs-check: 通过(等价 grep 检查无命中)
- git diff --check: 通过

## 遇到的问题
- **无法截取 Playwright 截图**:本 headless 会话无浏览器/screenshot 工具,`browser_take_screenshot` 不可用,未生成 `.playwright-mcp/` 截图。→ 改用代码级 + 单测逻辑验证;work/domain 收藏 pin 的视觉确认建议由 boss/merger 或用户浏览器复核。
- 并行注意:w3 也在动 `map-shell.tsx`;本次只改 overlay 合并(1105-1112)、未动加载/判定段,冲突留给收尾。
- mode-cache 旧用例(legacy internship key / clearModeCache / stale version)原用 domain 样本写 work 缓存,与新 kind 校验冲突,已改为 recruitment 样本以保持原断言语义。

## 证据
- 测试输出摘要:`ℹ tests 296 / pass 294 / fail 0 / skipped 2 / duration ~1016ms`
- overlay 过滤纯函数验证:savedPlacesToOverlay(…, 'work') 剔除 domain 地点、保留 work+internship 公司;savedPlacesToOverlay(…, 'domain') 仅剩 domain 地点;recruitment 未命中兜底 `kind:'recruitment'`。
- mode-cache kind 守卫:work 缓存含 domain 行 → `readModeCache('work')` 返回 null;domain 缓存含 recruitment 行 → `readModeCache('domain')` 返回 null;纯 recruitment 的 work 缓存正常通过。
- loadWorkViewport 守卫:existing 含 `kind:'domain'` 行时,merged/onBatch 结果仅剩 recruitment 行。
- 提交(3 个,均在工作树 `fix/work-domain-leak`,未 merge/push):
  - `08b65ef fix(work): saved-overlay 按当前模式过滤收藏,工作收藏兜底 recruitment pin`
  - `15c2820 fix(work): loadWorkViewport 客户端 kind 守卫清除 domain 残留`
  - `75172fc fix(cache): readModeCache 增加按模式 kind 校验,污染缓存整条作废`

门禁: PASSED
结论: OK
