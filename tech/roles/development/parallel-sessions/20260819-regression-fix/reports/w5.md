# w5 汇报(2026-08-19)

WS: fix/viewport-refresh(Bug 7 — 工作模式 poi 列表不随视角刷新)
worktree: `/Users/acccan/dm-wt-w5` | 分支 `fix/viewport-refresh` | commit `1826416`

## 实际改动

- `server/src/components/map-shell.tsx`
  - **work 视口刷新改为替换**(镜像 domain 分支,原 859-891 段):
    - `loadWorkViewport` 调用 `existing: []` → 按 live bounds 取新一批,不再并入旧累计池
      (修复根因:工作目录仅 ~79 家,首屏+加载更多几乎全捕获 → merge 去重无变化 → 列表冻结)。
    - `viewportEpochRef.current += 1` → 在飞主加载对旧视野的追加批次被 epoch 校验丢弃。
    - `setPageOffset(0)` + `skipFetchRef` 武装(与 domain 一致;offset 已为 0 时不 arm,
      避免吞掉下一次合法滚动加载)。
    - 视口替换时复位 noMore(`noMoreRef.current = false; setNoMoreData(false)`)——
      与 domain 先例(`e7323c7`)一致,对接 w3 的 work noMore 判定。
    - 保留:maxTier 过滤、alive 过滤、bounds 过滤、`batchMatchesCurrentMode` 模式守卫。
    - `writeModeCache` 的 `pageOffset` 改为 `0`(新视野从第 1 页起)。
  - **840 行丢刷新修复**(主加载在飞不静默丢弃):
    - 视口 loader 的 `load()` 遇 `loadingRef.current` 时置 `viewportRefreshPendingRef = true`
      再 return(不再直接丢弃)。
    - 主加载 `finally`(释放 `loadingRef.current = false` 后)检查 pending 标记,复位并
      `viewportLoaderRef.current?.schedule()` 补跑(经 800ms 防抖合并,不引入重复加载竞态)。
    - 新增 `viewportRefreshPendingRef` / `viewportLoaderRef`;视口 effect cleanup 置空 loader ref。
- `server/tests/viewport-search.test.mjs`
  - 新增 `loadWorkViewport with existing:[] replaces the catalog (viewport refresh replace
    semantics)` 断言:existing=[] 时只保留本视野取回的一页,旧视野公司不残留。
- `tech/22-hangzhou-poi-local.md`
  - §视口变化刷新 同步 work 分支「替换」语义(原为增量合并)+ 主加载在飞 pending 补跑行为。
- `CHANGELOG.md`
  - 2026-08-19 Fixed 新增 Bug 7 条目。

## 门禁结果

- `npm test`(server):289 tests / 287 pass / 2 skipped / 0 fail
- `npm run typecheck`:通过(tsc --noEmit 无错误)
- `make docs-check`:通过(Documentation policy check passed)
- `git diff --check`:通过(无 whitespace 错误)

## 遇到的问题

- **Playwright 实测未能在此会话执行**:本 headless worker 会话没有浏览器自动化工具,
  且 sandbox 拦截了所有网络调用(curl 到本机 dev server 被拒),无法驱动真实地图做
  平移/缩放的截图对比。已验证到逻辑层:新增单元测试覆盖替换语义、typecheck 全绿、
  变更严格镜像 domain 已实测可用的分支。建议收尾/merge 阶段用现有 Playwright 流程
  跑一遍 prompt 任务 3 的 5 项验证(work 平移刷新、快速连续平移 epoch 丢弃、主加载在飞
  不吞刷新、domain 不回归、新视野无限滚动)。不认为代码有逻辑缺口。
- **w3 并行依赖**:本分支基线上 w3 的 work noMore 判定尚未合入(map-shell.tsx:799-802 仍
  domain-gated)。我在 work 视口替换处按 domain 先例加了 noMore 复位;w3 合入后「判定放开 +
  视口替换复位」两段在 merge 时对接即可。未改 w3/w4 拥有段。
- **mode-cache 未动**:无需 bump version;视口替换写缓存 `pageOffset: 0` + 新视野 catalog,
  语义自洽。mode-cache.ts 未改(w4 在动)。

## 证据

- 单元测试:`npm test` 中新增用例
  `loadWorkViewport with existing:[] replaces the catalog (viewport refresh replace semantics)`
  通过(替换语义:旧视野 id 不残留、onBatch 只含新视野页)。
- 逻辑验证:work 分支代码现与 domain 分支(实测可用)逐点对齐——
  `noMore 复位 / epoch+1 / pageOffset 归零+skipFetch / existing:[] / pageOffset:0 写缓存`。
- 提交:`1826416 fix(work): viewport refresh replace + don't swallow refresh during main load (Bug 7)`
- 未产生 `.playwright-mcp/` 截图(见上,本会话无浏览器工具)。

门禁: PASSED
结论: OK
