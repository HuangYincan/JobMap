# Workstream w1 — fix/geocode-plan-count(全量计数输出修正)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。** 汇报写入批次目录 reports/w1.md(末两行 token,见文末)。

## 背景

2026-08-21 实测(复现):`Sites needing a point: 5` 在**配额耗尽时严重误导** —— 实际 drops 缺坐标站点 **1783 个**(radar 1363 + official-career 420,已实锤统计)。根因:geocode-sites-apply.mjs 的 `planCount` 是**处理过程中**累计的(L249-250),配额短路(QUOTA_SHORT_CIRCUIT_N=5,连续 5 站配额类失败即停)触发后计数停止在 5 → 输出「Sites needing a point: 5」「剩余 0 站」,用户误以为只剩 5 个缺坐标。实际是「已尝试 5 个后短路」。

## 任务

修正计数口径,短路时输出**真实全量**:

1. 读 `server/scripts/geocode-sites-apply.mjs` 主循环(L241-262 附近)与短路逻辑(L222-233,QUOTA_SHORT_CIRCUIT_N)。
2. 设计(自裁,最小改动):
   - 主循环**之前**预扫一遍 `files`(只读不处理),统计真实 `planTotal`(所有 `siteNeedsGeocode(site)` 的站点数,含 ONLY/CITIES 过滤前的口径或过滤后均可,选择能反映「待下次运行」真实剩余的口径,并在输出里说清)。
   - 输出改:`Sites needing a point: ${planTotal}(attempted: ${planCount})`(短路时 attempted < planTotal;正常跑完相等)。
   - QUOTA_EXHAUSTED 行的「剩余 N 站」改为 `planTotal - resolutions - unresolved - skipped`(真实剩余),不再用短路前的 planCount 算。
   - 预扫开销:724 文件 JSON.parse 两遍可接受(每遍毫秒级);若想避免,预扫时可缓存 needing 站点引用供主循环复用,自裁。
3. **测试**:`server/tests/` 新增或扩展测试覆盖:短路后 attempted 数正确、真实剩余计算正确(可对抽出的纯函数或脚本逻辑做单测;若脚本无导出,可抽取 `planTotal` 统计函数供测)。
4. 不跑真实 geocode(网络留给用户)。

## 文件边界

- **只允许改**:`server/scripts/geocode-sites-apply.mjs`、`server/tests/`(新增测试)。
- **不碰**:`server/data/recruitment/**`、`crawler/`、`db/`、`map-shell.tsx`、`server/src/lib/**`(若必须动 site-geocode.ts 需说明理由)、tech/ 文档。

## 门禁

1. `cd /Users/acccan/dm-wt-geo-count/server && npm test`(基线 530 pass/2 skip;**必须全绿**)
2. `cd /Users/acccan/dm-wt-geo-count/server && npm run typecheck`
3. `cd /Users/acccan/dm-wt-geo-count && make docs-check`、`git diff --check`
4. 汇报给出:计数修正摘要(关键行)+ 测试用例 + 短路场景的输出示例(attempted/剩余 数字推演)

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-geocode-count/reports/w1.md`。**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

不 push、不 merge、不切分支;Conventional Commits(如 `fix(geocode): report true plan total when quota short-circuit stops the run`)。
