# w1 汇报(2026-08-21)

Worktree `/Users/acccan/dm-wt-geo-count`(分支 `fix/geocode-plan-count`),2 commits:
`e5cd04d` fix + `fa5f854` test。未 merge、未 push。

## 实际改动

- `server/scripts/geocode-sites-apply.mjs` →
  - 主循环前新增只读预扫(遍历 `files` 调 `sitesNeedingGeocode(companies)`,只读不写、无网络),
    统计真实全量 `planTotal`(所有 `siteNeedsGeocode` 的站点数,ONLY/CITIES **过滤前**口径);
    预扫结果 `{file, company, site}` 引用直接供主循环复用,**不重复 JSON.parse**(724 文件仍只解析一遍)。
  - 主循环从三层嵌套(文件→公司→站点)重构为单层 `mainLoop: for (const { file, company, site } of needing)`,
    逐站语义/顺序/计数器完全不变(预扫收集顺序 = 原遍历顺序)。
  - 输出行:`Sites needing a point: ${planTotal} (attempted: ${planCount})`,
    skip 明细补上 `city-not-in-list` 计数(planTotal 含过滤前全量,明细对得上账)。
  - QUOTA_EXHAUSTED 剩余:`planTotal - resolutions.length - unresolved.length - skipped.length`
    (原为短路前累计的 `planCount` 算,短路后恒为 0)。
- `server/src/lib/site-geocode.ts` → 新增纯函数 `sitesNeedingGeocode(companies): NeedingSite[]` +
  `NeedingSite` 接口。
  **动 site-geocode.ts 的理由**:脚本顶层跑主循环 + 真实网络,import 即执行、无法单测;
  预扫统计逻辑必须抽到可单测模块 —— 与既有 `shouldShortCircuitQuota` / `placeSearchMemoKey` 完全同模式。
- `server/tests/geocode-quota-short-circuit.test.mjs` → 契约正则随主循环签名更新
  (`mainLoop: for (const { file, company, site } of needing)`),断言不减。
- `server/tests/geocode-plan-count.test.mjs` → 新增 8 个用例(见下)。

## 计数修正摘要(关键行)

```
Sites needing a point: 1783 (attempted: 5) | skipped (not-in-only-list): 0 | city-not-in-list: 0 | override-city-mismatch: 0
QUOTA_EXHAUSTED: AMap+百度 双配额耗尽(或无可用 key),已提前停止,剩余 1778 站待下次运行。
```

口径说明:planTotal 是**过滤前全量**(「所有缺坐标站点」);`--only`/`--cities` 过滤跳过的站
在 skipped 单列;剩余 = planTotal − resolutions − unresolved − skipped = 本次未尝试的缺坐标站点数。

## 短路场景输出示例(attempted/剩余 数字推演)

实跑复现(2026-08-21,无 key → 每站 `no-key` 配额类失败,连续 5 站短路):

| 场景 | planTotal | attempted | resolutions | unresolved | skipped | 剩余(新) | 旧口径剩余 |
|---|---|---|---|---|---|---|---|
| 全量跑,第 5 站短路(真实 bug 场景,1783 站) | 1783 | 5 | 0 | 5 | 0 | **1778** | 0(误导) |
| 4 配额失败 + 1 成功冲窗口 + 再 5 连败,20 站 | 20 | 10 | 1 | 9 | 0 | 10 | 0 |
| `--only=a`(b 站 5 个在前),10 站全配额失败 | 10 | 10 | 0 | 5 | 5 | 0 | 0 |
| `--only=a`(b 站排短路点后,从未访问) | 10 | 5 | 0 | 5 | 0 | 5 | 0 |
| 非配额失败(http)不短路,50 站全程跑完 | 50 | 50 | 0 | 50 | 0 | 0 | 0 |
| 正常跑完,100 站 | 100 | 100 | 100 | 0 | 0 | 0 | 0 |

(上表为 `simulateRun` 单测对脚本计数的逐行镜像;1783 = radar 1363 + official-career 420 实锤数,
dry-run 实跑输出一致。)

## 测试用例(新增 8 个)

1. `sitesNeedingGeocode` 只收集缺坐标站点(无 location / 0,0 / NaN),返回原对象引用
2. `sitesNeedingGeocode` 跳过非法 company(无 slug)、空输入 → []
3. **bug 复现**:1783 站全配额失败 → attempted=5、shortCircuited、剩余 1778(且 ≠ 旧口径 0)
4. 非配额类失败不短路:全程跑完 attempted === planTotal、剩余 0
5. 配额失败夹成功:窗口被冲掉,恢复后再次耗尽仍停(第 10 站尝试后停)
6. `--only` 过滤:过滤外站 skipped 单列,剩余只算未尝试站;两种站点顺序下 planTotal 恒为过滤前全量
7. 正常跑完:attempted === planTotal、剩余 0、无 QUOTA_EXHAUSTED
8. 脚本接线契约:预扫在主循环前、`planTotal (attempted: planCount)` 输出、真实剩余公式、主循环复用预扫结果

## 门禁结果

- npm test: **536 通过 / 0 失败**(2 skip,基线既有;基线 530 → +6,新增 8 用例全部运行)
- typecheck: 通过
- make docs-check: 通过
- git diff --check: 通过

## 遇到的问题

- 主循环重构后原契约测试正则 `mainLoop: for (const file of files)` 失配 → 随本次更新(断言未减)。
- `--only` 过滤下「剩余」语义:短路点之后的过滤外站点会计入剩余(未尝试),取决于站点顺序;
  报告行的 skipped 明细可对账。此为短路语义固有属性,已在测试中显式覆盖两种顺序。
- 实跑 dry-run 无 key 时每站 `no-key` 直接短路(既有行为,非本次引入),恰好完整复现 bug 场景。

## 证据

- 实跑输出(`AMAP_WEB_KEY= BAIDU_MAP_AK= node scripts/geocode-sites-apply.mjs --dry-run`,exit=2 为短路既有退出码):
  `/tmp/geo-dryrun.log`;关键行见上「计数修正摘要」。
- 新增/更新测试:见「测试用例」;`node --test tests/geocode-plan-count.test.mjs tests/geocode-quota-short-circuit.test.mjs` → 15/15 通过。

门禁: PASSED
结论: OK
