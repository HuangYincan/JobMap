# Session Prompt — w1:CHANGELOG smoke 条目

> boss-agent 端到端 smoke 测试。你是 headless `boss-worker`。
> **worktree 已由 boss 预建,不需要你创建**。

- worktree 绝对路径:`/Users/acccan/dm-wt-w1`
- 批次目录:`/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260819-boss-smoke`
- 汇报文件:`/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260819-boss-smoke/reports/w1.md`
- **不要 merge 回 dev、不要 push**(boss 统一收尾)。

## 背景

验证 boss-agent 派发链路(spawn → 开发 → 门禁 → 汇报 → 合并)。

## 任务

在 worktree 内打开 `CHANGELOG.md`,在文件顶部 `# Changelog` 段落之后、`## 2026-08-19` 之前插入一节:
`## 2026-08-19` 下的 `### Added` 加一行:`- **boss-agent smoke**(端到端验证)。`(与现有条目风格一致)。
然后用 Conventional Commits 提交,如 `docs(changelog): boss-agent smoke entry`。

## 文件边界

**拥有**:`CHANGELOG.md`
**不碰**:其余任何文件。`git add` 只加 `CHANGELOG.md`,绝不用 `git add -A` / `git add .`(会误收 `server/node_modules` symlink)。

## 门禁(全部通过才算完成)

```bash
cd /Users/acccan/dm-wt-w1/server && npm test && npm run typecheck
cd /Users/acccan/dm-wt-w1 && make docs-check && git diff --check
```

## 回报

写汇报到上述汇报文件路径(格式:实际改动 / 门禁结果 / 遇到的问题 / 证据),**末两行必须精确**:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```
stdout 只输出 ≤3 行:改动概要 + 门禁结果 + 汇报路径。不 dump 文件内容。
