# w6 汇报(2026-08-19)

WS: jobs-split(岗位多合一修复:群核科技数据拆分 + 聚合岗位诚实展示)
worktree: `/Users/acccan/dm-wt-w6`,分支 `fix/jobs-aggregate-split`,5 个 commit,未 merge 未 push。

## 实际改动

### 1. 数据拆分 — `server/data/recruitment/radar/manycore-hangzhou.json`
- `radar-735415a42603`(旧聚合行)保留 externalId,`status` 改为 `"closed"`,保留 `"aggregate": true` → 墓碑行。
- 新增 4 个拆分岗位(均 `family:"campus"`、`applySource:"official"`、`status:"open"`、`deadline:null`、`siteId:"manycore-hangzhou-site-hangzhou"`、`retrievedAt:"2026-08-11"`,externalId 唯一):
  - `radar-735415a42603-xinghe` — 【星核人才计划】-2027届校招生;applyUrl = mokahr per-job URL(`2832#/job/0fb79a20-2644-41b6-b888-d12076b57152`);JD 来源:**用户提供权威原文**(修正明显错别字后录入)。
  - `radar-735415a42603-suanfa` — 科研算法工程师;applyUrl = 群核 mokahr 列表页(与 careerUrl 同,per-job URL 在 WAF 后不可得);JD 来源:**牛客网群核 2026 届「科研算法(Neural Rendering 方向)」公开职位页**代表性摘录。
  - `radar-735415a42603-ai-infra` — AI Infra 工程师;applyUrl = 列表页;JD 来源:**国家大学生就业服务平台群核公开职位页**。
  - `radar-735415a42603-ai-pm` — AI产品经理;applyUrl = 列表页;JD 来源:**浙大就业网群核「海外AI产品经理」公开职位页**代表性摘录。
- 现有 `radar-21c6183e2b66`(公共战略培训生,真实 per-job URL)未动。

### 2. aggregate 贯通(代码)
- `server/src/lib/types.ts` → `JobTaxonomy` 加 `aggregate?: boolean`(:JobTaxonomy 区域);`Position` 加 `aggregate?: boolean`(:Position 尾部)。
- `server/src/lib/recruitment-source.ts` → `SourcePosition` 加 `aggregate?: boolean`(:32 区域);`positionFromSource` 带出 `aggregate: pos.aggregate || undefined`(:146-148 区域,offline 读路径)。
- `server/src/lib/recruitment-import.ts` → 新增导出 `positionTaxonomy(pos)`(normalizeDeadline 之后):保留 drop 全部 taxonomy 字段 + aggregate 写入 jsonb;位置 upsert 改用它(`JSON.stringify(positionTaxonomy(pos))`,原 :378)。
- `server/src/lib/recruitment-store.ts` → `loadWorkCatalogFromDb` position 映射带出 `aggregate: pos.taxonomy?.aggregate === true ? true : undefined`(:203 区域,DB 读路径)。

### 3. 前端诚实展示
- `server/src/components/poi-detail.tsx` → 岗位行 `jobTitle` 内,`pos.aggregate` 时追加 `jobBadge` 徽标(i18n `aggregateBadge`);仅加行内元素,不改行结构/交互(只动 :214-235 区域)。
- `server/src/components/poi-detail.module.css` → 新增 `.jobBadge`:小号 11px 标签、蓝 ink(`--accent`)、胶囊底色,不抢岗位标题。
- `server/src/components/jd-panel.tsx` → `fallbackDescription` 重构:事实前缀(公司/部门/学历/技能)不变;**聚合行**接 `aggregateJdNotice`(「该岗位为聚合招聘信息,包含多个相似方向,具体岗位与 JD 以招聘官网为准。」);**非聚合但 JD 缺失**的行把原来编造的「参与日常研发/业务协作,具体职责以部门安排为准」改为诚实文案 `jdFallbackNotice`(「具体职责以招聘官网为准。」)。apply 按钮不变。
- `server/src/lib/i18n.ts` → 新增 3 个键:`aggregateBadge`(汇总岗位/Aggregate)、`aggregateJdNotice`、`jdFallbackNotice`(zh/en)。

### 4. 测试 — `server/tests/recruitment-import.test.mjs`
- 新增 `positionTaxonomy keeps family and carries the aggregate flag for the DB upsert`:普通行 → `{family:'campus'}`;带 taxonomy + aggregate → family/campusSeason 保留 + `aggregate:true`;`aggregate:false` 不泄漏进 jsonb。
- 新增 `manycore drop keeps a closed aggregate tombstone and 4 split positions with real JDs`:墓碑 closed + aggregate:true;4 个拆分行 externalId 唯一、open、campus、official、siteId 正确、description 非空。

### 5. 文档 — `tech/roles/data/data-quality.md`
- 「聚合行落地(2026-08-19)」一句:aggregate 贯通导入与读路径、UI 诚实展示、群核拆分样例。

## 门禁结果
- npm test:**301 通过 / 0 失败 / 2 跳过**(299 pass,2 skipped 为既有)
- typecheck:通过
- docs-check:通过
- git diff --check:通过

## 遇到的问题
- **墓碑行 status 是否被 upsert 更新的验证(任务点)**:已确认 `applyRecruitmentImport` 的 positions `ON CONFLICT (source_id, external_id) DO UPDATE SET ... status = EXCLUDED.status`(recruitment-import.ts 原 :368)→ 导入会把 DB 行 322 置 closed,`isAlivePosition` / SQL `status='open'` 过滤即隐藏。墓碑方案可靠,无需额外手段。DB 环境侧行为以实际 import:apply 为准(本 WS 未执行 Env-only 命令)。
- **offline 读路径**:`mergeCompanyOntoSeedPois` 对 closed 行直接剔除 + `openOnly` 过滤,墓碑同样不会出现在 offline 目录。
- 无其他阻塞问题。

## 证据
- 新测试输出(从 `npm test` 全量日志截取,均 ✔):`positionTaxonomy keeps family...`、`manycore drop keeps a closed aggregate tombstone...`。
- 全量摘要:`ℹ tests 301 / ℹ pass 299 / ℹ fail 0 / ℹ skipped 2`。
- 提交序列:`4cca61b`(数据)→ `0dc22cb`(贯通)→ `b2f9038`(前端)→ `c78fe28`(测试)→ `b3ad601`(文档)。

门禁: PASSED
结论: OK
