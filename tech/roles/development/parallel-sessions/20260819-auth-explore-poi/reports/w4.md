# w4 汇报(2026-08-19)

WS: 移动端 profile 滚动重置 + 侧控栏搜索框失焦丢文本(mobile-microfixes)
分支: `fix/mobile-microfixes`(worktree `/Users/acccan/dm-wt-w4`),4 个 commit,未 merge/push。

## 实际改动

**Bug 1:移动端打开 profile 继承列表滚动位置**
- `server/src/components/map-shell.tsx:1694`(openMobileAccount 打开 account 分支末尾)→
  新增 `if (drawerContentRef.current) drawerContentRef.current.scrollTop = 0;`。抽屉滚动容器
  `.drawerContent` 常驻挂载,切到 account 面板前重置滚动,不再继承 POI 列表 scrollTop。
  头像按钮(2290/2348)只在 `!detailPoi` 分支渲染,与详情返回的 `useLayoutEffect` 滚动恢复
  (1786-1790)无冲突,未动 `drawerScrollRef` 语义。

**Bug 2:展开侧控栏搜索框有文本失焦后文本不可见**
- `server/src/components/map-shell.module.css:403-408`(`.searchBox` 区)→ 新增
  `.sidebarOpen .searchBox input:not(:placeholder-shown) { opacity: 1; }`(放在
  `:focus-within` 规则之后)。query 非空时 placeholder 不占位、label 不渲染,此规则让展开态
  失焦也常显;折叠态不挂 `.sidebarOpen` 仍只显示图标;空查询仍走 label,行为不变。
  纯 CSS 最小 diff,零 TSX 改动,与既有 `.sidebarOpen .searchLabel` 规则同构。

**测试(按 prompt 鼓励项补充断言)**
- `server/tests/component-contracts.test.mjs:54-61` → 新增契约测试
  「mobile account open resets drawer scroll; expanded search keeps query text visible」:
  断言 openMobileAccount 内 scrollTop 重置(map-shell.tsx 源码级)+ CSS
  `:not(:placeholder-shown)` 规则。原有 scroll save/restore 断言(44-49)不受影响。

**文档(按 tech/16-bug-fixes.md 协议)**
- `tech/16-bug-fixes.md` → 新增「2026-08-19: 移动端微修」节,记录两个 bug 的症状/根因/方案/
  文件行号/验证。

## 门禁结果

- npm test: 300 total / **298 通过** / 0 失败(2 skipped)
- typecheck: 通过(tsc --noEmit 无错误)
- docs-check: 通过(Documentation policy check passed)
- git diff --check: 通过(无 whitespace 错误)

## 遇到的问题

无。两处修复均为定位明确的纯增量改动:scroll 重置只发生在 explore→account 入口
(account→explore 的 toggle 分支未加重置,保持原有返回语义);CSS 方案经比对
`:focus-within`/`.sidebarOpen .searchLabel` 现有结构,选 `:not(:placeholder-shown)` 而非
加 class,避免 TSX 改动与折叠态行为漂移。

## 证据

- commit 序列(worktree 内):
  - `2a2a4b2 fix(mobile-microfixes): reset drawer scroll when opening mobile account`
  - `4514147 fix(mobile-microfixes): keep sidebar search query visible when blurred`
  - `9e2f88c test(mobile-microfixes): contract assertions for account scroll reset and search visibility`
  - `92d1775 docs(mobile-microfixes): record mobile micro-fixes in bug log`
- npm test 输出:`tests 300 / pass 298 / fail 0 / skipped 2 / duration_ms ~1191`
- 新增契约测试通过(含在 298 内)。
- 未做浏览器实机验证(无 Playwright 授权);修复为同步 DOM scrollTop 写入与纯 CSS
  可见性规则,语义可由契约测试覆盖,建议 merger/boss 视情况抽查视觉效果。

门禁: PASSED
结论: OK
