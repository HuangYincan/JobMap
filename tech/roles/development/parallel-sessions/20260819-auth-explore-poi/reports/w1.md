# w1 汇报(2026-08-19)

> 续作(resume):上一会话预算中断,worktree 留有未提交改动(11 文件,+382/-104)。
> 本次盘点确认设计方向正确、无回退;修复 1 处 typecheck 破损(mapCleanup 类型),补齐文档,
> 按主题拆 5 个 commit 提交,门禁全绿。

## 实际改动(5 commits on `feat/poi-load-more`)

1. **bc9c952 `fix(poi-load-more): scale 控件生命周期 + POI 加载修复(A/C/D) + 加载更多接线`** — `server/src/components/map-shell.tsx`
   - **(1) Scale 生命周期**:`initMap` 持有 `createMap` 返回值(`mapCleanup`,map-shell.tsx:478-491);effect cleanup 先 `mapCleanup?.()` 再 `map.destroy()`(:692-697),resize/主题/鼠标监听不再泄漏。`handleResize` 加销毁保护(`mapInstance.current` null / `map.isDestroyed?.()` 直接 return)与未就绪保护;统一 `addScaleControl()` 创建函数(:619-638),插件回调 `if (scaleControlRef.current) return` 防双 addControl 竞态。
   - **(C) skipFetch 不吞视口刷新**:skipFetch 提前 return 前,`viewportRefreshPendingRef` 已置位则 `viewportLoaderRef.current?.schedule()` 补跑(:754-763)。
   - **(D) noMore 用服务端 total**:`load()` 改用 `fetchPOIsForMode` 的 `result.noMore`,无 total 降级路径回退本地长度比较(:839-847)。
   - **(A) 错误态 + 重试**:catch 置 `error` 态(:865-868,不碰 `noMoreRef`),成功 `setError(null)`(:783);新增 `handleRetry`(清缓存 + refreshToken+1,同一 pageOffset 重拉,map-shell.tsx:1238-1242);桌面 SecondarySidebar 接线 `onLoadMore={handleNeedMore}`/`loadError={error}`/`onRetry`(:2097-2101),移动抽屉 POIList 同样接 error/onRetry(:2636-2640)。
2. **d309a62 `fix(poi-load-more): 错误≠没有更多 + 服务端 total 判 noMore + AMap 超时`**
   - `poi-service.ts`:返回类型改 `{ pois, noMore? }`(FetchPOIResult);三条失败路径(domain 关键词 :96-103、AMap 回退 :130-140、本地库+高德兜底 :190-200)一律 throw,不再静默 `return existing`;`fetchLocalPois` 用响应 `total` 判 `offset + rows.length >= total`(:165-172)。
   - `viewport-search.ts`:non-ok 响应抛错(:365);`fetchWorkViewportPage` 透出服务端 total(WorkViewportPage,:331-343);`loadWorkViewport` 满页但 `offset + page.pois.length >= total` → noMore,不白打后续页(:411-422);短页判定保留。
   - `amap-api.ts`:(B) 新增 `withTimeout` + `SEARCH_TIMEOUT_MS=15_000`(:309-334),`searchPOI` 的 promise 包超时,超时以 error 形态 settle;`complete` 空 poiList 才算「没有结果」,error 事件/异常回调一律 reject(:351-405)——loadingRef 不再可能永久卡死。
3. **1e56527 `feat(poi-load-more): 桌面「加载更多」按钮 + footer 错误重试态`**
   - `secondary-sidebar.tsx` resultHeader(:437-457):蓝色文字按钮(12px `--blue-ink` 玻璃底,`aria-label`,键盘可达);显隐规则:空列表/noMore/atCap 隐藏,loadingMore 禁用显示「加载中…」,错误态变「重试」(调 onRetry);props `onLoadMore`/`loadError`/`onRetry`(:110-119)。
   - `poi-list.tsx`:footer 错误态显示「加载失败,点击重试」按钮(:186-196);哨兵错误态不自动重发(:93);`secondary-sidebar.module.css` `.loadMore`(:433-453)、`poi-list.module.css` `.retryBtn`。
   - `i18n.ts`:新键 `loadMore`/`loadingMore`/`retry`/`loadFailedRetry`(zh+en)。
   - 移动抽屉不加按钮(保持滚动无限加载)。
4. **e240567 `test(poi-load-more): total 判 noMore/失败可重试/AMap 超时/按钮契约`**
   - `poi-service.test.mjs`(新):withTimeout 超时 error settle + `SEARCH_TIMEOUT_MS=15000`、正常 promise 不受影响、domain-local total 判 noMore(未到/到底)、本地库失败抛错。
   - `viewport-search.test.mjs`:透出服务端 total、non-ok 抛错(不再静默 `[]`)、total 判到底不白打后续页(seenPages=['1'])、失败上抛不置 noMore。
   - `component-contracts.test.mjs`:scale cleanup 接线/销毁保护/无双 addControl 静态断言 + resultHeader 加载更多按钮/错误重试/i18n 键断言。
5. **58439fa `docs(poi-load-more)`** — `tech/16-bug-fixes.md` 新增两节:比例尺控件崩溃(根因:cleanup 孤儿 + resize 摸销毁实例 + 双 addControl 竞态)、POI 停止加载 A/B/C/D + 加载更多按钮。

## 门禁结果

- npm test: **307 通过 / 0 失败**(2 skipped,cancelled 0)
- typecheck: 通过(修复 1 处既有破损:map-shell.tsx:478 `mapCleanup` 类型补 `| undefined`——createMap 分支早退返回 undefined)
- make docs-check: passed;git diff --check: 通过
- 工作树干净,未 merge / 未 push,分支 `feat/poi-load-more` 留原地(5 commits 在 dev 之上)

## 遇到的问题

- 上一会话遗留 typecheck 报错(mapCleanup 赋 undefined)→ 声明类型放宽为 `(() => void) | null | undefined`,已修。
- 无其他阻塞;所有任务项均按原 prompt 清单完成(1-7 逐项覆盖)。

## 证据

- `npm test` 输出摘要:`tests 309 / pass 307 / fail 0 / skipped 2`(viewport-search 新增 4 用例、poi-service 新增 5 用例全绿)
- `npm run typecheck` / `make docs-check` 无输出错误
- 5 commits:`bc9c952`→`d309a62`→`1e56527`→`e240567`→`58439fa`

门禁: PASSED
结论: OK
