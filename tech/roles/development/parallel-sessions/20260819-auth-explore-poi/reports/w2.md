# w2 汇报(2026-08-19)

## 实际改动(7 commits,分支 feature/auth-password,均未 merge/push)

- `server/src/lib/password.ts`(新建)→ scrypt 哈希/校验,格式 `scrypt$N$r$p$salt$hash`(N=16384/r=8/p=1,随机 16B salt,`node:crypto` scryptSync + timingSafeEqual,零新依赖);导出 `hashPassword` / `verifyPassword` / `isValidUsername`(2-32 位字母含中文/数字/下划线,`\p{L}\p{N}_`) / `isValidPassword`(≥8 位)。
- `server/src/lib/account.ts` → `AuthProvider` 加 `'password'`;`AccountUser` 加 `username?: string`(仅字段与类型,其余不动)。
- `server/src/lib/session-store.ts`(内存路径)→ 新增 `UsernameTakenError`(冲突 409)、`registerWithPassword`(subject=`password:<username>` 小写,存 username+passwordHash)、`loginWithPassword`(失败统一 null);`publicUser` 剥离 passwordHash 不外泄;`accountLabel` 支持 username。**既有函数行为零改动**(只新增 + 扩 accountLabel 签名)。
- `server/src/lib/account-store.ts`(门面)→ 加 `registerWithPassword`(Postgres:预查 `lower(username)` + 唯一索引兜底,23505 → UsernameTakenError;同时写 `auth_identities (password, <lower>)` 行)与 `loginWithPassword`(按 `lower(username)` + password_hash 查,`verifyPassword` 失败返回 null);`asUser` 支持 username/accountLabel 优先级 phone|email|username;`withDb` 对 `UsernameTakenError` 原样抛出(冲突不是「库不可用」,不能回落内存)。
- `src/app/api/auth/password/register/route.ts` → `{ ok: true, user }` + 写 cookie;400 `INVALID_USERNAME`/`PASSWORD_TOO_SHORT`/`PASSWORD_MISMATCH`(confirmPassword 可选),409 `USERNAME_TAKEN`。
- `src/app/api/auth/password/login/route.ts` → `{ ok: true, user }` + 写 cookie;401 `INVALID_CREDENTIALS`(不泄露账号是否存在)。user 形状与 `/api/auth/me` 一致,accountLabel=username。
- `src/components/auth-modal.tsx` + `.module.css` + `lib/i18n.ts` → 方法 tab 变 手机|邮箱|密码|其他(沿用 `.method`/`.methodActive` 语义);密码 tab 默认登录模式(账号+密码+绿色「登录」+「没有账号?立即注册」蓝链)→ 注册模式(账号+密码+确认密码+绿色「注册」+「已有账号?去登录」);前端先校验长度/一致性显示在 `.error` 区,接口错误按 code 映射 i18n(`USERNAME_TAKEN`/`INVALID_CREDENTIALS` 等);成功走 `onSignedIn()+onClose()`(注册即自动登录);autoComplete username/current-password(new-password);新增 15 组 zh/en i18n 键。
- `db/migrations/014_credentials_auth.sql`(新建)→ 迁移方案:**users 加 `username` + `password_hash` 列**(与 phone/email 同为 profile 字段,改动最小)+ `lower(username)` 部分唯一索引 + provider CHECK 扩 `'password'`(沿用 007 drop/re-add 幂等模式)。**未 apply**(Env-only,boss 记 deferred);代码路径在无库时走内存 store,测试不受影响。
- `server/tests/password.test.mjs`(新建,6 个 test)→ 哈希格式/round-trip/随机 salt/畸形串拒绝;用户名/密码规则;内存注册登录+session 复用+passwordHash 不外泄+大小写不敏感登录+错误密码/不存在用户统一 null;重复用户名抛 `UsernameTakenError`(含大小写变体);account-store 门面(无 DATABASE_URL)成功/失败/409 分支。
- `tech/14-api-contract.md` → Account 节补两条 password 端点契约与迁移/哈希说明(一处,docs-check 绿)。

## 门禁结果

- npm test: 305 通过(303 pass + 2 skip,skip 为既有 DATABASE_URL 门控)/ 0 失败
- typecheck / docs-check / git diff --check: 通过

## 遇到的问题

- **withDb 兜底语义**:`account-store.withDb` 原本 catch 一切错误回落内存,会把「用户名冲突」误当库不可用 → 在 `withDb` 中让 `UsernameTakenError` 原样抛出(冲突必须 409,不能回落)。仅影响新错误类型,既有回落行为不变。
- `.mjs` 测试不能用 TS 语法(首跑 `as` 断言 SyntaxError)→ 去掉类型断言后通过。
- 路由 handler 未做单测:`next/headers` cookies 在纯 node:test 下无法构造请求上下文(现有 tests 也全部只测 store/纯函数层);409/401/400 语义已由 store 层 + 校验函数覆盖,API 形状照 otp/oauth 既有模式。

## 证据

- `npm test`: `ℹ tests 305 / pass 303 / fail 0 / skipped 2`,新增 `tests/password.test.mjs` 6 test 全绿。
- `npm run typecheck`: `tsc --noEmit` 无输出(通过)。
- `make docs-check`: Documentation policy check passed;`git diff --check` 干净。
- commits: 4aadac4 → 5db541c(7 个,Conventional Commits,scope auth),`git status` 干净。

门禁: PASSED
结论: OK
