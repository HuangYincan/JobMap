# WS w3 — 上海试点(shanghai-pilot-data)

> 批次:20260819-data-quality-shanghai-poi | worktree: `/Users/acccan/dm-wt-w3`(已预建,分支 `feat/shanghai-pilot-data`)

## 需求(用户原话)

「拓展、新增真实公司/岗位数据,要求包含北上广深、成都、武汉、苏州、宁波等主流城市,可以先做上海市,先验证一下可行性,后续再全量拓展」

## 现状(已探明,勿再大范围排查)

- 397 家 radar 公司已有 `-shanghai` 站点(city 上海市/province 上海市,城市文本无坐标),207 个岗位挂在 -shanghai 站点上。
- **0 个真实上海坐标**。33 家公司的 -shanghai(及其他城市)站点带坐标,但全是 `fecef85`(2026-08-19 事故恢复)从杭州 office 复制的**错误坐标**(如 快手-site-shanghai = 滨江区星耀中心;禾赛-site-shanghai = 萧山奔竞大道)。audit:pins 会把这些标成 wrong-city。
- geocode 工具链**已是城市级**:`site-geocode.ts` `siteCityTarget`(:52-58)用 site.city,`placeTextSearchRest` citylimit=true(:340-344),regeo 直辖市 province 兜底已处理(:366-368)。**小修**:`site-geocode.ts:60-66`(geocodeQueryForSite)与 `:153-155`(listImportedSitesNeedingGeocode 的 DB 查询分支)硬编码「杭州 ${company}」,需改用站点城市。
- `geocode-overrides.json`(40 条,全杭州)只被 geocode-sites-apply.mjs 消费(keyed by slug,支持 exclude),导入不消费。
- 上海本地公司候选(radar 已有):得物、米哈游、叠纸游戏、哔哩哔哩、拼多多、携程、商汤、上汽系(上汽集团/乘用车/大众/通用-泛亚)、中微公司、联影集团、禾赛科技、燧原科技、智元机器人、傅利叶、云鲸智能、乐鑫科技、芯迈半导体、集益威半导体、上海电气、上海人工智能实验室、春秋航空、泰隆银行、兴业银行、申能集团。
- 试点跑法(boss 合并后执行):`geocode:sites:apply`(城市级,上海站点自动按城市解析)→ 新适配器爬试点公司官网职位 → LLM 质检 → `import:seed:apply` → 验证。

## 任务

1. **geocode 城市化小修**:`site-geocode.ts:60-66` 与 `:153-155` 的 DB 回退查询去掉杭州硬编码,改用 `siteCityTarget(site)`/站点城市;加测试(单测:北京/上海/杭州站点各走对的城市)。照 site-geocode 现有测试模式。
2. **试点公司清单(策展)**:从上述候选 + drops 现有上海站点里选 **10-15 家**作为上海试点(优先:真实上海总部/大办公点明确的知名公司:得物、米哈游、哔哩哔哩、拼多多、携程、商汤、上汽集团、中微公司、联影集团、禾赛科技、燧原科技、智元机器人、上海电气、春秋航空 等,自裁取舍并写明理由)。产出 `tech/roles/data/shanghai-pilot.md`:公司清单 + careerUrl 复核结果 + 各自 -shanghai 站点现状。
3. **错误坐标修正(试点范围内)**:试点公司的 `-shanghai` 站点(及其他城市站点)上来自 fecef85 的杭州坐标 → **清除**(恢复 city-text,等 geocode 解析真实上海 office)。非试点公司**不动**(口径:保持现状,deferred 记录)。
   - 注意:清除只动试点公司;写测试或用脚本校验清除结果(试点公司 -shanghai 站点不再有 lng/lat)。
4. **overrides 预置**:对试点公司中「公司名歧义/易错实体」的,在 `geocode-overrides.json` 预置正确的上海 office(如:得物 → 得物 App 上海总部(黄浦区?自裁,需可验证地址——**宁缺毋滥**,不确定就不预置,留给 geocode 自动解析 + regeo 校验))。exclude 用于确定无上海可验证办公点的。预置项注明依据(公开官网/工商地址)。
5. **文档**:`tech/roles/data/data-quality.md` 或 `shanghai-pilot.md` 写试点数据现状(0 真实坐标 → 待 geocode)与后续步骤。保持 docs-check 绿。
6. **不做**:不跑 geocode/爬虫/导入(Env-only,boss 合并后跑);不删非试点公司坐标;不动 w2 的适配器文件;不做苏州/宁波等其他城市。

## 文件边界

- **拥有**:`server/src/lib/site-geocode.ts`(仅 :60-66/:153-155 区域)、`server/tests/`(geocode 相关)、`server/data/recruitment/radar/`(仅试点公司文件的 -shanghai 等非杭州站点坐标清除)、`server/data/recruitment/geocode-overrides.json`(仅新增试点条目)、`tech/roles/data/shanghai-pilot.md`(新建)。
- **不碰**:其他城市站点、非试点公司、crawler/、map-shell、import 管线。

## 门禁(必须全绿,在 worktree 内跑)

```bash
cd /Users/acccan/dm-wt-w3/server && npm test && npm run typecheck
cd /Users/acccan/dm-wt-w3 && make docs-check && git diff --check
```

## 回报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260819-data-quality-shanghai-poi/reports/w3.md`:试点清单及理由、坐标清除范围(file:line 级)、overrides 新增项与依据、geocode 小修、测试;遇到的问题(若有)。末两行精确 token:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

不要 merge / push / 建分支;不碰主工作树;不要跑 geocode / 爬虫 / 导入。
