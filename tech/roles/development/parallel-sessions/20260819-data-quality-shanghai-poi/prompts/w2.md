# WS w2 — 官网 ATS 适配器:feishu jobs / hotjob / zhiye(official-ats-adapters)

> 批次:20260819-data-quality-shanghai-poi | worktree: `/Users/acccan/dm-wt-w2`(已预建,分支 `feat/official-ats-adapters`)

## 需求(用户原话)

「目前很多公司岗位还是很多个岗位合成一个岗位,然后岗位信息也不真实,请做好数据质检,新数据从官网自动爬取,要求信息来源真实可信」

## 现状与决策依据(已探明,勿再大范围排查)

- 聚合行问题:630 radar 公司 761 岗位中 700 聚合(上游快照的类别标题,非公司官网数据)。**真实 JD 全目录仅 4 条**(群核手工)。已有:LLM 校验工具链(`validate-positions-llm.mjs` + `llm-validate.ts`,用户 key 已配)、聚合标记贯通导入/读路径、`split-aggregates-report.mjs` 拆解计划。**本 WS 不做全量拆分**(那是后续 B2 里程碑),做「官网真实数据获取能力」。
- 官方文档已裁定路径:`tech/roles/data/data-sources.md:10` —— **per-ATS 公开 JSON 适配器是受批准的下一步**(「wire it as another adapter instead of scraping SPA HTML」),mokahr WAF 挡(302 自环,已记录)。
- 现有爬虫:`crawler/app/domain_map_importer/html_jobs.py`(JSON-LD + `<a>` 启发;SPA 空壳失败)、`cli.py:33-67`(official 命令,PoliteFetcher 2s 间隔、robots 门禁、blocklist:boss/nowcoder/xiaohongshu/shixiseng/51job/zhaopin/liepin/lagou)、`official_refresh.py:25-52`(dedupe externalId=`web-{slug_id(title)}`;写 position 无 JD)。
- **ATS 分布**(全 drops careerUrl):app.mokahr.com 141(WAF 挡)、zhiye.com 子域 138、*.jobs.feishu.cn 75、wecruit.hotjob.cn 30+sec 2、问卷星 10(硬伤,不在本 WS 范围)。
- **实测可抓**(boss 已探,样本存 `fixtures/`):
  - `fixtures/feishu-nio.html` — nio.jobs.feishu.cn HTTP 200/179KB(Next.js SSR,含社招字样;职位数据接口/内嵌 JSON 需你从样本里挖)。
  - `fixtures/zhiye-iflytek.html` — iflytek.zhiye.com HTTP 200/35KB(含「校园招聘」字样,服务端渲染?分析样本)。
  - `fixtures/hotjob-st.html` — hr.sensetime.com HTTP 200/12KB(Next.js 壳,可能 SPA;同样分析)。
  - 你也可以用 `curl` 探测样本站的公开 JSON 接口(礼貌、单请求、不绕登录/验证码/限流;不要对 blocklist 域名动手)。
- 数据流:新适配器产出 `SourceCompany` 形状(`server/src/lib/recruitment-source.ts` 的 SourceCompany/SourcePosition)→ drop JSON(如 `server/data/recruitment/official-career/` 或新 `portal-<slug>.json`)→ 现有导入管线。

## 任务

1. **分析样本并定接口**:从 3 个样本 HTML 挖出职位数据的真实来源(内嵌 JSON / 公开 API 端点 / SSR 渲染的岗位列表)。**优先做能拿到「岗位标题 + 真实 JD 文本 + per-job 链接」的平台**。判断各平台可行性,输出「平台 × 数据可得性」结论(写进 ETL 文档)。
2. **实现适配器**(至少 2 个平台,推荐 feishu jobs + hotjob 或 zhiye 中可行的):新模块放 `crawler/app/domain_map_importer/`(如 `ats_feishu.py` / `ats_hotjob.py` / `ats_zhiye.py`),风格对齐现有 `html_jobs.py`/`official_refresh.py`:
   - 输入:company slug + careerUrl + 站点信息;输出:SourcePosition 列表(title/JD/applyUrl/applySource='official'/family 推断),**带真实 JD 文本**(这是本 WS 的价值核心)。
   - 遵守:PoliteFetcher/robots/blocklist/限速复用;失败不中断整批;dry-run 支持(`cli.py` official 模式对齐)。
   - 若样本中职位数据是分页 JSON API,实现分页;注意单请求间隔 ≥2s。
3. **接入 CLI + 导入**:`cli.py` official 命令支持新适配器(按 careerUrl 域名路由到对应适配器);产出 drop 与现有 `plan-seed-import` 兼容(写进 `server/data/recruitment/official-career/` 或验证导入计划 0 issues)。
4. **测试**:crawler/tests/ 用样本衍生的 fixture(trim 到最小、脱敏)覆盖:适配器解析(标题/JD/链接提取)、分页、坏响应降级、domain 路由。照现有 test_imports.py 风格(node 侧照 recruitment-import.test.mjs 风格若涉及导入形状)。`make docs-check` 绿。
5. **ETL 来源审查记录**:`tech/roles/data/etl/` 写 `feishu-ats.md` / `hotjob-ats.md`(或 zhiye):平台、数据可得性、robots/条款观察、抓取规范、样本日期、来源可信度评估。`data-sources.md` 的 Moka 候选行如仍不适用可加注。
6. **不做**:不跑全量网络爬取(Env-only,boss 合并后跑试点);不碰 mokahr(WAF,已记录);不拆聚合行(后续里程碑);不碰 radar drops。

## 文件边界

- **拥有**:`crawler/app/domain_map_importer/`(新适配器文件 + 必要小改 cli.py)、`crawler/tests/`、`tech/roles/data/etl/*-ats.md`、`tech/roles/data/data-sources.md`(加注)。
- **不碰**:`server/src/`(导入管线已有 aggregate/portal 支持,只需产出兼容 drop)、radar/official-career 现有 drops、mokahr。
- fixture 从批次目录读:`/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260819-data-quality-shanghai-poi/fixtures/`(worker 可读);测试 fixture 落到 `crawler/tests/fixtures/`(提交)。

## 门禁(必须全绿,在 worktree 内跑)

```bash
cd /Users/acccan/dm-wt-w2 && make test-unit && cd server && npm test && npm run typecheck
cd /Users/acccan/dm-wt-w2 && make docs-check && git diff --check
```

## 回报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260819-data-quality-shanghai-poi/reports/w2.md`:平台可行性结论(每个平台的职位数据可得性/真实 JD 可得性)、实现的适配器与解析路径(file:line)、测试、ETL 文档;遇到的问题(若有,尤其样本接口挖掘结果)。末两行精确 token:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

不要 merge / push / 建分支;不碰主工作树;不要跑全量网络爬取。
