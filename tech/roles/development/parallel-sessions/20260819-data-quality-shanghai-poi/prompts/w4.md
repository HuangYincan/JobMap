# WS w4 — 登录卡片「新用户将自动注册」小字(auth-auto-register-hint)

> 批次:20260819-data-quality-shanghai-poi | worktree: `/Users/acccan/dm-wt-w4`(已预建,分支 `feat/auth-auto-register-hint`)

## 需求(用户原话)

「在手机注册和邮箱注册底下标志一行小字"新用户将自动注册"」

## 现状

- auth-modal.tsx 手机/邮箱 tab 是 OTP 登录流(`/api/auth/otp/send|verify`,demo 验证码 000000),登录即自动建号(现有语义)。表单尾部是绿色「登录」按钮(auth-modal.tsx:237-244)。密码 tab(上一批 w2 加入)有登录/注册切换,**不加这行小字**(用户只点名手机/邮箱)。
- 布局结构:`.form` 内 tab nav → 字段 → `.login` 按钮 → `.error` 区域。

## 任务

1. 在手机 tab 与邮箱 tab 的登录按钮**下方**加一行 12px 小字(i18n 键 `autoRegisterHint`,zh:「新用户将自动注册」/ en:「New users are registered automatically」)。
2. 样式:小号(12px)、弱化色(用 muted/次级文本色,查 globals.css 的 muted token)、不与 `.error`(红色)混淆;布局上按钮与提示之间有合适间距。仅文字行,无交互。
3. ASCII:
   ```
    [ 登录(green) ]
    新用户将自动注册   ← 12px muted,仅手机/邮箱 tab
   ```
4. 测试:component-contracts.test.mjs 加静态断言(auth-modal.tsx 含 autoRegisterHint 渲染于手机/邮箱分支、i18n 双语文案存在),照现有 regex 风格。保持 `npm test` 全绿。

## 文件边界

- **拥有**:`src/components/auth-modal.tsx`(仅提示行插入点)、`src/components/auth-modal.module.css`(仅新 class)、`src/lib/i18n.ts`(仅新键)、`tests/component-contracts.test.mjs`(仅新增断言)。
- **不碰**:密码 tab、其他组件、行为逻辑。

## 门禁(必须全绿,在 worktree 内跑)

```bash
cd /Users/acccan/dm-wt-w4/server && npm test && npm run typecheck
cd /Users/acccan/dm-wt-w4 && make docs-check && git diff --check
```

## 回报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260819-data-quality-shanghai-poi/reports/w4.md`。末两行精确 token:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

不要 merge / push / 建分支;不碰主工作树。
