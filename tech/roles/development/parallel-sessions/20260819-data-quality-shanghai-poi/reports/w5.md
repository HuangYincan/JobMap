# w5 汇报(2026-08-19)

## 实际改动

**方案选择:A(保留 fit-to-pins UX,相机移动前开抑制窗口吞掉视口刷新)**

- `server/src/components/map-shell.tsx`
  - 模块常量 `VIEWPORT_SUPPRESS_MS = 500`(置于 DrawerState/RailPanel 类型声明后,:70-72)
  - ref `suppressViewportRefreshUntilRef = useRef(0)`(:235,紧随 viewportRefreshPendingRef)
  - `onViewChange`(:1036-1040)加 3 行抑制检查:当前时间在抑制窗口内 → return 不 schedule;
    窗口过期自动失效,不影响后续用户操作触发的视口刷新
  - `handleToggleSavedOverlay`(:1398-1401)相机移动前置位
    `suppressViewportRefreshUntilRef.current = Date.now() + VIEWPORT_SUPPRESS_MS`,
    覆盖 setBounds 主路径与 setCenter fallback(:1406/:1412)
- `server/tests/component-contracts.test.mjs`:新增静态契约「saved overlay toggle:
  programmatic camera move suppresses viewport refresh (saved-overlay-wipe)」——
  常量/ref 存在;onViewChange 窗口内跳过 schedule;toggle 函数体内(indexOf 限定
  handleToggleSavedOverlay→handleModeChange 区间,避免误中 flyToLocation 的 setCenter)
  抑制标记先于 setBounds 与 setCenter fallback 置位
- `tech/16-bug-fixes.md`:新增「2026-08-19: 收藏图层启停导致所有 POI 消失」小节
  (症状/根因链/方案 A 修复/时间窗口而非一次性标记的原因/修改文件/测试验证)

## 空批次保护归属(任务 2)

**w1 承担,w5 已确认**。w1 prompt 任务 6 明确要求 w1 在视口 loader onBatch
(map-shell.tsx :960-961 work / :1008-1009 domain)实现「已有非空目录时新批次为空 →
保留旧目录」;manifest 同文件分区约定 w1 拥有 749-1039、w5 仅 toggle 1376-1398 +
onViewChange 1029 加抑制钩子。w5 未重复实现,避免与 w1 区域冲突(合并顺序 w5 在 w1 前)。
handlePickSaved 同型风险(单 pin flyTo 空视野清空)由该保护兜底。

## 门禁结果

- npm test: 332 通过 / 0 失败 / 2 skip(契约测试首版曾因全局 indexOf 误中
  flyToLocation 的 `map.setCenter?.(`(:137)失败——`setAt < centerAt` 断言全局序;
  改为限定 toggle 函数体区间后全绿)
- typecheck / docs-check / git diff --check: 通过

## 遇到的问题

- 程序化相机移动的事件时序:setBounds 会连续触发 moveend + zoomend 双事件,
  prompt 建议的「一次性清除标记」会被第二个事件重新调度而失效 → 改用
  500ms 时间窗口(两事件均落在窗口内被吞,自动过期)。若后续需要更精确可改为
  per-gesture 计数器,但窗口方案足够且简单。
- 汇报与测试输出:全套件输出过大被工具截断 → 用 `--test-reporter=dot` + persisted
  output 文件核验 `# fail 0`。

## 证据

- `npm test` 摘要:ℹ tests 332 / pass 330 / fail 0 / cancelled 0 / skipped 2 / todo 0
  (persisted output: tool-results/btvj1kw60.txt 尾部)
- 契约测试(component-contracts)全 ✔;typecheck 无输出;docs-check passed;
  `git diff --check` 无输出
- commits(worktree `fix/saved-overlay-wipe`,基于 dev `5fa67e2`):
  - fdc3911 fix(w5): suppress viewport refresh during saved-overlay toggle camera move
  - 013cbac test(w5): contract asserts suppress marker precedes toggle camera move
  - e573772 docs(w5): record saved-overlay-wipe incident + suppress fix in bug-fixes log

门禁: PASSED
结论: OK
