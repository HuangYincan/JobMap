# WS w3(prefs & switcher)— defaultMap / industries 选项 + 模式名 + searchPlaceholder 英文

## 背景

用户点名的问题有三处在账户偏好与模式 UI:
1. **default map 选项**(「默认地图」下拉):`getMode(m).name`(中文「地图/工作」)— `ModeConfig.nameEn` 已存在,直接用。
2. **industries**(「意向行业」):选项与触发钮文本用 `INDUSTRY_OPTIONS` 的中文 `label`。
3. **mode name 的 title/aria + 搜索框 placeholder**(switcher hover/无障碍 + 两个搜索框)。

w1(并行开发中)会给 `FilterOption` 加 `labelEn?: string` 并在 i18n.ts 加 `uiLabel(o, lang)`(en 时返回 labelEn,缺失回退 label)。`ModeConfig.nameEn` / `searchPlaceholderEn`(w1 将给 ModeConfig 加 `searchPlaceholderEn?: string`)同契约。**本 WS 只改这四个组件文件,不碰 lib/。**

## 工作环境

- worktree(已预建,当前分支 `feature/i18n-option-labels-prefs`):`/Users/acccan/dm-wt-i18n-prefs`
- 汇报写入:`/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-i18n-option-labels/reports/w3.md`
- node_modules 已 symlink,直接 `cd server && npm test` 可跑。
- **不要 merge / push / 切分支**;boss 统一合并。小步 Conventional Commits(`feat(i18n): …`)。
- worktree 从 `dev` 切出,`searchPlaceholderEn` / `uiLabel` 可能尚不存在 → 按契约写,typecheck 契约缺失问题在汇报中说明。

## 契约(以 w1 为准,冻结)

```ts
// server/src/lib/i18n.ts(将由 w1 新增)
export function uiLabel(o: { label: string; labelEn?: string }, lang: Language): string;
// server/src/lib/modes.ts(将由 w1 新增)
// ModeConfig: searchPlaceholderEn?: string
// INDUSTRY_OPTIONS 各项: labelEn: 'Internet' | 'Finance' | ...
// ModeConfig.nameEn 已存在: domain → 'Domain', work → 'Work'
```

## 任务

### 1. `server/src/components/account-panel.tsx`

- `defaultModeText`(约 381 行):`getMode(prefs.defaultMode).name` → 英文 UI 用 `nameEn`:
  ```ts
  const defaultModeText = lang === "en" ? getMode(prefs.defaultMode).nameEn : getMode(prefs.defaultMode).name;
  ```
- defaultMode 下拉选项(约 456-458 行):`ACTIVE_MODES.map((m) => ({ id: m, label: getMode(m).name }))` → label 用 `lang === "en" ? getMode(m).nameEn : getMode(m).name`。
- `industriesText`(约 366-372 行):`item.label` → `uiLabel(item, lang)`(import `uiLabel` from `@/lib/i18n`)。
- industries 下拉选项(约 445 行):`INDUSTRY_OPTIONS.map((i) => ({ id: i.value, label: i.label }))` → `label: uiLabel(i, lang)`。
- **验证不改动**:`languageText`(「中文」/「English」— 语言名不翻译,保留);`statusText`/`familiesText`/`strengthsText` 已走 `t(s.labelKey, lang)` ✓;`emptyText`/`countText` 已有 lang 分支 ✓。

### 2. `server/src/components/mode-switcher.tsx`

- 约 118-119 行 `title={config.name}` / `aria-label={config.name}` → `lang === "en" ? config.nameEn : config.name`(组件是否已有 lang?若无,检查它从哪拿语言——如 props 或 i18n 的 `getBrowserLanguage()`;选最贴近现有代码的取法,保持架构一致)。

### 3. `server/src/components/secondary-sidebar.tsx` + `server/src/components/map-shell.tsx`

- secondary-sidebar 约 358 行、map-shell 约 2118 / 2491 行:`placeholder={modeConfig.searchPlaceholder}` → en 时 `modeConfig.searchPlaceholderEn ?? modeConfig.searchPlaceholder`(两个文件都改,map-shell 有两处)。

## 不碰

- lib/** 全部;filter-panel.tsx / sort-selector.tsx(w2 负责)
- 不改布局/交互/样式;不改组件 props;不跑 env 步骤

## 门禁(必须全绿)

```bash
cd /Users/acccan/dm-wt-i18n-prefs/server && npm test
cd /Users/acccan/dm-wt-i18n-prefs/server && npm run typecheck
cd /Users/acccan/dm-wt-i18n-prefs && make docs-check && git diff --check
```

若 typecheck 因 `searchPlaceholderEn`/`uiLabel` 尚不存在而报错:在汇报中如实说明,commit 代码标注「等待 w1 合并后验证」。

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-i18n-option-labels/reports/w3.md`:
- 改动点清单(file:line)+ 遇到的问题(尤其 typecheck 契约依赖、mode-switcher 的 lang 取法)
- 末两行必须精确:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```
