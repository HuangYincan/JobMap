# w1 汇报(2026-08-21)

## 实际改动

**前置发现(幂等对账):本 WS 主体工作已在 dev 中,非重复开发。** worktree 建于 dev tip(`7c7acec`),`git log dev..HEAD` 为空;经 `git log --all --grep=tencent` 核实,腾讯第三级兜底已由此前批次完成并合并进 dev:
- `01c2ec9 feat(geocode): 腾讯 WebService 第三级兜底 — tencent* 三函数 + fallbackChain 链式改写(AMap→百度→腾讯)`(合并 `21c430e`)
- `e9a3049 test(geocode): 腾讯兜底链用例`(同批次合并)
- 后续相关:`b6bf105`(311 真实探测校准)、`8159167`(provider 注册表 + REPORT)

按铁律 8 不重做;逐条核对 prompt 要求后,补了唯一缺口(见下),其余验证通过。

- `server/tests/site-geocode.test.mjs` → 新增 1 个测试 `tencent endpoints surface http failures (res.ok / thrown fetch) as reason http`(29 行,node:test 风格):正向 geocode / place 检索 / regeo 三端点各覆盖 `res.ok=false` 与 fetch 抛错两条网络失败路径,断言 reason 归属 `http`(place 同时断言 `pois=[]`)。commit `7358a13 test(geocode): tencent 三端点 http 失败路径(res.ok=false / fetch 抛错 → reason http)`。
- prompt 要求的其余测试点(成功解析 / status 非 0 / 链式顺序 / 短路兼容 / 直辖市 regeo)dev 已有覆盖,核对清单见「证据」。

## 门禁结果

- npm test: **1027 通过 / 0 失败 / 2 skip**(prompt 基线 754 已过时,dev 后续批次使套件增长)
- typecheck: 通过(tsc --noEmit 无输出)
- git diff --check: 通过
- make docs-check: **失败(与本 WS 无关,dev tip 既有问题)** — `tech/roles/development/parallel-sessions/20260821-boss-agent-thinkfix/merge-report.md:20` 含自匹配 grep 正则文本(该文件经 `7c7acec` 已并入 dev;其报告原文自述「合并前已存在」)。tech/ 在我「不碰」边界内,按 prompt 记入汇报由 boss 处理。

## 遇到的问题

1. **任务重复派发**:prompt 背景称「geocode 链当前为 AMap→百度双兜底」,但腾讯三级兜底已于 2026-08-21 早些批次实现并合并进 dev(见上)。处理:按幂等规则验证既有实现 + 补缺口,不重做。若 boss 本意是让本批独占该功能,需与 merge-agent 对齐(dev 已有,本分支无实现冲突)。
2. **docs-check 失败为 dev tip 既有问题**(agent-thinkfix merge-report 自匹配行),超本 WS 文件边界 → 记入汇报,建议 boss 派 docs 修复批次或由 merger 一并处理。
3. **worktree 无 `.env.local`**(主树才有):key 验证时从 `/Users/acccan/domain-map/server/.env.local` 读取,未打印 key。

## 证据

**实现核对(prompt 逐项)** — `server/src/lib/site-geocode.ts`(均在 dev 中):
- `tencentWebKey()` L218-221(读 `process.env.TENCENT_MAP_KEY`,trim 后非空)
- `tencentGeocodeAddressRest(query, city, fetchImpl)` L1199-1237:`ws/geocoder/v1` 正向,`cityQualifiedAddress` 前缀拼城市,`region` 参数消歧;status≠0 → `tencent-status:N`
- `tencentPlaceSearchRest(query, city, fetchImpl)` L1124-1161:`ws/place/v1/search`,`boundary=region(<city>,0)`;解析 `data[0].location.lat/lng` + title/address
- `tencentRegeoCityRest(lng, lat, fetchImpl)` L1164-1196:`ws/geocoder/v1` 逆向,`location=lat,lng`(纬度在前);解析 `result.ad_info.city/district/province`
- 兜底链 `fallbackChain` L523-535:AMap 无 key 或配额耗尽(10044/10043)时,先试百度再试腾讯;百度成功则腾讯不调用;两兜底都缺 → noFallback('no-key'/'quota');`amapUnavailable` 语义保持;成功 provider 记为 `'tencent'`。三个主函数(geocodeAddressRest L543 / placeTextSearchRest L811 / regeoCityRest L886)均接入
- 限流:腾讯 120(每秒限流)内部重试 1 次(1.5s);其余失败交给调用方 sleep ≥340ms(scripts 层,不在本 WS 边界)
- 短路兼容:`QUOTA_CLASS_REASONS` L293-306 含 `tencent-status:121/321/322/110/112/190/199/311`;`tencentQuotaExhausted` L272-275 判 121/321/322;`isQuotaClassReason`/`shouldShortCircuitQuota` 复用同一集合,连续 5 站配额类失败即短路

**测试清单(dev 已有 + 本次新增)**:
- 既有:`tencentQuotaExhausted` 状态族 / `parseTencentOfficePoi`(title/location 对象/ad_info,缺 location → null)/ `tencentGeocodeAddressRest` 解析+城市前缀拼接 / `tencentPlaceSearchRest` boundary=region 参数+data[] 解析 / `tencentRegeoCityRest` lat,lng 顺序+ad_info 解析 / 120 重试一次 / 链式:AMap 10044 + 百度 302 → 腾讯成功(provider='tencent',amapUnavailable=true,tencentCalls=1)/ 腾讯-only(AMap、Baidu key 全缺)/ reason 归属 `tencent-status:121` / 百度成功时不调腾讯 / 无 key no-op('no-key')/ 短路:`tencent-status:121/321/322/110/112/190/199/311` 均配额类、120 非配额类、连续 5 触发 / 直辖市 regeo:cityname 空 → province 兜底(regeoMatchesTarget)
- 本次新增:`tencent endpoints surface http failures`(三端点 × res.ok=false + fetch 抛错)

**key 验证(2 次真实请求,腾讯官方 Web 服务 API + 用户 key)**:
- `GET ws/geocoder/v1?address=杭州市西湖区文一西路969号` → **status 121,message「此key每日调用量已达到上限」**(无 result)
- `GET ws/place/v1/search?keyword=得物&boundary=region(上海市,0)&page_size=3` → **status 121,同上**
- 结论:key 有效(非 110/112/311 配置类错误),但**今日日配额已耗尽** — 恰好真实校准了配额类映射:121 即 `tencent-status:121` 配额类,当前生产环境下腾讯兜底会如实进入短路判定。成功响应形状(result.location / data[].location+ad_info)无法用真实请求验证,由 mock 单测钉住;311 key 格式错误此前已真实探测校准(b6bf105)。未跑全量 geocode(网络留给用户)。

门禁: FAILED
结论: OK
