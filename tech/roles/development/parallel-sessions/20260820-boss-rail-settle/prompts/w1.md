# Workstream w1 — fix/settle-user-interaction-gate(settle 门控加「用户已交互」ref)

## 你的身份

你是 boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。** 汇报写入批次目录 reports/w1.md(末两行 token,见文末)。

## 背景

用户报告 bug:「第一次点击侧控栏任何会弹出二级卡片的 item(Profile/Saved/Recent/Layers)时,整个网页刷新一下」。用户补充「复现取决于视角,绝大多数情况触发」。**上一轮修复(挂载预载面板 chunk)已被用户证伪**——bug 与 dev chunk 编译无关。

**根因已由 boss 三重确认**(代码深挖 + 双环境实测),你不必重查,但欢迎抽查:

### 机制:geolocation settle 相机跳变

文件:`/Users/acccan/dm-wt-w1/server/src/components/map-shell.tsx`

1. `createMap`(L500 起,唯一 `new window.AMap.Map` 调用点在 L507)内,geolocation resolve 后(约 L531-553):
   - 门控 L544:`!userMovedMapRef.current && isNearDefaultCenter(map.getCenter())`(lib/camera-center.ts 的 isNearDefaultCenter,0.1°≈11km 阈值);
   - 命中 → L545-547:`map.setCenter([lng,lat]); map.setZoom(15); setMapCenter({lng,lat})` —— **瞬间、无动画、整幅相机跳变**;
   - 跳变后果:全屏地图(inset:0)瓦片整体重载 + 距离圈重建(L1073-1076)+ work 列表客户端重裁(pois memo L1164-1182)+ 聚合/LOD 切换(L1258/1319)—— **一帧内整个页面内容全部变化,观感 = 「整页刷新」**。
2. **「取决于视角」对应**:门控 `isNearDefaultCenter` —— 用户拖过图(视角离开默认中心)→ 不触发;大多数用户不拖 → 触发。
3. **「首次点击」关联**:跳变时机完全由 geolocation resolve 决定(权限弹窗 / 8s 超时 / 30s maximumAge 缓存),**恰落在用户首交互时间窗**。实测:geolocation 授权 + 延迟 resolve 的 Playwright 环境,页面加载 1.2s 时 zoom=13(未跳),resolve 时刻 zoom 瞬间 13→15。无 geolocation 权限的环境(此前批次)永不跳变 → 无法复现。
4. React 层无 remount(createMap effect 依赖 `[]`)、无浏览器 reload(实测 navType 恒 navigate)—— 已排除。

## 任务

**settle 门控增强**:增加「用户已交互」门控。

1. 在 map-shell.tsx 新增 ref(如 `userInteractedRef`,模式对齐现有 `userMovedMapRef` L202 的注释风格)。
2. 在 MapShell 挂载 effect(现有 mount useEffect,L402-413 附近,注意 51c0406 已在其中加了 prefetchAllRail,别动它)注册**一次性** document 级监听:`pointerdown` / `keydown` / `touchstart` / `wheel`(任一中事件即置位 `userInteractedRef.current = true` 并移除全部监听;wheel 注意 passive:true)。
   - 事件监听要在合适的作用域注册(挂载后立即生效),避免重复注册(严格一次性,用 `{ once: true }` 或注册后手动 removeEventListener,两种都要)。
3. settle 门控(L544)改为:`!userMovedMapRef.current && !userInteractedRef.current && isNearDefaultCenter(map.getCenter())`。
4. **不得改动**:settle 的跳变动作本身(setCenter/setZoom)、isNearDefaultCenter 逻辑、userMovedMapRef 语义、dynamic 声明与挂载预载(51c0406 的 RAIL_PANEL_MODULES/prefetchAllRail)、任何 UI 样式/交互。不加动画不改视觉。
5. 行为语义(保持):用户加载后**从未交互**(没点过/没按过键/没滚过)→ resolve 时依然自动定位;用户**已交互**(首点 rail 等)→ 不再抢镜头(用户需要定位点「定位」按钮,handleLocate L1731-1752 已有)。

注意:
- 首点 rail 本身不置位 `userMovedMapRef`(这是刻意的相机语义,别动);新 ref 是**独立**的「用户是否进行过任何交互」信号,只服务 settle 门控。
- 检查是否已有现成的「首次交互」监听/ref 可复用(如 ignoreNextMapClick 等),有则优先复用语义,没有就新建。
- 代码注释说明:为什么加这个门控(geolocation resolve 可能晚于首交互,导致点击后整幅跳变 = 「整页刷新」观感)。

## 文件边界

- **只允许改**:`server/src/components/map-shell.tsx`。
- **不碰**:其他一切文件(lib/camera-center.ts、各面板、样式、tech/ 文档;本改动属内部实现,boss 判断无需文档变更;若 docs-check 因故要求,在汇报「遇到的问题」段说明,不要擅自大改)。

## 门禁(必跑,全部通过)

1. `cd /Users/acccan/dm-wt-w1/server && npm test`(基线:500 pass / 2 skip,不得低于)
2. `cd /Users/acccan/dm-wt-w1/server && npm run typecheck`
3. `cd /Users/acccan/dm-wt-w1 && make docs-check`
4. `git diff --check`

## 可选行为验证(做不了不强求,如实说明)

worktree 的 node_modules 是 symlink,Turbopack dev 无法启动(已知);webpack dev 语义不同。boss 会用 Playwright + geolocation 授权在合入后复验。若你想自行验证,可用 `next dev --webpack -p 3999`(worker 上一批已验证可启动),但注意 webpack 下 geolocation 行为一致,可观测 zoom 跳变门控。

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-rail-settle/reports/w1.md`:改动摘要(diff 概述 + 关键行号)、门禁结果(每项一行,真实数字)、遇到的问题(若有)。**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

## 约束重申

- 不 push、不 merge、不切分支(分支已预建)。
- Conventional Commits(建议:`fix(map-shell): settle 自动定位加「用户已交互」门控,消除首点整幅跳变` 或类似)。
- 不改现有 UI 设计;如需改设计语义,停下写 BLOCKED。
