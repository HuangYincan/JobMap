# w10 汇报(2026-08-22)— fix/geocode-grader-relax(office POI 匹配放宽:限定词组合序列)

## 实际改动(commit `fafaf9b`,叠加于 boss 6193ba1 之上,分支 fix/geocode-grader-relax)

- `server/src/lib/site-geocode.ts`
  - `officeNameMatchStrength` 的 `matches()`:suffix/prefix 从「单个限定词 token」放宽为
    「**限定词 token 序列**」(整段必须完全由集合 token 拼接而成):
    - suffix 序列 = QUALIFIER_SUFFIXES token + 城市名 token(快手北京 类既有行为并入序列,
      城市名也参与复合:杭州研究院 = 杭州+研究院);最长 token 优先(研发大厦 → 研发|大厦,
      研究院 单段 3 字优先于拆段;集合外前缀不存在,天然不会 研发大|厦);
    - prefix 序列 = 城市名 token(可带 省/市 后缀,北京市 → 北京)+ 品牌拼音 token
      (连续 ≥2 位小写字母,ROMAN_PREFIX_RE 语义);
    - **假阳性防线不变**:非限定词 token(包装/实业/造型/鱼庄/驿站/店/站/旗舰店…)
      混入 → 整段拒绝;精确同名(两侧皆空)保持 strong(既有测试 得物=得物,见问题 3)。
    - 新增 `isQualifierPrefixSeq` / `isQualifierSuffixSeq`(线性 while 拼接,不递归,
      超长串不崩);删除旧单 token `isQualifierPrefix` / `isQualifierSuffix`。
  - `GOOD_BRACKET_SEG_RE` 增加 `分公司|公司|科技|研发|基地|大楼|学校`(允许表语义,
    不在表内即拒 → 分店/旗舰店/体验店/站/驿站 括号段仍拒收,无需显式排除)。
- `server/tests/site-geocode.test.mjs` — 新增 2 个测试:
  - 复合序列接受组(百度研发大厦/百度大厦/百度科技公司/百度研究院/4 段长串
    百度研发大厦总部基地/得物大厦/快手北京/上海燧原科技/百度智图 子品牌)+ 拒收组
    (广州得物包装实业/百度鱼庄/百度造型/纤百度造型/百度造型(分店002)/得物旗舰店/
    某司驿站/百度研发鱼庄 复合中混入/北京百度智图科技有限公司(上海分公司)vs 百度);
  - 括号段扩展组((上海分公司)/(北京研发)/(软件园基地)/(总部大楼)接受;
    分店002/旗舰店/体验店/驿站 拒收)。
- `server/tests/geocode-address-first.test.mjs` — 2 处旧严格行为断言更新
  (边界外,见问题 2)。

## 门禁结果

- npm test: **1376 通过 / 0 失败 / 2 skip**(基线 1374/0/2;site-geocode.test.mjs 62→64)
- typecheck: 通过
- docs-check: 通过
- git diff --check: 通过

## 遇到的问题

1. **boss 案例自相矛盾(需 boss 裁决)**:「北京百度智图科技有限公司(上海分公司)→ strong」
   与规则文本「整段必须完全由集合 token 组成」**互斥**。该 POI 对公司名 `百度` 的认领后缀是
   `智图科技` — `智图` 是非限定词 token,与必须拒收的 `广州得物包装实业有限公司` 中的 `包装`
   结构完全相同(公司名后 2 字品牌/业务词 + 限定词/法律形式);任何接受 `智图` 的规则都会同时
   接受 `包装` → 突破 2026-08-19 防线。按规则文本实现:该 POI 对 `百度` = **no**;对子品牌名
   `百度智图` = **strong**(城市前缀 北京 + 限定词 科技 + 括号 分公司,已入接受测试)。r5 实际
   影响 ≈ 0:百度站点的 POI 命中由 `百度研发大厦`(本次已修复 strong)承担。若 boss 确要
   「智图-with-百度」通过,需授权追加容忍规则(如「品牌词 + 科技/集团/研究院 类限定词」例外),
   当前未实现。
2. **文件边界外测试更新(预授权条款)**:放宽后 `网易杭州研究院`(杭州+研究院 城市+限定词复合)
   被裸公司名单级评分接受 → `geocode-address-first.test.mjs` 2 处断言旧严格行为
   (gradeVariantHit low / pickBestOfficePoi undefined)转红。按任务「若既有测试断言旧严格行为,
   更新为新语义(与 w2 先例一致,更新时在汇报列出)」条款更新为 high/命中 —— 动了边界外文件,
   此为本文件边界唯一例外。
3. **「两者都空不允许」未采纳**:精确同名(候选名 = 公司名,得物 vs 得物)保持 strong ——
   既有测试如此断言、且精确同名 POI 是最强真实办公室认领,拒绝只增 r5 假阴性;按
   「既有行为不回退」保留。若 boss 确要改,是一行改动。
4. **科技园/产业园 类复合未覆盖**:`园` 不是独立 token(加入会误收 果园/生态园 类),且
   QUALIFIER_SUFFIXES 无 `科技园/产业园` 整词 → `百度科技园` 类 POI 仍拒。任务未列此案例,
   token 集合按 spec 未动;如需覆盖需加整词 token(GOOD_BRACKET 已有 科技园/产业园 先例)。

## 证据

- **r5 预期尝试数**(只读,无网络):`node scripts/plan-site-geocode.mjs` → `needs = 1248`
  (与 prompt 一致;grader 改动不影响 plan 计数,影响的是 apply 命中率 — 831 no-result 中
  复合限定词类转为命中)。
- 测试输出摘要:site-geocode.test.mjs 62→64 pass;全量 1376 pass / 0 fail / 2 skip。
- commit:`fafaf9b fix(geocode): office POI 名称匹配放宽 — 限定词 token 序列 (w10)`
  (3 files changed, 134 insertions, 18 deletions)。

门禁: PASSED
结论: OK
