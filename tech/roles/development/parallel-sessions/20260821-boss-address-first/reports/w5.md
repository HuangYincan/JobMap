# w5 汇报(2026-08-22)

WS: fix/embodied-loc-contract,commit `71714f6`,worktree `/Users/acccan/dm-wt-testfix`(分支 `fix/embodied-loc-contract`)。

## 实际改动

- `server/tests/embodied-jobs-drops.test.mjs`(:99-111 段)→ 更新 embodied 站点 location 形态契约:
  - 旧:location 必须为空对象或仅含 `address`,`locKeys` 长度只能为 0 或 1,不得混入 lng/lat;
  - 新:允许三种形态 —— 空对象 / 仅含 `address` / `{address, lng, lat}`(geocode:sites:apply 写回坐标形态,
    键集精确匹配 `address+lat+lng`);语义上禁止「仅坐标无地址」的畸形形态(防丢地址)。
  - 下游断言不变:`site.location.address` 非空抽查、45 带 address / 2 空对象计数、Tesla/柏楚/浙江大学
    抽查、301 岗/537 externalId 等全部保持原样。
  - 注释同步更新为「地址回填 + geocode:sites:apply 后契约」。

## 门禁结果

- npm test:1254 通过 / 0 失败 / 2 skip(基线一致)
- typecheck:通过
- docs-check:通过
- git diff --check:通过

## 遇到的问题

无。说明(逻辑推演,未实际验证主树):用户在主树工作区跑 `geocode:sites:apply` 后,
部分 embodied-jobs drop 的 location 变为 `{address, lng, lat}`,旧断言「不得混入 lng/lat」导致主树
npm test 1 红;本次更新使两种数据形态(纯 address 与 geocode 后形态)都合法,主树在用户产物存在时
测试同样全绿。主树产物是用户工作区,未触碰。

## 证据

- `npm test` 尾部:`ℹ pass 1254 / fail 0 / skipped 2`
- `npm run typecheck`:`tsc --noEmit` 无输出(干净)
- `make docs-check`:`Documentation policy check passed.`
- `git diff --check`:干净;commit `71714f6`,1 file changed,9 insertions(+),5 deletions(-)

门禁: PASSED
结论: OK
