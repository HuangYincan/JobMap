# w3 汇报(2026-08-22)

WS: fix/geocode-qqdoc-embodied — apply 覆盖 qqdoc/embodied 源。worktree
`/Users/acccan/dm-wt-geo-ext`(分支 `fix/geocode-qqdoc-embodied`,基于 dev 790682e)。

## 实际改动

- `server/scripts/geocode-sites-apply.mjs` → `dropFiles()` 扫描目录从 2 源扩到
  **5 源**:`[RADAR_DIR, OFFICIAL_CAREER_DIR, QQDOC_JOBS_DIR, QQDOC_OFFICIAL_DIR, EMBODIED_JOBS_DIR]`
  (复用各 adapter 已导出的 `*_DIR` 常量,未新增任何 DIR 常量、未动 adapters);
  新增 3 行 import。配额短路 / memo / regeo 双闸门 / override 优先级 /
  confidence 门 / 写回逻辑零改动(纯目录扫描扩展)。函数体上方注释记录 w3 背景。
- `server/tests/geocode-dropfiles-coverage.test.mjs`(新增,167 行,4 用例):
  - **契约测试**(源码 regex,沿用 geocode-plan-count.test.mjs 模式——脚本顶层
    跑主循环 + 真实网络无法 import):`dropFiles()` 数组字面量含全部 5 个
    `*_DIR` token + 3 个新 import 行真实存在;
  - **运行时测试**:锚定 cwd=server(脚本运行契约)后动态 import 5 个 adapter
    常量,断言互不相同、锚定 `server/data/recruitment/`、真实存在且含 json drops;
  - **dry-run 计划测试**:`planSiteGeocode` 对三源真实形态样例(qqdoc-official
    中国矿产资源集团 city_pending、qqdoc-jobs 新东方西安学校、embodied-jobs
    高仙机器人)生成 needs,query = `<地址> <公司名>`(地址 geocode query);
  - **city_pending 处理测试**:脏 city 不崩——`addressConflictsWithCity` 放行、
    真实 regeo(河北省/雄安)vs 脏 city target → `outside-province:河北省` 跳过。
- `tech/roles/data/data-quality.md` → 追加 w3 小节「geocode:sites:apply 覆盖
  5 源(2026-08-22,`fix/geocode-qqdoc-embodied`,w3)」。

## dropFiles 覆盖清单

radar / official-career / qqdoc-jobs / qqdoc-official / embodied-jobs(5 源)。

## qqdoc-official city_pending 站点处理说明

`qq-中国矿产资源集团-site-hq`(city_pending: true,city =「中国矿产资源集团总部」
脏值,address =「河北省雄安新区启动区中谷甲1号」)照常进 geocode 计划:有街道地址
→ 走 `geocodeAddressRest` 地址检索分支;地址-城市一致性闸门对脏 city 放行(无
已知区县冲突判定,纯 regex 不抛);regeo 城市闸门(`regeoMatchesTarget`)把落点
(河北省/雄安)与脏 target(浙江省 fallback)比较 → `outside-province` 跳过、记
unresolved。**不崩脚本、不写错坐标**,符合任务预期(留待 city 修正后自然解)。
同类 2 站 city-unresolved(qq-三菱东京日联银行 海外地址)同路径处理。

## 测试用例清单(新增 4 个)

1. `dropFiles: 契约 — 扫描数组列出全部 5 个源目录常量`
2. `5 源目录常量互不相同且真实存在、含 json drops (dropFiles 运行时扫描)`
3. `planSiteGeocode: qqdoc-official city_pending + 脏 city 站点照常生成 needs (不崩)`
4. `planSiteGeocode: qqdoc-jobs / embodied-jobs 有地址站点 → 地址 geocode query`

## 门禁结果

- npm test: 1155 通过 / 0 失败 / 2 skip(基线 1149 pass/2 skip + 新增 4;
  `node --test --test-reporter=dot tests/*.test.mjs` 全绿,exit 0;首轮 exit 1 为
  本 WS 测试自身两处 bug——契约测试误用常量值对比 token、运行时测试依赖 cwd——
  已修复后全绿,非既有用例失败)
- typecheck: 通过(`npm run typecheck` 无输出)
- docs-check: 通过(`make docs-check` → Documentation policy check passed)
- git diff --check: 通过

## 遇到的问题

- 无遗留问题。测试写作两处自纠:(a) 契约断言应比 token 名而非解析后的目录
  路径;(b) adapter `*_DIR` 按 `process.cwd()` 解析,测试需先 `chdir(server)` 再
  动态 import 才能与脚本运行契约一致。已在测试内注释说明理由。
- 未重构 dropFiles 为可导出函数:脚本顶层有副作用(打桩 fetch + 主循环),沿用
  仓库既有「源码契约 + 目录常量运行时验证」双模式测试,未动 src/lib 边界。

## 证据

- 新增测试单独运行:`node --test tests/geocode-dropfiles-coverage.test.mjs` →
  tests 4 / pass 4 / fail 0。
- 全量:`cd server && npm exec -- node --test --test-reporter=dot tests/*.test.mjs`
  → 全点无 ✖(1155 测试,exit 0)。
- 提交:`8dc5e43 feat(geocode)` + `3d92ecb test(geocode)` + `4214c2d docs(data)`。

门禁: PASSED
结论: OK
