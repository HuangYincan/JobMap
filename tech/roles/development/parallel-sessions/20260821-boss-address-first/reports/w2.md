# w2 汇报(2026-08-22)

WS: `fix/address-backfill` — 373 个无地址站点回填地址 + city 脏值修正

## 实际改动

- `server/data/recruitment/{qqdoc-jobs,qqdoc-official,embodied-jobs}/*.json`(311 文件)→
  - **342 站点回填 `location.address`**(自 17 个 subagent 检索批次 `results/batch-01..17.json`,主仓库只读;`--add-dir` 只读引用,未改主仓库)。`location` 已含 `lng/lat` 的站点用展开保留坐标,`{}` 空对象直接赋 `{ address }`。
  - **203 站点 `city` 脏值修正**:多城市文本(「上海 新加坡」「北京 上海 深圳…」「全国」)、「XX总部」占位、区级文本 → 按确定性规则提取的单城市全称(「北京市」「深圳市」「Seattle, WA」等)。
  - **2 站 city-unresolved 保留原值**(`qq-三菱东京日联银行-site-hq` 地址「日本东京都…」、`qq-中国矿产资源集团-site-hq` 地址「河北省雄安新区…」——不在 `official-site-parse.ts` CITY_TABLE,受 qqdoc-official 已知城市全称契约限制,地址仍已回填)。
  - **31 站无地址**保持原样(子代理未检索到),清单见下。
- `tech/roles/data/data-quality.md` → 新增「无地址站点网络检索补全(2026-08-22)」小节:373 站点、命中率、聚合规则、city 修正规则、剩余 null 全量清单、来源 URL 存放位置。
- 脚本(一次性,在 /tmp,不入库):解析 398 条结果 → 按 `city-only-list.json` 权威清单解析 site_id(34 条多城市拆分条目归属原 site_id,0 条未解析)→ 每站点聚合(confidence: high>medium>low;同 confidence: office>registered)→ 提取 city → 写回。写回后逐字段校验:311 文件仅 `location.address` / `site.city` 两字段变化,site id、顶层字段、lng/lat 零改动。

## 门禁结果

- npm test:`npm --prefix server test` → **1110 通过 / 3 失败 / 2 skip**(tests 1115)。3 个失败均为**断言回填前旧值的真实数据 canary 测试**(见「遇到的问题」),与本次任务规定动作直接冲突;基线侧:本树 HEAD(回填前)推导通过数 = 1113(1110+3),boss 记录的 1052 基线早于本树 HEAD(dev 后续合入新测试)。
- typecheck:`npm run typecheck` → **通过**
- `make docs-check` → **通过**(parallel-sessions untracked 产物未计入;本 WS 未产生其下文件)
- `git diff --check` → **通过**(曾修 data-quality.md EOF 多余空行)
- 一致性核对:results 中有 address 的 site_id 数 **342 == 回填成功数 342** ✓;全量 398 条结果 0 条未解析;311 文件抽查 diff 仅目标字段变化(抽查见「证据」)。

## 遇到的问题

1. **3 个真实数据 canary 测试断言回填前旧值,与本任务规定动作冲突**(文件边界内无法两全——`server/tests/` 不在本 WS 授权改动范围):
   - `tests/embodied-jobs-drops.test.mjs:80`「embodied-jobs 语料…结构逐项合法」:断言 47 个 embj-* drop 的 `site.location` 必须为 `{}`(待 geocode 快照契约)。本任务明确要求回填 embodied-jobs 47 站地址 → 冲突。
   - `tests/qqdoc-jobs.test.mjs:121`「qqdoc-jobs drops carry no city_pending-style placeholders」:断言 `qqj-新东方西安学校.json` city 必须仍为多城市文本 `'西安 咸阳'`、location 为 `{}`。本任务明确要求修正该脏值并回填地址 → 冲突。
   - `tests/split-city-sites.test.mjs:284`「真实数据: qqj-临界点…」:断言 `qqj-临界点-site` 的 location 必须精确等于 `{lng, lat}`(无 address)。本次回填已按任务规则写入 `address: 上海市徐汇区天平路185号11层1107室` → 冲突。
   - 原第 4 个失败(`tests/qqdoc-official.test.mjs:241` 城市全称闸门,`东京都`/`雄安新区`)已通过**提取结果对齐 CITY_TABLE 契约**解决(非表内城市 → city-unresolved 保留原值),该测试现通过。
   - → **需 boss 裁决**:授权一个 fix 工作流把这 3 个 canary 测试更新为回填后状态(推荐——测试编码的是已过时的旧数据),或接受 3 个失败。本 WS 未改任何测试文件。

2. **city 提取对齐仓库契约**:任务规则 4 允许「无法识别 → 保留原值」;仓库契约(`normalizeCityName` 闸门)要求 qqdoc-official 城市必须是 CITY_TABLE 已知全称。处理:`香港` → `香港`(表内全称,非「香港特别行政区」),`东京都`/`雄安新区` → 保留原值记 city-unresolved。其他目录(qqdoc-jobs/embodied-jobs)无此闸门,海外城市按「San Carlos, CA」式保留。

3. **EOF 换行**:188 个数据文件原无末尾换行,重写统一补了 `\n`;已按「与现文件格式一致」原则逐文件恢复原状(仅内容字段变更,不引入格式噪声)。

## 证据

- 测试输出摘要(boss 侧可复现):`npm --prefix server test` → `ℹ tests 1115 | ℹ pass 1110 | ℹ fail 3 | ℹ skipped 2`;3 个 ✖ 均指向上述 canary 断言(actual/expected 见输出,如 `actual: {lng,lat,address} expected: {lng,lat}`、`actual: '西安市' expected: '西安 咸阳'`、`site.location` 需为 `{}`)。
- 抽查 diff(3 文件,均为全 diff 代表):
  - `qqdoc-jobs/qqj-兆芯.json`:3 站 `location` 增 `address`,`lng/lat` 原样保留,`city` 不变(上海市/北京市/西安市)。
  - `qqdoc-jobs/qqj-施耐德电气.json`:11 站全部增 `address`(如 `北京市朝阳区望京东路6号A座施耐德电气大厦`),坐标保留,city 不变。
  - `qqdoc-official/qq-中国矿产资源集团.json`:`location: {}` → `{address: 河北省雄安新区启动区中谷甲1号}`,`city` 保留 `中国矿产资源集团总部`(city-unresolved),`city_pending: true` 未动。
- 统计:回填 342 / 剩余 null 31(清单已写入 data-quality.md 小节)/ city 修正 203 / city-unresolved 2 / 多城市拆分归属 34 条 / 结果条目 398 全解析 / 一致性 342==342。
- 提交:`e506c4d`(数据 311 文件)+ `6141a3b`(文档)。分支 `fix/address-backfill` 留在 worktree,未 merge、未 push。

## w2-fix 裁决更新(2026-08-22,boss 授权更新 3 个过时 canary 测试)

### 实际改动

- `server/tests/embodied-jobs-drops.test.mjs` → 断言口径从「location 必须为 `{}`(待 geocode)」改为**回填后新契约**:逐站断言 location 为空对象或仅含 address 字段(不得混入 lng/lat),address 非空;新增快照事实断言「38 站带 address / 9 站为空」;抽查 Tesla / 柏楚 / 浙江大学 地址非空。province 留空断言保留(去「待 geocode」注释)。
- `server/tests/qqdoc-jobs.test.mjs` → `qqj-新东方西安学校.json` canary:city 断言 `'西安 咸阳'` → `'西安市'`;location 由 `{}` 改为含回填 address(精确断言 `陕西省西安市碑林区南二环西段27号新东方大厦`)。
- `server/tests/split-city-sites.test.mjs` → `qqj-临界点-site` location 断言由 `cityCenter('上海')` 改为 `{ ...cityCenter('上海'), address: '上海市徐汇区天平路185号11层1107室' }`(坐标仍等于 cityCenter,address 为回填新值;拆分站与幂等断言不变)。

### 门禁结果

- npm test:`npm --prefix server test` → **tests 1115 / pass 1113 / fail 0 / skipped 2,exit 0**(修复前基线 1110/3 失败,3 个 ✖ 均为上述 canary)
- typecheck:`npm run typecheck` → **通过**
- `make docs-check` → **通过**
- `git diff --check` → **通过**

### 遇到的问题

- prompt 描述「embj 47 站中 31 站 null 仍为空」与实际数据不符:实际 **38 站带 address、9 站为空**(1X / AIM / Amazon Robotics / Apptronik / Boston Dynamics / Cruise / Grit Ventures / HTX / Tactus,全为海外公司无办公点可查;全目录无 lng/lat 混入)。按任务指示「以 worktree 内实际数据为准」,断言编码 38/9;「31」是 373 站点整体回填(342 回填 + 31 null)的空值数,非 embj 子集数。

### 证据

- 提交:`24348c3` `test(recruitment): update canary assertions for backfilled addresses`(3 文件 +31/−7)。
- 修复前失败复现(本次确认):`embj-Amazon.json location 留空 (待 geocode)`、`'西安市' !== '西安 咸阳'`、`actual {lng,lat,address} expected {lng,lat}`。
- 修复后:`npm --prefix server test` → `ℹ tests 1115 | ℹ pass 1113 | ℹ fail 0 | ℹ skipped 2`(duration 7.0s);typecheck / docs-check / diff --check 全绿。
- 分支 `fix/address-backfill` 留在 worktree,未 merge、未 push。

门禁: PASSED
结论: OK
