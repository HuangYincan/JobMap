# Workstream w7 — fix/geocode-province-infer(province 空时从 city 推断省)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。**
汇报写入批次目录 reports/w7.md(末两行 token,见文末)。

## 背景(实测,2026-08-22)

`geocode:sites:apply` 全量跑:**492 缺坐标站 0 resolved、492 unresolved**,其中 **332 个
`regeo-outside:outside-province:<真实省>`**(qqdoc-jobs/qqdoc-official/embodied 站)。根因:
这些站 `province` 字段为空,`siteCityTarget`(`server/src/lib/site-geocode.ts:79-85`)对空 province
默认「浙江省」,而站点真实城市在上海/北京/广东/湖北等 → 地址 geocode 命中后 regeo 校验
(落点省 vs target 省)必拒。radar/official-career 无此问题(province 有值)。

## 任务

1. 读 `server/src/lib/site-geocode.ts` 的 `siteCityTarget`(L79)与
   `server/src/lib/recruitment-adapters/official-site-parse.ts` 的 `CITY_TABLE`(L25,
   `{ 北京: { full: '北京市', province: '北京市' }, ... }`,含直辖市/各省主要城市)。
2. 修 `siteCityTarget`:**province 为空(或 trim 后空)时,从 site.city 推断省**——
   - city 已是全称(「上海市」「杭州市」「深圳市」):用 CITY_TABLE 反查(city 去「市/省」后缀作 key,
     或匹配 full 字段)取 province;查不到回退现行为('浙江省')。
   - city 为空/脏值:回退现行为。
   - 海外城市(「San Carlos, CA」「Singapore」):查不到 → 回退(不影响:海外站后续 geocode 会
     no-result,但不再误报 outside-province)。
   - 实现放 site-geocode.ts 内(可 import CITY_TABLE 或写最小本地映射——自裁,优先复用,最小改动)。
3. **测试**:`server/tests/` 新增/扩展 site-geocode 相关单测:province 空 + city=上海市 → target.province=上海市;
   city=杭州市 → 浙江省;city=深圳市 → 广东省;city 海外 → 回退;city 空 → 回退。
   对齐现有 vitest/node --test 风格。
4. 门禁:`cd /Users/acccan/dm-wt-prov/server && npm test`(基线 1267 pass/2 skip,全绿)、
   `npm run typecheck`;`cd /Users/acccan/dm-wt-prov && make docs-check`、`git diff --check`。

## 文件边界

- **只允许改**:`server/src/lib/site-geocode.ts`、`server/src/lib/recruitment-adapters/official-site-parse.ts`
  (仅当需要导出 CITY_TABLE 或加 helper,最小改动)、`server/tests/`(新增/扩展)。
- **不碰**:scripts、数据文件、其他模块、文档(若需记录变更在汇报说明,不要求改 tech/)。

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-address-first/reports/w7.md`。
**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

Conventional Commits(如 `fix(geocode): infer province from city when site.province empty`)。不 merge、不 push。
