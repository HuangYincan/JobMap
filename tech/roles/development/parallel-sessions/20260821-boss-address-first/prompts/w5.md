# Workstream w5 — fix/embodied-loc-contract(embodied 形态契约更新)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。**
汇报写入批次目录 reports/w5.md(末两行 token,见文末)。

## 背景

用户已真实执行 `geocode:sites:apply`(Env-only,5 源覆盖),把 `{address, lng, lat}` 写回部分
embodied-jobs drop(主树工作区 49 个未提交产物,含坐标)。但
`server/tests/embodied-jobs-drops.test.mjs:99-104` 仍断言 embodied 站点 location 形态
「空对象或仅含 address,**不得混入 lng/lat**」——该契约是「待 geocode 快照」时代的过时断言,
现在 embodied 数据可合法含坐标。主树 npm test 因此 1 红(用户产物存在期间持续)。
boss 已裁决:更新契约(同 w2-fix `24348c3` / w4-fix `cc0c484` 先例)。

## 任务

1. 读 `server/tests/embodied-jobs-drops.test.mjs`(重点 :99-104 与相关断言)。
2. 把 location 形态契约从「空对象或仅含 address,不得混入 lng/lat」改为
   **「空对象,或含 address,或含 {address, lng, lat}(geocode 后形态)」**——语义:禁止
   「仅坐标无地址」的畸形形态(防丢地址),允许 geocode 写回坐标。
   若测试里有「location 不得含 lng/lat」的精确断言,改为「有 lng/lat 时必须同时有 address」。
3. 保持其他断言(38/45 计数类已更新为 45/2;address 非空抽查等)不变。
4. 重跑门禁:`cd /Users/acccan/dm-wt-testfix/server && npm test`(全绿)、`npm run typecheck`;
   `cd /Users/acccan/dm-wt-testfix && make docs-check`、`git diff --check`。
5. 说明:此更新使主树在用户 geocode 产物存在时测试也能全绿(可在汇报中说明逻辑,
   不要求实际验证主树——主树产物是用户工作区,勿碰)。

## 文件边界

- **只允许改**:`server/tests/embodied-jobs-drops.test.mjs`。
- **不碰**:数据文件、其他测试、src/、文档、主树工作区任何文件。

## 门禁

1. `cd /Users/acccan/dm-wt-testfix/server && npm test` 全绿(基线 1254 pass/2 skip)
2. `cd /Users/acccan/dm-wt-testfix/server && npm run typecheck`
3. `cd /Users/acccan/dm-wt-testfix && make docs-check`、`git diff --check`

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-address-first/reports/w5.md`。
**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

Conventional Commits(如 `test(recruitment): allow geocoded lng/lat in embodied location contract`)。不 merge、不 push。
