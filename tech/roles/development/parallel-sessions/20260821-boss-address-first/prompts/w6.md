# Workstream w6 — fix/seed-import-env(plan-seed-import.mjs 缺 env 加载)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。**
汇报写入批次目录 reports/w6.md(末两行 token,见文末)。

## 背景

用户跑 `npm run import:seed:apply`(cwd=server)输出 `"apply": {"wrote": false, "reason": "no-database"}`
——`server/scripts/plan-seed-import.mjs` **没有任何 env 加载**,而 `db.ts` 只读 `process.env.DATABASE_URL`
(server/src/lib/db.ts:18)。对照:`geocode-sites-apply.mjs` 与 `import-hz-pois.mjs` 都有 loadEnv
(从 `server/.env.local` 注入 key)。这是脚本缺陷,修掉。

## 任务

1. 读 `server/scripts/geocode-sites-apply.mjs` 的 loadEnv 实现(约 L79-93)与 `server/scripts/plan-seed-import.mjs`。
2. 给 `plan-seed-import.mjs` 加同款 loadEnv(从 `server/.env.local` 读取并注入 `process.env`,
   **不打印任何 key**;文件缺失时静默跳过)。放在 import 之后、main 逻辑之前。
3. 注意保持脚本其余逻辑零改动;`--apply` 无 DATABASE_URL 时仍应输出 no-database(语义不变,
   只是现在有 .env.local 时会正确注入)。
4. 测试:该脚本无测试文件;检查 `server/tests/` 是否有引用 plan-seed-import 的测试需要适配
   (grep 确认;若无则跳过,说明)。
5. 门禁:`cd /Users/acccan/dm-wt-seed-env/server && npm test`(基线 1254 pass/2 skip,全绿)、
   `npm run typecheck`;`cd /Users/acccan/dm-wt-seed-env && make docs-check`、`git diff --check`。

## 文件边界

- **只允许改**:`server/scripts/plan-seed-import.mjs`。
- **不碰**:其他一切。

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-address-first/reports/w6.md`。
**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

Conventional Commits(如 `fix(import): load .env.local in plan-seed-import so apply reaches DB`)。不 merge、不 push。
