# Workstream w9 — fix/geocode-citycenter-rerun(城市中心假坐标站重新 geocode)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。**
汇报写入批次目录 reports/w9.md(末两行 token,见文末)。

## 背景(实测,2026-08-22,用户反馈「很多 POI 都堆在一个位置,比如上海」)

DB 中 **1583 站点(67%)坐标精确等于某城市中心**(city-centers 批次历史落点):
上海 121.47/31.23 堆 376 站、北京 327、深圳 248、成都 128、广州 121…。其中 **~1362 站现在已有
具体街道地址**(后续 w2/w4 回填),但 `siteNeedsGeocode` 判定「有坐标即跳过」→ apply 永不重跑 →
地图上几百个 POI 永久堆在同一中心点。仅 **221 站地址仍是城市名**(这些留在城市中心是合理的)。

## 任务

1. `server/src/lib/site-geocode.ts` 的 `siteNeedsGeocode`(L53)扩展为「需要 geocode」当:
   - 原判定(无 location / 无有限坐标 / 0,0)不变;或
   - **坐标与 `CITY_CENTERS`(`server/src/lib/city-centers.ts`)任一值匹配(±0.0005)
     且 `location.address` 非空且不是城市名**(判定:address 与 site.city 相等、或等于「city+市」、
     或等于去「市」的 city、或仅含城市名)→ 视为需要重新 geocode。
   - 实现:import `CITY_CENTERS`(或 `cityCenter`/`bareCityName` helper,自裁);83 个中心 × 全量
     站线性匹配即可(量级小)。注意导出名与现有调用兼容(plan-site-geocode、apply 预扫、
     listImportedSitesNeedingGeocode 都走它——扩展判定会自然让这些站进入 needs,这是预期)。
2. apply 主循环适配检查:`server/scripts/geocode-sites-apply.mjs` 对这些站应走**地址 geocode**
   分支(siteHasStreetAddress → geocodeAddressRest)。确认 `siteHasStreetAddress` 对「上海市黄浦区
   马当路388号」这类地址判定为街道地址(是);「三亚市」判定为否(否)——保持原逻辑。
3. **测试**(对齐现有 geocode 测试风格):`siteNeedsGeocode` 新判定——城市中心坐标+具体地址 → true;
   城市中心坐标+城市名地址 → false;真实坐标+具体地址 → false;无坐标 → true。若既有测试断言
   「城市中心坐标站不需要 geocode」之类,更新为新语义。
4. 门禁:`cd /Users/acccan/dm-wt-center/server && npm test`(基线 1299 pass/2 skip,全绿)、
   `npm run typecheck`;`cd /Users/acccan/dm-wt-center && make docs-check`、`git diff --check`。
5. 汇报给出:识别逻辑摘要 + 测试清单 + 预期重新 geocode 的站数估算(可跑
   `node scripts/plan-site-geocode.mjs` 或等价只读统计,不跑真实网络)。

## 文件边界

- **只允许改**:`server/src/lib/site-geocode.ts`、`server/src/lib/city-centers.ts`(仅当需要导出
  helper,最小改动)、`server/tests/`(新增/扩展)。
- **不碰**:scripts(除非主循环确需小适配,说明理由)、数据文件、其他模块。

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-address-first/reports/w9.md`。
**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

Conventional Commits(如 `fix(geocode): re-geocode sites pinned at city centers when they have a street address`)。不 merge、不 push。
