# Workstream w3 — fix/geocode-qqdoc-embodied(apply 覆盖 qqdoc/embodied 源)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。**
汇报写入批次目录 reports/w3.md(末两行 token,见文末)。

## 背景

2026-08-22 用户目标「为所有不带地址的点配好地址」已落地:w2 批次把 **342 个站点地址回填**进
`server/data/recruitment/{qqdoc-jobs,qqdoc-official,embodied-jobs}/*.json`(dev 790682e,已合入本树)。
但 `geocode-sites-apply.mjs` 的 `dropFiles()`(L152-153)只含 `RADAR_DIR + OFFICIAL_CAREER_DIR`——
**qqdoc/embodied 的 342 个有地址站点不会被 geocode 落坐标,永远上不了地图**。本 WS 补这个缺口。

## 任务

1. 读 `server/scripts/geocode-sites-apply.mjs` 的 `dropFiles()`(L152)与 import(L62-63)。
2. 扩展 `dropFiles()` 覆盖 `qqdoc-jobs` / `qqdoc-official` / `embodied-jobs`:
   - 优先复用 adapters 导出的目录常量(看 `server/src/lib/recruitment-adapters/` 下
     qqdoc-jobs / qqdoc-official / embodied-jobs 的 adapter 是否导出 `*_DIR`;没有则在本脚本内
     定义与 RADAR_DIR 同风格常量,或 import 它们的文件路径函数——自裁,选最贴合现有风格)。
   - 注意 qqdoc-official 有 `city_pending: true` 标记的站点(如 `qq-中国矿产资源集团-site-hq`,
     city 仍为「XX总部」脏值,地址已回填)——此类站点走地址 geocode 没问题(city 用于校验,
     regeo 校验用 address 落点 vs target.city;city 脏值会因 `city-not-in-list` / regeo mismatch
     被跳过是**可接受行为**,不强制处理,但要确认不会崩脚本)。
   - 保持:配额短路、memo、regeo 双闸门、override 优先级、confidence 门、写回逻辑不变。
3. **测试**(对齐 `server/tests/` 风格,node --test):新增/扩展一个测试断言 `dropFiles()` 现在
   覆盖 5 个目录(radar / official-career / qqdoc-jobs / qqdoc-official / embodied-jobs);
   dry-run 计划(planSiteGeocode)对 qqdoc/embodied 样例站点生成 needs(有地址→地址 geocode query)。
   如现有结构不便单测 dropFiles,可重构为可导出函数(说明理由)。
4. 文档:`tech/roles/data/data-quality.md` 补一句「geocode:sites:apply 现覆盖 5 源(2026-08-22,w3)」。
5. 不跑真实 geocode(Env-only,留给用户)。

## 文件边界

- **只允许改**:`server/scripts/geocode-sites-apply.mjs`、`server/src/lib/recruitment-adapters/`
  (仅当需要新增 DIR 常量时,最小改动)、`server/tests/`(新增/扩展)、`tech/roles/data/data-quality.md`。
- **不碰**:`server/data/recruitment/**`、crawler/、db/、前端、其他 src/lib 模块。

## 门禁

1. `cd /Users/acccan/dm-wt-geo-ext/server && npm test`(基线 1149 pass/2 skip;**必须全绿**)
2. `cd /Users/acccan/dm-wt-geo-ext/server && npm run typecheck`
3. `cd /Users/acccan/dm-wt-geo-ext && make docs-check`、`git diff --check`
4. 汇报:dropFiles 覆盖清单 + 新增测试用例 + qqdoc-official city_pending 站点处理说明

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-address-first/reports/w3.md`。
**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

Conventional Commits(如 `feat(geocode): include qqdoc/embodied drops in geocode apply`)。不 merge、不 push。
