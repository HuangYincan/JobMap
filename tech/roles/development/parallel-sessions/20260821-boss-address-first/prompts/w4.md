# Workstream w4 — fix/address-backfill-r2(第二轮再查 18 个新地址回填)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。**
汇报写入批次目录 reports/w4.md(末两行 token,见文末)。

## 背景

用户「为所有不带地址的点配好地址」二轮再查:上轮 31 个 null 站点中,4 个再查 subagent
又找到 **18 个地址**(结果在主仓库
`/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-address-first/results/null-01..04.json`,
--add-dir 已授权只读)。w2 批次已回填 342 站(dev 已含),本 WS 把这 18 个补进去。

## 任务(确定性规则,沿用 w2 已验证逻辑)

1. 读主仓库 `results/null-01..04.json`(JSON 数组,末行 NULL_RECHECK_DONE 排除),提取 address 非空的条目(共 18 条)。
2. **site_id 归属**:`city-only-list.json`(主仓库)是权威清单。多城市拆分条目(如 `qqj-中际旭创-site-上海`)
   归属原 site_id——用 w2 同款规则:优先精确匹配清单 site_id;不中则去掉最后一段 `-<城市>` 再匹配
   (注意 site_id 本身可含 `-`,如 `qq-中国远洋海运集团-site-hq`;仍不中记入报告)。
3. **回填**(写本 worktree 内 drop JSON):
   - `site.location = { ...(site.location ?? {}), address: <地址> }`(保留已有 lng/lat)。
   - `site.city` = 从地址提取城市(w2 规则:直辖市/省+市全称;海外取最后逗号段;空→保留原值记 city-unresolved)。
   - 特例标注:该条在 w2 已回填 address 的站点(如 `qqj-临界点-site` 主站已回填天平路185号,
     本批 18 条中不重复)——**已回填的站点不要覆盖/重复写**,只补 address 为空的站点。
   - **Tactus**(`embj-Tactus-site`):地址 `47509 Seabridge Dr, Fremont, CA 94538` 与数据行 city
     (Irvine)不符,疑数据错配——仍回填地址、city 按提取规则写 Fremont,在汇报与文档中标注「疑错配需人工复核」。
4. **文档**:`tech/roles/data/data-quality.md` 的「无地址站点网络检索补全」小节更新:
   31 null → 13 null(列出缩减后清单与新增 18 条来源说明);注明二轮再查渠道
   (360 搜索/搜狗微信/官网深层/Ashby ATS/OpenCorporates/SEC 等)。
5. 统计与一致性:回填 18 条全部归属成功;抽查 2 个文件 diff 仅目标字段变化。

## 文件边界

- **只允许改**:`server/data/recruitment/{qqdoc-jobs,qqdoc-official,embodied-jobs}/*.json`(仅 18 站所在文件)、
  `tech/roles/data/data-quality.md`。
- **不碰**:其他一切(server/src、scripts、tests、crawler、db、前端)。

## 门禁

1. `cd /Users/acccan/dm-wt-backfill-r2/server && npm test`(基线 1155 pass/2 skip;**必须全绿**)
2. `cd /Users/acccan/dm-wt-backfill-r2/server && npm run typecheck`
3. `cd /Users/acccan/dm-wt-backfill-r2 && make docs-check`、`git diff --check`
4. 汇报:18 条归属/回填/统计 + Tactus 标注 + 剩余 null 13 清单

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-address-first/reports/w4.md`。
**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

Conventional Commits(如 `data(recruitment): backfill 18 addresses from second-pass lookup (r2)`)。不 merge、不 push。
