# Session Prompt — w3:搜索占位文案(UI 3)

> 独立 headless 开发会话(极简)。先读 `CLAUDE.md`,再开工。
> **worktree 已由 boss 预建**(`/Users/acccan/dm-wt-w3`,分支 `chore/search-placeholder`)。
> 所有改动/提交都在 worktree 内;**不要 merge/push**;完成后写汇报。

## 背景(用户明确指定)

- 工作模式搜索框提示文本 → **「搜索公司或岗位」**。
- 地图(domain)模式搜索框提示文本 → **「搜索地点或地址」**。
- 默认处于工作模式。

## 现状事实(Explore 定位)

- 占位文案**已是 mode-aware**:全部输入框读 `modeConfig.searchPlaceholder` /
  `config.searchPlaceholder`(`map-shell.tsx:1878`、`2237`;`secondary-sidebar.tsx:268`)。
  字符串集中在 `server/src/lib/modes.ts` 的 `ModeConfig.searchPlaceholder`:
  - `domain` → `'搜索地点、美食、商场…'`(`modes.ts:150`)
  - `internship` → `'搜索公司、岗位…'`(`modes.ts:197`,canonical 后归 work,仅为自洽)
  - `work` → `'搜索公司、岗位…'`(`modes.ts:212`,实际渲染项)
- 占位文案**不做 i18n**(中文硬编码,zh/en 同显,维持现状,勿改 i18n.ts)。
- **默认模式已是 work**:`map-shell.tsx:190` `useState<MapMode>('work')`;登录用户偏好默认
  `account.ts:120` `DEFAULT_PREFERENCES.defaultMode:'work'`;登录用户若存过 domain 偏好会按偏好
  恢复(`map-shell.tsx:370-379`)——这是预期行为,不是 bug。

## 任务

1. `server/src/lib/modes.ts` 改 3 个 `searchPlaceholder`:
   - `domain`(150):`'搜索地点或地址'`
   - `internship`(197):`'搜索公司或岗位'`(与 work 一致,自洽)
   - `work`(212):`'搜索公司或岗位'`
2. **默认模式**:不改码——在汇报里确认 `map-shell.tsx:190` 已为 `'work'`(若实际读到别处非 work,
   报 BLOCKED 说明)。用户浏览器为桌面/移动一致。

## 文件边界

**拥有**:仅 `server/src/lib/modes.ts`。
**不碰**:`map-shell.tsx`、`secondary-sidebar.tsx`、`i18n.ts`、`poi-list.tsx`、`poi-card.tsx`、
`account-panel.*`、`db/`、`server/data/`、`tech/` 下非 modes 相关文档。

## 门禁(全部通过才算完成)

```bash
cd /Users/acccan/dm-wt-w3/server && npm test && npm run typecheck
cd /Users/acccan/dm-wt-w3 && make docs-check && git diff --check
```
- 若有测试断言 placeholder 文案(查 `server/tests/modes*.test.mjs` 等),同步更新。
- Conventional Commits:`chore(mobile): 更新搜索占位文案`。**不要 merge/push**。

## 回报

写汇报到 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260819-mobile-ux/reports/w3.md`。
内容:改动行、默认模式确认(给出 map-shell.tsx:190 证据)、测试结果、遇到的问题。不要倾倒文件。

门禁(末两行必须精确):
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```
