# w2 汇报(2026-08-19,boss 代写——worker 预算耗尽于收尾,实现与门禁已由 boss 复验)

> 本汇报由 boss 依据 worker 已提交 commit 的 diff + boss 亲跑门禁结果整理。
> worker 原始会话在写完 3 个 commit 后 `Exceeded USD budget (3)` 中断,未及写汇报。

## 实际改动(3 个 commit,均在工作树 `fix/mobile-card-interactions`,未 merge/push)

1. `7fb11df fix(mobile): restore drawer list scroll on detail back` — 交互 1:
   - `map-shell.tsx`:`drawerContentRef` + `drawerScrollRef`;移动端卡片 onClick 进详情前保存
     `.drawerContent.scrollTop`;`useLayoutEffect`(key=`detailPoi`)在 detailPoi→null 时恢复
     scrollTop(覆盖抽屉把手 2159 / onBack 2204 / 手势 1801 全部返回路径)。
   - 失效守卫:模式切换(1362)、刷新(1166)、新搜索(1714)、桌面 openDetail(2056)均清零
     drawerScrollRef,避免把旧上下文滚动带到新列表。
2. `b710120 test(mobile): cover scroll restore and edge-deselect contracts` —
   `server/tests/component-contracts.test.mjs` 新增 18 行断言(滚动恢复契约 + 边缘取消选中契约)。
3. `5540ea6 docs(mobile): record mobile card interaction fixes` — `CHANGELOG.md` + `tech/16-bug-fixes.md`。

交互 2(含在 7fb11df 内,poi-list/poi-card):
- `poi-list.tsx`:新增 `onDeselect?: () => void` prop;列表根 `.list` + `.cardSlot` 加 onClick
  (onDeselect 存在时)。
- `poi-card.tsx`:`<article>` onClick 加 `e.stopPropagation()`,点卡片自身不冒泡触发取消选中。
- `map-shell.tsx`:移动端 POIList(`2537` 附近)传 `onDeselect={() => { setSelectedId(null); setHighlightedId(null); }}`;
  桌面 secondary-sidebar 不传 → 桌面行为不变。

## 门禁结果(boss 亲跑,worktree 内)

- npm test: 299 通过 / 0 失败(2 skipped)
- typecheck: 通过(tsc --noEmit 无错误)
- docs-check: 通过
- git diff --check: 干净
- worktree `git status` 干净(全部已提交)

## 遇到的问题

- **worker 预算耗尽于收尾**:实现+测试+文档均已 commit 落盘、门禁绿,仅汇报未写;boss 复验后
  代写本汇报。
- 交互 2 仅移动端生效(onDeselect 仅移动端接线);桌面仍走「点地图取消选中」(map-shell.tsx:647-652)。

## 证据

- `git -C /Users/acccan/dm-wt-w2 log --oneline -3`:`7fb11df` / `b710120` / `5540ea6`(dev `a70f71e` 之上)
- `git diff dev --stat`(6 文件,+136/−3):map-shell.tsx +33、poi-list.tsx +12、poi-card.tsx +6/−1、
  测试 +18、tech/16 +63、CHANGELOG +7
- 复验命令:npm test / npm run typecheck / make docs-check / git diff --check

门禁: PASSED
结论: OK
