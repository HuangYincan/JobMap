# w1 汇报(2026-08-21)

worktree `/Users/acccan/dm-wt-csplit`,分支 `feat/city-split-sites`,3 个 commit,未 merge 未 push。

## 实际改动

- `server/scripts/split-city-sites.mjs`(新增)→ 拆分脚本,纯函数可单测:
  - `splitCityText` / `splittableCities`:多城市字符串按分隔符(、 , ， / 空白)解析,剔除「等/等地/等城市/全国其他/海外/线上/远程」等非城市 token,只保留 `CITY_CENTERS` 可归一城市(海外/未收录城市不处理),去重保序;
  - `planSiteSplit`:多城市 site(≥2 可归一城市)→ 原 site 保留为主站点(id 不变,city = 第一个**可归一**城市全称,补中心坐标;首个原始 token 不可归一时取第一个可归一城市,否则主站点仍无坐标、公司依旧不可见),其余城市各一个 site(id = `<site.id>-<裸城名>`,city 全称,坐标 = `cityCenter`);岗位按 `position.city/workCity/location.city` 匹配拆分城市 → `positions.siteId` 同步改挂,无法匹配留主站点;
  - `patchCityCenterCoords`:单城市无坐标 → 补 `cityCenter` 坐标,city 归一全称(「北京 哈尔滨 大连」这类只剩 1 个可归一城市的文本不留多城标签);
  - `processCompany`:只拆**有 positions** 的公司;已有坐标的 site 整体跳过(幂等闸门 + 街道级 geocode 优先);拆分城市 id 与公司现有 site id 撞车 → 跳过该城市;
  - 用法:`node scripts/split-city-sites.mjs`(dry-run)/ `--apply`(写回)。
- `server/data/recruitment/{radar,qqdoc-jobs,qqdoc-official,official-career}/**`(650 个文件)→ 数据拆分落地,仅动 `city` / `location`(lng/lat)/ 新增站点字段,`province`、`positions`、其他字段零改动(`git diff` 抽查确认)。
- `server/tests/split-city-sites.test.mjs`(新增)→ 18 个单测。
- `server/tests/drops-coordinate-consistency.test.mjs` + `server/scripts/fix-sweep-accident-coords.mjs` → 规则 1(非杭州站点坐标不得落杭州框)与清扫脚本加**精确等值豁免**:坐标 == `cityCenter(city)` 的静态行政中心不清扫、不判串味(金华中心 119.65/29.08 地理上在杭州框内,但它是 CITY_CENTERS 城市中心而非 fecef85 事故复制的杭州 office 坐标;事故坐标无法等值命中,守卫不削弱)。

## 拆分统计

- 扫描 1029 个文件(4 目录);有岗位公司 739 家;改动 650 文件。
- **多城市站点拆分:13 家**(全部 qqdoc-jobs,如临界点/兆芯/施耐德电气/圣邦微电子等),**拆出城市级站点 39 个**;主站点 13 个同时补中心坐标。
- **单城市无坐标补中心点:1485 个**站点。
- **岗位重挂:0**(数据中岗位无 `position.city` 字段,全部岗位按契约留主站点,`positions.siteId` 引用不变)。
- **跳过**:有坐标站点(街道级 geocode 不动)+ 无岗位公司(qqdoc-official 142 家全跳)+ 未收录城市站点 299 个(新加坡/中国香港/兰州/安庆/无锡/济南/洛杉矶/伦敦/吉隆坡/慕尼黑等,`CITY_CENTERS` 未收录,按「只处理可归一城市」口径不动)。
- **幂等**:已拆分数据重跑 → 0 改动(`改动文件: 0(dry-run)`)。
- **效果验证**:离线 catalog 可见 POI **106 → 885**(radar + official-career 读路径,`loadOfflineWorkCatalog` 实测),城市标签 9 → 30。

## 拆分前后样例(qqdoc-jobs/qqj-临界点.json,「上海 深圳 北京」,100 岗)

前:
```json
{ "id": "qqj-临界点-site", "city": "上海  深圳 北京", "location": {} }
```
后:
```json
{ "id": "qqj-临界点-site", "city": "上海市", "location": { "lng": 121.47, "lat": 31.23 } },
{ "id": "qqj-临界点-site-深圳", "city": "深圳市", "location": { "lng": 114.06, "lat": 22.55 } },
{ "id": "qqj-临界点-site-北京", "city": "北京市", "location": { "lng": 116.4, "lat": 39.9 } }
```
(坐标全部 = `cityCenter`,串味防御天然满足;岗位 100 条 siteId 仍指向主站点 `qqj-临界点-site`,引用一致。)

## 测试列表(split-city-sites.test.mjs,18 个)

- `splitCityText`:分隔符(、 , ， / 空白/双空格)、「等/等地/等城市」剔除、非城市词剔除、空值;
- `splittableCities`:只保留 CITY_CENTERS 城市、去重保序、全称归一;
- `fullCityName`:裸名 → 全称(北京→北京市,重庆→重庆市);
- `planSiteSplit`:主站点 id 不变 + 每城一站点坐标 = cityCenter + 串味判定通过;首 token 不可归一取第一个可归一城市;已有坐标 → null;单城市/无可归一/不足两个 → null;
- 岗位挂载:position.city / workCity 命中 → siteId 改挂,非拆分城市/无城市/他站岗位不动;
- `patchCityCenterCoords`:单城市补点 + 地址保留;多城文本剩 1 可归一 → 归一并补点;未收录/已有坐标原样返回;
- `processCompany`:无岗位不拆;混合场景(拆分+补点+已有坐标不动);幂等二次运行零改动;拆分 id 撞车跳过;
- 真实数据:四目录全量跑一遍 site id 唯一 + 岗位 siteId 全部可解析;临界点最终状态断言 + 二次运行幂等。

## 门禁结果

- npm test:**609 通过 / 0 失败 / 2 skip**(基线 591 通过/2 skip,+18 新测试)
- typecheck:通过
- docs-check:通过
- git diff --check:通过

## 遇到的问题

- **金华中心撞杭州参考框**:`drops-coordinate-consistency.test.mjs` 规则 1(非杭州站点坐标不得落杭州框)被 3 个金华补点站点触发 —— 金华中心 119.65/29.08 地理上落在杭州参考框(118.3–120.8 × 29.05–30.75)内。处理:测试与 `fix-sweep-accident-coords.mjs` 同加「坐标精确等于 `cityCenter(city)` 豁免」,事故坐标(杭州 office 对 120.221266/30.201767)无法等值命中,守卫不削弱;清扫脚本重跑验证幂等零改动。
- **岗位无城市字段**:4 目录 12410 条岗位扫描确认无 `position.city`,岗位挂载逻辑已实现并单测(合成数据),真实数据重挂数为 0 —— 多城市公司当前显示在主站点城市,徽章按城市级站点计数;后续若岗位带城市字段,重跑脚本即可改挂,无需改代码。

## 证据

- 门禁输出:`npm test` → `611 tests / 609 pass / 0 fail / 2 skipped`;`npm run typecheck`、`make docs-check`、`git diff --check` 全通过。
- 拆分统计与拆分明细:脚本 dry-run / --apply 输出(13 家 +39 站点 +1485 补点 +0 重挂,650 文件)。
- 效果:离线 catalog POI 106 → 885(HEAD 数据 vs worktree 数据,`loadOfflineWorkCatalog` 两次进程实测)。
- 幂等:已拆分数据重跑 `node scripts/split-city-sites.mjs` → 0 改动。
- commits: `26fbc21` fix(tests) 豁免、`c1f40d3` feat(data) 拆分落地、`ce78c14` feat(scripts) 脚本+单测。

门禁: PASSED
结论: OK
