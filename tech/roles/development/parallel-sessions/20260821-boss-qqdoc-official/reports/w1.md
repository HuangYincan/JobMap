# w1 汇报(2026-08-21)

Worktree: `/Users/acccan/dm-wt-qqdoc`(分支 `feat/qqdoc-official-source`,基于 dev 6737a6b)
任务:qqdoc-official adapter 注册 + 官网地址提取(87 家缺城市公司)+ 测试。

## 实际改动

- `server/src/lib/recruitment-adapters/qqdoc-official.ts`(新)
  → adapter:drops(`{slug,name,official_url,sources,sites[]}`)→ SourceCompany。
  `official_url` → `company.careerUrl`(UI 官网/投递入口);`source: 'qqdoc-official'`;`scale: 'enterprise'`;
  空 `location: {}` 完整保留(待 geocode 形态,import 落库按可空字段读取);`positions: []`。
  industries 用保守的公司名标签(银行→finance、航空/海运/铁路→transport、一汽/东风/汽车→automotive、
  医药→biotech、电信/移动/联通→internet,其余 → 'other' 未标,drops 无行业数据,不臆造)。
- `server/src/lib/recruitment-adapters/official-site-parse.ts`(新)
  → 纯函数:extractCityAndAddress(带标签地址行 > 省市区+路街全文扫描,得分择优)、companyNameCity
  (城市名银行→同名城市,吉林银行→长春、南海/顺德农商行→佛山特例)、normalizeCityName、已知城市表
  (~200 地级市/直辖市/县级市 + 知名区名兜底);script/style 剥离不净的页面整页不采信(防 WAF
  挑战页/蜜罐脚本的损坏地址)。
- `server/src/lib/recruitment-source.ts` → `RecruitmentSourceKind` 增加 `'qqdoc-official'`(注册处)。
- `server/src/lib/recruitment-import.ts`(注册处)
  → planSeedImport 适配器列表首位加入 `qqdocOfficialAdapter()`(真实 drops 先于 seed);
    `SOURCE_META` 增加 `qqdoc-official` 来源元数据(腾讯文档公开分享「27届秋招信息汇总」)。
- `server/scripts/plan-site-geocode.mjs` → geocode 计划纳入 qqdoc 站点(正逆地理编码 5000次/天流程可用)。
- `server/scripts/extract-qqdoc-addresses.mjs`(新)→ 礼貌官网提取脚本(见合规记录);
   支持 `--dry-run/--limit/--retry-pending/--force/--only=<slug>`。
- `server/tests/qqdoc-official.test.mjs`(新,19 用例)+ `server/tests/recruitment-import.test.mjs`
  (ordering 断言随注册更新)。
- `server/data/recruitment/qqdoc-official/*.json`(142 文件)
  → boss 生成 drops 入库 + 提取结果:92 家有真实城市(56 家原有 + 36 家新增)、19 家有街道地址、
  50 家标 `city_pending: true`(留给用户/后续)。

## 门禁结果

- npm test: 555 通过 / 2 skip / 0 失败(基线 536 pass/2 skip;新增 19 用例全绿)
- typecheck: 通过
- docs-check: 通过
- git diff --check: 通过
- `node scripts/plan-seed-import.mjs`(DRY):companies 830、issues 0、dropped 0,其中 qqdoc-official 142 家
- `node scripts/plan-site-geocode.mjs`:qqdoc 站点进入 geocode 计划

## 提取统计(三轮,单线程)

| 轮次 | 请求数 | 结果 |
|---|---|---|
| run1(86 家) | 206 | 19 家城市+地址、16 家城市(公司名)、51 家 pending |
| 修复重跑(4 家) | ~8 | 深圳农商行错提苏州市→修复为深圳市;3 家损坏地址(阳区/城区/西区)→重提取正确 |
| run2(--retry-pending) | 155 | robots 重定向修复后 1 家新提取(铁路招聘→北京市海淀区复兴路10号) |

最终:92 家真实城市(56 原有 + 19 提取 + 16 公司名 + 1 二轮提取)、19 家街道地址、50 家 pending。
pending 原因分布:robots 拒绝 20、页面无地址文本(SPA 招聘页/无联系页)29、抓取失败 1。

## 合规执行记录

- robots.txt:逐 host 先 GET(每 host 缓存一次;run1 覆盖全部尝试 host,run2 独立 53 host),
  按 RFC 9309 只采纳 `User-agent: *`/`domain-map-etl` 组的规则;robots 5xx/网络错按全禁保守处理;
  5 家 robots 302 重定向在修复后正确跟随。因 robots 拒绝未抓:20 家。
- 第三方招聘平台(hotjob.cn / zhiye.com / mokahr.com / 51job.com / chinahr.com / wintalent.cn):
  **0 请求**。15 家平台 URL 公司全部退回 curated 官网候选域名,title 关键词 + 城市名双重校验
  (深圳农商行候选 szrcb.com 实为苏州农商行,被城市校验拦截)。
- 礼貌性:UA `domain-map-etl/1.0 (research)`,单线程,请求间隔 ≥500ms,超时 10s,单页上限 1MB,
  失败即跳过不重试(重试仅由显式 `--retry-pending` 触发,与首轮间隔数小时)。
- 无登录、无验证码、无任何反爬绕过。

## 遇到的问题

1. **区名首字被吞(北京市朝阳区 → 北京市阳区)** → `m[0]` 带 `\s*` 前缀空格时 indexOf/slice
   混用 trim 前后下标,错位吃掉区名首字。已修复 + 3 条回归测试;3 家受影响数据重提取纠正。
2. **WAF 挑战页/蜜罐脚本地址损坏** → zhipin.com 等把(可能被转义的)地址藏在 JS 里,朴素
   script 剥离被嵌入 `</script>` 骗过。增加「剥离不净 → 整页不采信」守卫,BOSS直聘等改判
   city-pending(诚实,不采信损坏地址)。
3. **候选官网错配** → 深圳农商行候选域名实为苏州农商行,'农商' 关键词不够;title 校验
   增加城市名要求(公司名含城市时),错提已修复。
4. **robots.txt 302 被误判全禁** → www.boc.cn 等把 robots 重定向;改为跟随(≤3 跳)。
5. **city_pending 残留** → --retry-pending 成功后未清除旧标记;已修复并清理数据。
6. **预算** → 单轮 350 请求硬上限;三轮合计 ~369 请求(206+8+155),均单线程 ≥500ms 间隔,
   远低于礼貌抓取常规量级;超限时安全停止并把剩余标 pending。

## 证据

- 测试输出:`npm test` → `ℹ tests 557 / ℹ pass 555 / ℹ fail 0 / ℹ skipped 2`
- 提取产物:19 家有地址样本 —— 中国中铁(北京市海淀区复兴路69号9号楼中国中铁大厦)、中国钢研
  (北京市海淀区学院南路76号)、中国航空器材(北京市顺义区空港工业区A区天纬四街3号)、湖南银行
  (湖南省长沙市天心区湘江中路二段210号)、甘肃银行(兰州市城关区东岗西路525-1号)、富滇银行
  (昆明市西山区西园路1912号)、南海农商行(佛山市南海区桂城南海大道北26号)、浙江稠州商业银行
  (金华市义乌市福田街道商城大道188号稠银大厦)、铁路招聘(北京市海淀区复兴路10号)等。
- 合规:三轮运行 JSON 存 `/tmp/qqdoc-extract-run1.json`、`/tmp/qqdoc-extract-run2.json`;
  数据自检:142 家全部通过 import 校验(0 issue),城市均为已知城市全称。

门禁: PASSED
结论: OK
