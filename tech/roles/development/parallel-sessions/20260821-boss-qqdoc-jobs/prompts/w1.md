# Workstream w1 — feat/qqdoc-jobs-source(投递链接岗位提取)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。** 汇报写入批次目录 reports/w1.md(末两行 token,见文末)。

## 背景

用户提供的腾讯文档「27届公司汇总」(boss 已提取):**499 家公司全部带投递链接 + 城市 + 岗位方向 + 截止日**(`.playwright-mcp/qqdoc/tvVDZj-rows.json`)。用户明确指示:「他都有投递链接了,顺着投递链接来就行了」—— 投递链接是岗位提取入口。boss 已生成 **165 家新增公司 drops**(`server/data/recruitment/qqdoc-jobs/*.json`,含 apply_url),另有 334 家与现有 catalog 重复(它们的投递链接用于岗位数据补充)。

背景知识:工作模式 POI 需「岗位 + 坐标」才显示(server-catalog.ts:53);项目已有飞书 ATS 适配器(`crawler/app/domain_map_importer/ats_feishu.py`,公开 JSON API)与 zhiye 适配器(ats_zhiye.py,robots 阻断)。

## 任务

### 任务 1:注册 qqdoc-jobs adapter
- 读 `server/src/lib/recruitment-adapters/` 的 radar.ts 与索引注册处(qqdoc-official 同批次已加,参考其模式)。
- 新增 `qqdoc-jobs` adapter:读 `server/data/recruitment/qqdoc-jobs/*.json`,转换 SourceCompany(sites[].city 可能为多城市字符串「北京、杭州、上海」,与 radar 同处理;location 可能为空)。

### 任务 2:顺着投递链接抓岗位(499 家的 apply_url)
- 数据源:`/Users/acccan/dm-wt-qqj/.playwright-mcp/qqdoc/tvVDZj-rows.json` 不存在(worktree 无此文件)—— 用 boss 放的 `/Users/acccan/domain-map/.playwright-mcp/qqdoc/tvVDZj-rows.json`(主树,只读引用)或 `server/data/recruitment/qqdoc-jobs/`(165 家)+ 重复公司的链接映射(自裁:可从主树 tvVDZj-rows.json 读全部 499 家)。
- **按平台分类提取**(优先级从高到低):
  a. **飞书 jobs**(`*.jobs.feishu.cn` / `*.jobs.f.mioffice.cn` / 分享短链 `/s/<code>`):复用 `crawler/app/domain_map_importer/ats_feishu.py` 的 portal API 逻辑(公开 JSON API);分享短链先 resolve 到 portal 再调 `/api/v1/search/job/posts`。
  b. **北森 zhiye**(`*.zhiye.com`):用 ats_zhiye.py 的适配器逻辑;**先 robots.txt 检查**,拒绝则跳过(合规铁律,已验证 148 租户全拒;单公司链接可能不同,试一次)。
  c. **hotjob**(`wecruit.hotjob.cn`):试公开 API;失败跳过。
  d. **官网自研**(`join.chaitin.cn` 等非上述平台):礼貌 GET 招聘页,解析岗位列表(标题/城市/截止);JS 渲染解析不到 → 跳过。
  e. 其他(阿里 campus、ddhd.cn、微信文章):尝试;失败跳过,记 reason。
- **合规铁律**(违反即失败):礼貌 GET(单线程、≥500ms 间隔、UA `domain-map-etl/1.0 (research)`);先 robots.txt(Disallow 不抓);**不登录、不碰验证码、不绕过任何反爬**;第三方平台拒绝就跳过不轰炸。
- 岗位字段:title、city(岗位城市,解析不到用公司城市)、deadline(有则记)、url(投递链接)、source。
- 产出:**positions 写入 drops**—— 165 家新增公司:岗位挂到 `qqdoc-jobs/*.json` 的 positions 数组;334 家重复公司:岗位写入**对应现有公司 drops**(匹配:用主树 tvVDZj-rows.json 的 name 精确/包含匹配现有 drops 文件名——匹配不到的记 `unmatched` 不强行写入)。
- **规模控制**:≤499 家 × ≤2 页;每平台失败 3 家连续 → 该平台跳过;总请求预算 ≤1200。
- 幂等:可重复运行(已抓的 skip)。

### 任务 3:测试
- qqdoc-jobs adapter 单测;岗位解析纯函数单测(飞书 JSON → position、官网 HTML → position、robots 拒绝 → skip);风格对齐现有 vitest。

## 文件边界

- **只允许改**:`server/src/lib/recruitment-adapters/`(新增 qqdoc-jobs.ts + 注册)、`server/data/recruitment/qqdoc-jobs/**`(补 positions)、`server/data/recruitment/{radar,official-career,qqdoc-official}/**`(**仅给匹配到的现有公司追加 positions**,不删改其他字段)、`server/scripts/`(提取脚本)、`server/tests/`、`crawler/`(若需复用 ats 逻辑,自裁)。
- **不碰**:`db/`、`map-shell.tsx`、`tech/` 文档、`server/data/recruitment/{boss,nowcoder,shixiseng}/**`。

## 门禁

1. `cd /Users/acccan/dm-wt-qqj/server && npm test`(基线 566 pass/2 skip;**必须全绿**)
2. `cd /Users/acccan/dm-wt-qqj/server && npm run typecheck`
3. `cd /Users/acccan/dm-wt-qqj && make docs-check`、`git diff --check`
4. 汇报给出:adapter 注册位置 + 平台提取统计(每平台:尝试/成功/跳过+reason)+ 岗位总数 + 合规执行记录(robots 检查数/拒绝数)+ 测试列表

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260821-boss-qqdoc-jobs/reports/w1.md`。**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

不 push、不 merge、不切分支;Conventional Commits(如 `feat(recruitment): qqdoc-jobs adapter + apply-link job extraction`)。
