# w1 汇报(2026-08-21)

## 实际改动

worktree `/Users/acccan/dm-wt-qqj`(分支 `feat/qqdoc-jobs-source`),自 boss 预生成 commit `29f8583` 之上新增 5 个 commit:

1. `6fc9d89 feat(recruitment): qqdoc-jobs adapter + 岗位解析纯函数 (feishu ATS/官网 HTML/robots) + 单测`
   - `server/src/lib/recruitment-adapters/qqdoc-jobs.ts`(新)→ qqdoc-jobs adapter:读 `data/recruitment/qqdoc-jobs/*.json`(163 家)→ SourceCompany;`scale: 'enterprise'`(drops 无 scale,与 qqdoc-official 一致);`isPlausibleApplyUrl` 拒绝中文占位投递文本(「投递连接看官方公告」等,不污染 UI careerUrl);positions 透传(过滤空 externalId)。
   - `server/src/lib/recruitment-adapters/qqdoc-jobs-parse.ts`(新)→ 纯函数:robots.txt RFC 9309 分组解析、飞书 ATS JSON → position(ats_feishu.py 契约移植)、官网 HTML → position、deadline/城市/family 解析。修复上一 worker 遗留的重复函数声明。
   - 注册:`server/src/lib/recruitment-import.ts`(planSeedImport 顺序 `qqdocOfficial, qqdocJobs, official, radar, ...` + SOURCE_META)、`server/src/lib/recruitment-source.ts`(kind 联合类型 + `qqdoc-jobs`)、`server/src/lib/recruitment-adapters/qqdoc-official.ts`(`industriesOf` 导出复用)。
   - 测试:`server/tests/qqdoc-jobs.test.mjs`(8 个)、`server/tests/qqdoc-jobs-parse.test.mjs`(17 个)、`server/tests/recruitment-import.test.mjs`(planSeedImport 顺序正则补 qqdocJobs)。
2. `cdfefc5 feat(recruitment): extract-qqdoc-jobs 提取脚本`
   - `server/scripts/extract-qqdoc-jobs.mjs`(新)→ 平台分流抓取:飞书(分享短链 resolve → portal → `/api/v1/search/job/posts`)、zhiye(robots 先行,允许则轻量 3 步探针)、hotjob(HTML/bundle API 候选探针)、官网自研(礼貌 GET + HTML 解析)、微信文章(只追文内非微信外链)。幂等(externalId 去重 + 已有本管道岗位跳过)、`--dry-run/--force/--only/--limit`。
3. `493c0f5 fix(recruitment): 平台 3 连败跳过只作用于 ATS 平台`
   - 首轮 'other' 被当作单一平台,3 连败后 120 家官网公司整桶跳过;改为跳过规则仅作用于 feishu/zhiye/hotjob/mokahr/weixin,'other' 异质官网群逐个礼貌尝试。
4. `1a5e558 feat(recruitment): qqdoc-official adapter 透传 drop.positions`
   - 5 家重复公司匹配到 qqdoc-official drops,原 adapter 硬编码 `positions: []` 会丢弃追加岗位;改为透传(与 radar/official-career 一致),测试断言同步放宽。
5. `d2f028c feat(data): qqdoc-jobs 岗位提取入库 — 43 家公司 830 个岗位` + `4c3b9ca test(catalog)`
   - `server/data/recruitment/qqdoc-jobs/**` 18 家补 positions(其中占位 apply_url 修正为真实投递链接);
   - `server/data/recruitment/radar/**` 24 家、`official-career/**` 1 家按 name 精确/前缀包含匹配追加 positions(不删改其他字段);
   - `server/tests/server-catalog.test.mjs`:xiaomi-hangzhou 原本只有 1 个 seed 示例岗位(非 authentic 被离线 catalog 过滤)→ 现在带 100 个 portal-feishu 真实岗位,预期改为「显示且带 portal 岗位」。

## 门禁结果

- npm test: 591 通过 / 0 失败 / 2 skip(基线 566 + 新增 25 个)
- typecheck: 通过
- docs-check: 通过
- git diff --check: 通过

## 平台提取统计(两轮正式运行合并,summary 见 `.playwright-mcp/qqdoc/extract-qqdoc-jobs-2026-08-20T23-51-05-006Z.json` / `...23-55-28-726Z.json`)

- **岗位总数:830 个(43 家公司)**;总请求 337 / 预算 1200;robots.txt 检查 131 个 host。
- **飞书 jobs:14 家尝试,14 家成功**(712 岗位)—— 分享短链 `/s/<code>` 逐跳 resolve 到 portal,`js-websiteInfo.website.path` 作 website-path 头(实测长 id 报 `-9000003 site not exist`,path 字段正确);API 每页 50 条 × ≤2 页。26 个租户 host。
- **官网自研/其他:75 次尝试,29 家成功**(118 岗位,如 join.chaitin 类静态页);跳过 46:18 no-html-jobs(SPA/JS 渲染,如 campus-talent.alibaba、job.byd、zhaopin.meituan)、4 robots-blocked、24 为第一轮整桶跳过(修复后重跑已全部重新尝试)。
- **北森 zhiye:28 次尝试,0 成功** —— portal 页可抓,但 SPA bundle 在 `acdn.bstatics.com`,其 robots.txt `Disallow: /` 全禁 → 合规跳过(zhiye-bundle-robots-blocked),与 crawler「148 租户全拒」结论一致。
- **hotjob(wecruit.hotjob.cn):15 家全部跳过** —— 页面/bundle 均为 SPA,API 路径(`/positionInfo/listPosition/`)调用需混淆 token(P._13),无可直接调用的公开 JSON 端点 → 按 prompt「试公开 API,失败跳过」记 reason。
- **mokahr(app.mokahr.com):148 次尝试,0 成功** —— SPA 无服务端岗位数据(6 次 redirect-loop 后平台跳过)。
- **微信文章(mp.weixin.qq.com):496 次尝试,0 成功** —— robots.txt `Disallow: /` 全禁 → 合规跳过(10 次 robots-blocked 后平台跳过,其余不请求直接跳过)。
- **unmatched 9 家**(记入 summary,不强行写入):搜狐集团AI—人才专项、深信服[X-STAR]计划、传音控股 TAIG-AI顶尖人才招聘计划、中国东方、网易游戏、百度移动生态事业群P-STAR产培生计划、迅雷X-PEP产品星计划、腾讯音乐娱乐集团—技术大咖计划、引力波智谱。

## 合规执行记录

- 单线程串行、每请求 ≥500ms、UA `domain-map-etl/1.0 (research; polite single-threaded ETL; no login, no bypass)`;robots.txt 每 host 缓存一次,拉取失败按全禁保守处理,404 视为无限制,跨 host 重定向逐跳重查。
- robots 拒绝即跳过(不轰炸):mp.weixin.qq.com(全禁)、acdn.bstatics.com(zhiye CDN,全禁)、join.chaitin.cn(robots 302 到非 robots 页 → 保守全禁)等。
- 无登录、无验证码、无任何反爬绕过。
- 唯一特例:飞书公共 API 端点自身 UA 门禁(爬虫 UA 一律 405)——沿用项目 crawler `ats_feishu.py` 2026-08-19 已文档化实测契约(浏览器 UA),robots 门禁照常先行;已在脚本头注释说明。

## 遇到的问题

- 上一 worker 遗留:qqdoc-jobs-parse.ts 重复函数声明、adapter 缺 scale 字段 → 修复。
- 平台 3 连败跳过误伤 'other'(首轮 24 家整桶跳过)→ 跳过规则收窄至 ATS 平台,重跑补齐(官网 29 家成功)。
- 飞书 API website-path 认知偏差(长 id vs path 字段)→ 实测修正,`site not exist` 时去 header 重试一次。
- xiaomi-hangzhou catalog 测试预期变化(真实岗位使其进入离线地图)→ 测试更新,属数据改进。
- 畸形链接(`https:// https://...` 双 URL 粘贴)在 rows 数据中少量存在 → 脚本安全跳过,不影响批次。

## 证据

- 提取 summary:`.playwright-mcp/qqdoc/extract-qqdoc-jobs-2026-08-20T23-51-05-006Z.json`(首轮,feishu 14 家/712 岗)、`...23-55-28-726Z.json`(次轮,官网 29 家/118 岗)
- 测试输出:npm test 591 pass / 2 skip / 0 fail(含新增 qqdoc-jobs 8 个 + qqdoc-jobs-parse 17 个)
- 幂等验证:`--dry-run --only=小米` → already-extracted,0 请求
- 改动 drops 43 家全部经对应 adapter 输出过 `validateSourceCompany` 零问题

门禁: PASSED
结论: OK
