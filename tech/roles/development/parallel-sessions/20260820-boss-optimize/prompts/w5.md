# Workstream w5 — 数据代码侧优化(provenance + 城市覆盖 + 站点名归一)

## 背景(boss 已查明的现状,2026-08-20)

- **串味根因已于 2026-08-19 修复**(`server/scripts/geocode-sites-apply.mjs:186-196` 已加 override.city 门控 + regeo 城市校验 :241-254 + city-scoped 检索 site-geocode.ts:52-66)。DB 147 条串味行的**数据修正**(重跑 geocode)是 Env-only,boss 合并后统一跑,**你只管代码侧**。
- **provenance 缺口**:部分 drops(得物.json、腾讯.json)缺 `source` 字段,crawler `crawler/app/domain_map_importer/imports.py:10-12` 契约要求 7 键;server 落库时 `server/src/lib/recruitment-import.ts:281-292` 把 source 硬编码成 'Domain Map curated seed' → 应尊重 drop 自带 source。
- **CITY_CENTERS 只 16 城**(`server/src/lib/city-centers.ts:18-35`),苏州/重庆/珠海/佛山/宁波等站点城市未收录 → 徽章锚点回退 pin 均值。
- **radar drops 占位站点名**:`server/data/recruitment/radar/腾讯.json:13-40` 三条「腾讯—剩余岗位」,address=「北京/上海/广州/深圳」文本(scan #15);「按城市派生站点名」在 geocode 落地前可先做**展示层/数据文件归一**。
- **radar 抓取目标只 7 城**(`crawler/app/domain_map_importer/radar_jobs.py:26` CITY_TARGETS=7),可扩南京/苏州/西安(扩的是抓取配置,跑抓取是 Env)。
- **city_site_id 兜底**(`crawler/app/domain_map_importer/ats_feishu.py:228-232`):非 CITY_PINYIN 城市走 regex/'unknown',site id 一致性可修。
- 采集合规:CLAUDE.md 禁抓 BOSS直聘/牛客/小红书/实习僧(BLOCKED_HOSTS 已落实);radar 源 = GitHub 快照(xiaozhao-radar.md 允许)。**本 WS 不发起任何网络抓取/下载**。

## 任务(worktree:`/Users/acccan/dm-wt-w5`,分支 `feat/data-code-coverage`)

1. **provenance 修复**:
   a. grep `server/data/recruitment/**/*.json` 找缺 `source` 键的 drops,补上正确值(radar 系列 = `"source": "xiaozhao-radar"`,official-career 系列 = 对应 ETL 文档源名;以 `tech/roles/data/etl/` 各文档与 imports.py 契约为准)。
   b. `recruitment-import.ts:281-292` 落库 source 改为读 drop 自带 source(缺失才回退默认),保持现有 provenance 校验语义不变。
2. **CITY_CENTERS 扩展**:grep `server/data/recruitment/**` 实际出现的 site.city 值,把缺的站点城市补进 `city-centers.ts`(格式对齐现有条目;不删不改现有条目,只增)。补完跑 server 相关测试。
3. **站点名归一**:radar drops 中「{公司}—剩余岗位」类占位 name,按 site.city 派生为「{公司}·{城市}」(如「腾讯·上海」)直接改数据文件(server/data/recruitment/radar/*.json 的 site.name);address 保留原样(是 geocode 输入,勿动)。只动 name,不引入新结构。改完在汇报列出改动文件与条数。
4. **radar CITY_TARGETS 扩展**(radar_jobs.py:26):加南京/苏州/西安(与任务 2 的城市一致),跑 crawler 单测确认不破契约。
5. **city_site_id 兜底**(ats_feishu.py:228-232):对非 CITY_PINYIN 城市给确定性 id 派生(或注明保持 unknown 兜底的理由),跑 crawler 单测。
6. 全量门禁:server npm test + typecheck + crawler 单测 + docs-check。

## 文件边界(拥有)

- `crawler/app/domain_map_importer/`(imports.py / radar_jobs.py / ats_feishu.py 等,仅任务 1/4/5 涉及处)
- `crawler/tests/`(仅新增/更新对应契约)
- `server/src/lib/recruitment-import.ts`(仅 source 落库段)、`server/src/lib/city-centers.ts`(只增)
- `server/data/recruitment/**/*.json`(仅 source 补齐与 site.name 归一,坐标/address/tier 均不动)

**不碰**:city-cluster.ts / spatial-query.ts / map-shell.tsx(w1)、POI 定位链(w2)、company-logo.ts / seed-data.ts(w3)、docs(w4)、server/scripts/geocode-*.mjs(Env 归 boss)。**不要**运行 `make refresh-radar` / `crawl-official` / 任何网络抓取(工具已禁)。

## 门禁(全部须绿)

- `cd /Users/acccan/dm-wt-w5/server && npm test`(基线 455/453/2)与 `npm run typecheck`(0 错误)
- `cd /Users/acccan/dm-wt-w5/crawler && PYTHONPATH=app python3 -m unittest discover -s tests -v`(全绿;这是 `make test-unit`,本 WS 被授权跑)
- `make docs-check`(worktree 根)、`git diff --check` 无输出

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-optimize/reports/w5.md`:各任务改动清单(file:line)、补齐 source 的 drops 清单、站点名归一条数、遇到的问题。小步 commit(Conventional Commits)。**不要 merge/push**。
末两行必须精确:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```
