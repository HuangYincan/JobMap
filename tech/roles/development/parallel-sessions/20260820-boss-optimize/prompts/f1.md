# Workstream f1 — 修 import upsert EXCLUDED 歧义 + 收尾同步

## 背景(boss 2026-08-20 实测发现,已定位)

- `npm run import:seed:apply` 当前必败:`column reference "logo_url" is ambiguous`(PG 42702)。
- 根因:`server/src/lib/recruitment-import.ts:354-355` 的 upsert SET 里
  `logo_url = COALESCE(EXCLUDED.logo_url, logo_url)` / `logo_emoji = COALESCE(EXCLUDED.logo_emoji, logo_emoji)`
  —— DO UPDATE SET 的 **RHS 表达式**对「目标表 + EXCLUDED」通用解析,未限定的 `logo_url`/`logo_emoji`
  两边都有 → 歧义。LHS(赋值目标)与 `EXCLUDED.xxx` 限定引用均无问题。
- 引入点:commit `d78e6f3`(2026-08-19 ws3「import 合并 logoUrl/logoEmoji + upsert COALESCE 保既有 logo」);
  此后 import:seed:apply 一直 deferred 未跑,故未暴露。
- 已实证(boss 在本地 PostGIS 16.3 上):未限定写法报错;`COALESCE(EXCLUDED.logo_url, companies.logo_url)`
  写法成功(INSERT 0 1)。
- 全库 grep 复核:其余 EXCLUDED 引用(recruitment-import.ts:333/348-353/472-486、account-store.ts:735/811/896)
  均为 `SET col = EXCLUDED.col`(RHS 已限定),无同类问题。
- 同批次遗留(worktree `/Users/acccan/dm-wt-f1` 一次性处理):
  - `server/src/lib/freshness.ts:7` 注释仍写「Frontend rendering awaits approval (tech/17)」——tech/17 提案
    已被 tech/18 §A1(2026-08-17 用户决策:不做新鲜度徽标,只突出在招)取代,注释应同步。
  - `server/src/lib/mode-cache.ts:43` `MODE_CACHE_VERSION = 13` → bump 到 14(本批数据语义变更,
    boss 合并后跑 import/audit,客户端旧缓存需失效)。
  - `tech/16-bug-fixes.md` 追加 2026-08-20 修复日志(append-only 惯例):①B3 聚合串味剔除+LOD 计数口径
    (w1,详 reports/w1.md)②首点 POI 视角 geoSettled 门控补放(w2,reports/w2.md)③favicon IP 覆盖
    (w3,reports/w3.md)④import EXCLUDED 歧义(本 ws)。

## 任务(worktree:`/Users/acccan/dm-wt-f1`,分支 `fix/import-upsert-ambiguity`)

1. `recruitment-import.ts:354-355` 两处 COALESCE 回退参数改为表限定(`companies.logo_url` / `companies.logo_emoji`),
   保持逻辑不变(保既有 logo 语义)。
2. 找 server/tests/ 下 recruitment-import 的既有契约测试(若存在),补一条回归断言:
   upsert SET 的 RHS 不得含未限定列引用(如断言模板含 `companies.logo_url` 且不含 `EXCLUDED.logo_url, logo_url` 未限定形)。
   若该文件无 import SQL 契约测试,可新建小契约文件(参照仓库测试风格)。
3. `freshness.ts:7` 注释更新为反映 tech/18 §A1 决策(提案已存档,判断函数仍用于真实岗位过滤)。
4. `mode-cache.ts:43` 13 → 14。
5. `tech/16-bug-fixes.md` 追加 4 条 2026-08-20 日志(根因+修复+验证三要素;事实以本批次
   reports/w1.md、w2.md、w3.md 与本次 fix 为准,报告在批次目录 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-optimize/reports/`)。
6. 门禁跑全:server npm test / typecheck / make docs-check / git diff --check。

## 文件边界(拥有)

- `server/src/lib/recruitment-import.ts`(仅 354-355)、`server/tests/`(仅 import 契约)、
  `server/src/lib/freshness.ts`(仅 :7 注释)、`server/src/lib/mode-cache.ts`(仅 :43)、
  `tech/16-bug-fixes.md`(仅追加)

**不碰**:其余 server/src、crawler/、server/data/、tech/ 其他文件。**不要**跑 import/geocode/audit
(工具已禁;boss 合并后统一跑并验证)。不需要 DB 即可完成本 WS(回归用 SQL 模板断言)。

## 门禁(全部须绿)

- `cd /Users/acccan/dm-wt-f1/server && npm test`(基线 477 tests / 475 pass / 2 skip,不得减少)
- `cd /Users/acccan/dm-wt-f1/server && npm run typecheck`(0 错误)
- `make docs-check`(worktree 根)、`git diff --check` 无输出

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-optimize/reports/f1.md`:
改动清单(file:line)、回归测试方式、16-bug-fixes 条目摘要、遇到的问题。小步 commit(Conventional Commits)。
**不要 merge/push**。
末两行必须精确:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```
