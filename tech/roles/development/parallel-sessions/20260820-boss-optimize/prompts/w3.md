# Workstream w3 — 扩展公司 logo 覆盖(IP 域名识别 + 域名映射 + 兜底链 + 清脏数据)

## 背景(已查明的现状,2026-08-20 boss 派发)

- `server/src/lib/company-logo.ts`(73 行):`hostFromUrl`(23-32)对裸 IP hostname 原样放行;`faviconFromUrl`(44-48)生成 `https://favicon.im/{host}?size={size}`,**无白名单/映射表/无 IP 识别**;`resolveCompanyLogo`(55-72)解析链 = 站点 logo → 站点 favicon → 公司 logo → 公司 favicon → 🏢。favicon.im 对裸 IP host 实测返回 404 → 全线落到 emoji(tech/16-bug-fixes.md:914 已记录)。
- 消费点(均有 onerror→emoji fallback):`server/src/components/poi-card.tsx:17-36`(用点 :269)、`server/src/components/poi-detail.tsx:170,182-192`、`server/src/lib/map-markers.ts:150-168`(recruitmentBadgeHTML,:159-163,用点 416-417/472)、SVG 徽章 :172-180(用点 506)。聚合徽章不含 logo,不动。
- 脏数据:`server/src/lib/seed-data.ts` 49 处 `logoUrl` 是已弃用的 Google s2 死链(`https://www.google.com/s2/favicons?...`,如 :155/:225/:267/:310/:361/:404/:446/:478/:542);DB 侧 672 家 logo_url 100% 空(ADR-007,tech/06-decisions.md:160-198)。ADR-007 备选兜底服务 = **icon.horse**(:185,实测 200)。
- 单测:`server/tests/company-logo.test.mjs`(faviconFromUrl + 5 层链 + DB 路径回归),改动须同步。

## 任务(worktree:`/Users/acccan/dm-wt-w3`,分支 `feat/logo-coverage`)

按优先级实现(全部纯代码,无密钥):

1. **IP 识别**(company-logo.ts `faviconFromUrl`):IPv4 host(正则 `\d+\.\d+\.\d+\.\d+`)→ 不请求 favicon.im(直接 undefined/跳过),配合 2 的映射表兜底;无映射则降级到现有 emoji/SVG 占位。
2. **`DOMAIN_LOGO_MAP` 映射表**(company-logo.ts 顶部):裸 IP host → 真实公司域名(查 `server/data/recruitment/` 各 drops 的 careerUrl/域名与 seed 数据,把「有真实域名但被写成 IP 的站点」与「favicon.im 拿不到的大厂」列进去,如 IP → 对应官方域名),在链的 favicon 层之前查表(resolveCompanyLogo 55-72 插入一层,source 记 'company')。**先 grep 全库实际出现哪些裸 IP host**(crawler drops / server/data / seed),表只覆盖真实存在的,避免空表。
3. **兜底链**(韧性,ADR-007 备选):`faviconFromUrl` 支持候选数组 `[favicon.im, icon.horse]`,三个消费组件(`poi-card.tsx` / `poi-detail.tsx` / `map-markers.ts` badge)的 onerror 先切下一候选再降 emoji。若改动面大,至少实现 company-logo.ts 侧导出候选列表 + poi-card/poi-detail 接线(map-markers 徽章可注明留待验证,但尽量三处一致)。
4. **清脏数据**:seed-data.ts 49 处 Google s2 `logoUrl` → 替换为 favicon.im URL(与 faviconFromUrl 同构:`https://favicon.im/{host}?size=128`)或删除(优先替换,保留数据语义);改前确认无 careerUrl 的公司仅「曦曦AI」。
5. 同步/新增单测 `company-logo.test.mjs`(IP 识别、映射表命中、候选 fallback 链、无 careerUrl 公司)。

## 文件边界(拥有)

- `server/src/lib/company-logo.ts`、`server/tests/company-logo.test.mjs`
- `server/src/components/poi-card.tsx`(仅 logo 渲染 onerror 部分)、`server/src/components/poi-detail.tsx`(仅 :170-192 logo 区)、`server/src/lib/map-markers.ts`(仅 badge logo 区 :150-168)
- `server/src/lib/seed-data.ts`(仅 logoUrl 清理)

**不碰**:city-cluster.ts / spatial-query.ts / map-shell.tsx(w1)、POI 点击定位链(w2)、crawler/ 与 server/data/ 数据文件(w5)、docs(w4)。DB 存量导入(`import:seed:apply`)是 Env-only,boss 合并后统一跑,**不要运行**(工具已禁)。

## 门禁(全部须绿)

- `cd /Users/acccan/dm-wt-w3/server && npm test`(基线 455 tests / 453 pass / 2 skip,不得减少 pass)
- `cd /Users/acccan/dm-wt-w3/server && npm run typecheck`(0 错误)
- `make docs-check`(worktree 根)、`git diff --check` 无输出

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-optimize/reports/w3.md`:改动清单(file:line)、映射表收录的 IP→域名对清单(实际 grep 到的)、fallback 接线验证方式、遇到的问题。小步 commit(Conventional Commits)。**不要 merge/push**。
末两行必须精确:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```
