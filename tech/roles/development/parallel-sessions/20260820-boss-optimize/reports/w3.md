# w3 汇报(2026-08-20)

WS: 扩展公司 logo 覆盖(IP 域名识别 + 域名映射 + 兜底链 + 清脏数据)
Worktree: `/Users/acccan/dm-wt-w3`,分支 `feat/logo-coverage`,3 commits,未 merge/push。

## 实际改动

### 1. `server/src/lib/company-logo.ts`(核心,commit 65ad2a6)
- `IPV4_RE`(:41):裸 IPv4 host 识别。
- `DOMAIN_LOGO_MAP`(:52-56):裸 IP host → 官方域名映射表。**先 grep 全库**
  (crawler/ + server/data + seed-data),实际出现的裸 IP host 仅 1 个:
  `47.96.146.209`(浙江省发展规划研究院 radar drop 的 careerUrl)。表只收录该条。
- `FAVICON_SERVICES`(:58-61):候选服务数组 `[favicon.im, icon.horse]`(ADR-007 备选,tech/06-decisions.md:185)。
- `faviconCandidatesFromUrl`(:75-79,新导出):候选列表;裸 IP 无映射 → 空列表(不请求服务)。
- `faviconFromUrl`(:86-88):改返回首选候选,旧签名/行为不变(兼容)。
- `mappedCompanyFavicon`(:95-102):映射层,IP 命中表 → 官方域名 favicon。
- `resolveCompanyLogo`(:109-135):链插入两层映射(site 层 :117-120、company 层 :127-130),
  在 favicon 层之前,source 记 `'company'`。链 = 站点 logo → 站点映射 → 站点 favicon →
  公司 logo → 公司映射 → 公司 favicon → emoji。

### 2. `server/src/lib/seed-data.ts`(commit 4c17000)
- 49 处 google s2 死链 `logoUrl`(grep 全库确认恰好 49 处,`server/src/lib/seed-data.ts` :155-1792)
  → 全部替换为 `https://favicon.im/{host}?size=128`(与 faviconFromUrl 同构,保留数据语义)。
- 改前核对:seed 公司中无 careerUrl 的仅「曦曦AI」(:496-528,无 careerUrl/logoUrl,emoji ✨ 兜底)。

### 3. 三个消费组件 onerror 候选链接线(commit 28c688d)
- `poi-card.tsx` `CompanyLogo`(:22-53,用点 :287-291):`logoUrl` 失败 → careerUrl 派生的
  favicon 候选(favicon.im → icon.horse,含 IP 映射)→ emoji。
- `poi-detail.tsx` `RecruitmentDetail`(:171-199):同链,替换原 `logoFailed` 布尔。
- `map-markers.ts` `recruitmentBadgeHTML`(:149-182):新增 `fallbackUrls` 参数;
  内联 onerror 用 `data-fb`(JSON 候选数组)依次切下一候选,全失败才隐藏 img 显示 emoji。
  两处调用点(:429-434、:483-493)经 `logoFallbackUrls`(:177-182)从 `company.careerUrl` 派生候选。

### 4. `server/tests/company-logo.test.mjs`(commit 65ad2a6,新增 7 个测试)
- IP 识别(裸 IP 不直连 favicon 服务)、`faviconCandidatesFromUrl` 候选链
  (favicon.im → icon.horse)、映射表命中(链层 source='company' + 候选派生)、
  未映射 IP → emoji、DOMAIN_LOGO_MAP 键集合(防空表/防漂移)、
  无 careerUrl 公司(曦曦AI 模式,emoji 直出)、DB 读路径 IP 映射回归。

## 映射表收录清单(实际 grep 到的裸 IP → 域名)

| 裸 IP host | 出现位置 | 映射域名 | 备注 |
|---|---|---|---|
| `47.96.146.209` | `server/data/recruitment/radar/浙江省发展规划研究院.json` :9/:35(careerUrl,共 2 处) | `zdpi.org.cn` | 官方域名**未联网复核**(沙箱无 egress),见「遇到的问题」 |

favicon.im 拿不到的大厂:经核查无——ADR-007 已实测 favicon.im 对 alibaba/tencent 等大厂子域名
均 200,不占表。

## 门禁结果

- npm test: 462 通过 / 0 失败 / 2 skip(基线 455/453/2 → pass +7,不减反增)
- typecheck: 通过(tsc --noEmit 0 错误)
- docs-check: 通过(无违禁文档模式)
- git diff --check: 无输出(工作树已全部提交,status 干净)

## 遇到的问题

1. **`47.96.146.209` 的官方域名未联网验证** → 映射值 `zdpi.org.cn` 来自对
   「浙江省发展规划研究院」官网的记忆,开发沙箱禁 egress(curl/nslookup/dig 全部被拦),
   无法实测确认。已采取防御:onerror 兜底链保证域名即使错误也只是多一次 404 → 优雅降级
   emoji,不破相;修正成本一行。**需 boss 联网复核**(如错,改 `DOMAIN_LOGO_MAP` 值即可,
   单测同步改)。
2. 沙箱禁止 node 脚本执行(批量替换 49 处只能逐条 Edit 完成,已用 grep 复核 49/49)。
3. map-markers 徽章为纯字符串 HTML,候选链以内联 `data-fb` JSON + onerror 实现
   (无闭包可用);`recruitmentBadgeSVG` 保持原样(测试/无 logoUrl 路径,本就只显示 emoji)。

## 证据

- 测试输出摘要: `npm test` → `tests 462 / pass 460 / fail 0 / skipped 2`
- 新测试逐条: `npm --prefix ... test -- --test-name-pattern 'favicon|IPv4|DOMAIN_LOGO|mapped|unmapped|曦曦'` → 全绿
- seed 清理复核: `grep -c 'google.com/s2' seed-data.ts` → 0;`grep -c 'favicon.im/'` → 49
- 裸 IP grep 复核: 全库仅 `47.96.146.209:8111` 一处(2 次出现,同一 URL)
- 提交: `65ad2a6`(lib+测试)/ `4c17000`(seed 清理)/ `28c688d`(三消费点接线)

门禁: PASSED
结论: OK
