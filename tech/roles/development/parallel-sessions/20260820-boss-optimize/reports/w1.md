# w1 汇报(2026-08-20)

worktree:`/Users/acccan/dm-wt-w1`,分支 `fix/cluster-consistency`,base `cc9fae1`。

## 实际改动(3 commits,全部小步)

- `server/src/lib/spatial-query.ts` → 新增导出纯函数 `cityLabelMatchesCoordinates(city, lng, lat)`(spatial-query.ts:118-141):
  - 标签 bare 归一(去 省/市 后缀)后命中 `CITY_REFERENCE_BOXES` 已知框,但坐标不在框内 → false(串味行);
  - 参考框未收录城市 / 坐标缺失或非法 → true(无可判断,放行);
  - 复用既有参考框表,**零扩展**(深圳/成都/北京/上海/广州/武汉/杭州 7 城已全),不碰 `cityBoundsConsistencySql`/`singleCityReference` 查询语义(仅新增导出)。
- `server/src/lib/city-cluster.ts` → `clusterCities` 分组循环内两道防御(city-cluster.ts:82-87):
  - ① 串味剔除:`!cityLabelMatchesCoordinates(...)` → continue(成都假聚合根因);
  - ② LOD 计数口径:`lodVisibleAtZoom`(tier <= `maxTierForZoom(zoom)`,未打标按 `TIER_DEFAULT=12` 不计,该 zoom 下服务端取数口径一致)→ 残留 tier>zoom 行不进徽章,同 zoom 导航历史无关。徽章样式/触发阈值 zoom≤8/下钻 zoom 11 均未动。
- `server/tests/city-cluster.test.mjs` → 7 项新契约 + `workPoi` 助手加 tier 参数(缺省 0=永显,旧用例语义不变)。
- `server/tests/spatial-query.test.mjs` → 2 项 `cityLabelMatchesCoordinates` 正反例。
- `tech/21-city-clustering.md` → 规则 6/7 与 §5.1 记录新语义与验收映射。

**未改**:`map-shell.tsx`(clusterState 区无需改动——两道防御都在纯函数内,调用面不变)、`use-work-viewport.ts`(池语义未动,计数口径在 clusterCities 内修正)、`city-centers.ts`/`company-logo.ts`/`crawler/`/`server/data/` 零改动。

## 三个症状各自修复方式与验证

1. **成都假聚合**:串味行(city=成都 但坐标=杭州)在分组前被参考框判定剔除。验证:新测试「剔除 city 标签与坐标参考框不符的串味行」「串味防御覆盖深圳/北京/上海/广州/武汉」——每城只保留真实坐标行,徽章 count=1;成都徽章不再含杭州坐标行。
2. **数量不正确/不稳定**:徽章 N = 该 zoom 下 LOD 可见(tier ≤ floor(zoom))公司数,与同 zoom 服务端 `/api/pois?filters.maxTier` 取数口径完全一致。验证:新测试「LOD 阈值边界(tier==zoom 计入/tier==zoom+1 不计)」「计数稳定性——池残留 tier 6/9/13 行时 zoom 5 徽章数不变」「未打标公司(tier 缺省 12)在 zoom≤8 不计」。下钻 zoom 11 后个体 pin 按 LOD 显示 tier≤11 公司,数量多于徽章是 LOD 设计(放大涌现),非计数 bug(已写入 tech/21 规则 7)。
3. **贝达药业**:代码级无 bug(seed `seed-data.ts:1070` 的 betta-hangzhou 无 sites/tier,是 seed 路径形态;drop `official-career/betta-hangzhou.json` 导入后归 city='杭州'/tier 6)。已补契约测试:构造 drop 导入形态 POI(city='杭州',120.258/30.438,tier 6)→ zoom∈[6,8] 出现于「杭州」徽章(count=1,锚点=杭州静态中心 120.15/30.27);zoom 5(tier 6>5)LOD 不可见 → 不出现在徽章;同坐标标签成都的串味行在混池中被剔除。DB 行是否过期(疑 tier=12)属数据层,需 boss 合并后验证。

## 门禁结果

- npm test:**464 tests / 462 pass / 2 skip / 0 fail**(基线 455/453+2;净增 9 pass,无减少)
- typecheck:0 错误
- make docs-check:通过
- git diff --check:无输出

## 遇到的问题

- **DB 验证 SQL 未执行**:worktree 内 `make db-status` 被权限拒绝,且规则禁止自行起 DB/跑 Env 命令。串味 147 行是否仍残留、betta-hangzhou DB 行 tier 值,均留给 boss 合并后验证(下述 SQL)。
- 新测试初版期望值有误(「北京」与「北京市」是不同标签分组;无坐标组按既有规则省略)→ 已修正测试并确认是既有语义,非新 bug。
- 计数口径取舍:严格「徽章 N == 下钻后立即渲染 pin 数」在客户端池模型下不可达(zoom≤8 时 tier 6-11 行尚未拉取,无法先知);采用「与同 zoom 取数口径一致(导航历史无关)」的可验证语义,已在 tech/21 规则 7 说明。

## DB 验证 SQL(留给 boss)

```sql
SELECT c.name, s.city, s.lng, s.lat, c.tier FROM company_sites s JOIN companies c ON c.id=s.company_id WHERE c.slug='betta-hangzhou';
```
期望:city='杭州',lng≈120.258,lat≈30.438,tier=6(若 tier=12/行过期 → 属数据层问题,w5/boss Env 步骤处理)。

## 证据

- `cd server && npm test` 输出:`tests 464 / pass 462 / fail 0 / skipped 2`(city-cluster 新增 7 项、spatial-query 新增 2 项全绿)
- `npm run typecheck`:`tsc --noEmit` 0 错误
- `make docs-check` + `git diff --check`:通过/无输出
- 汇报路径:本文件;git log:`f860800 docs(w1)` ← `7d3e91e fix(w1) 串味剔除+LOD` ← `0b247a5 fix(w1) cityLabelMatchesCoordinates`

门禁: PASSED
结论: OK
