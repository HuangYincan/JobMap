# w5 汇报(2026-08-20)

worktree: `/Users/acccan/dm-wt-w5`,分支 `feat/data-code-coverage`,基于 `cc9fae1`,4 个 commit,未 merge/push。

## 实际改动(按任务)

### 1a. 数据文件补齐 source(704 drops)

> ⚠️ 与 prompt 描述不符的现状:grep 全量后,**704 个 drops 全部缺 `source` 键**(不是「部分/得物+腾讯」)。逐文件扫过 `server/data/recruitment/**/*.json`(排除 geocode-overrides.json 与 README),无一有 source。已按 ETL 文档补齐全部 704 个,commit `9a57758`。

- `server/data/recruitment/radar/*.json`(630 个)→ `"source": "xiaozhao-radar"`(tech/roles/data/etl/xiaozhao-radar.md;与 radar_fixture 的 source.code 一致)
- `server/data/recruitment/official-career/*.json` 含 `portal-feishu-*` 岗位的 23 个(得物/蔚来/小马智行/智元机器人/禾赛科技/拓竹科技/影石Insta360/元气森林/轻舟智航/锐石创芯/臻驱科技/小鹏集团/安克创新/莉莉丝游戏/游戏精酿/新石器/卡尔动力/度小满/光轮智能/它石智航/波克/MiniMax/Momenta)→ `"source": "feishu-ats"`(feishu-ats.md)
- 其余 `official-career/*.json`(51 个)→ `"source": "official-career"`(official-career.md)

插入位置:每个 drop 顶层 `"slug"` 之后;diff 精确为 704 insertions、零其他改动。未动 geocode-overrides.json。

### 1b. 落库 source 尊重 drop(commit `c799336` + 注释修正随 `2da8a6e`)

- `server/src/lib/recruitment-import.ts:39-76` 新增 `SOURCE_META` 常量表(drop source code → sources 表元数据;取值对齐 ETL 文档:xiaozhao-radar 的 origin_uri/attribution 等)
- `server/src/lib/recruitment-import.ts:320-343` apply 改为按 company 解析 source:`sourceIdFor(company.source ?? 'seed')`,按 code 幂等 upsert + 同次 apply 内缓存;缺失 source 回退 'seed'(旧语义完全保留);未收录 code 保留其 code、元数据用 seed 默认值(不臆造 attribution)
- `server/src/lib/recruitment-source.ts:38-41` `SourceCompany` 增加 `source?: string`(类型侧最小补充;边界外的唯一触碰,已在结论标注)
- provenance 校验语义(validateSourceCompany 等)零改动

### 2. CITY_CENTERS 扩展(commit `033ab4e`)

- `server/src/lib/city-centers.ts:36-51` 只增 15 城:东莞 佛山 福州 合肥 湖州 金华 九江 宁波 南宁 石家庄 芜湖 肇庆 郑州 珠海 鄂尔多斯
- 口径:grep 全部 drops 的 site.city(242 个不同值)后,收录**此前缺失、≥3 个站点的中国大陆城市**;坐标取行政中心一带(0.01° 精度,通过既有 test 的 lng 73–136 / lat 3–54 校验)。苏州/重庆已在表内(现 16 城未删改);海外城市(伦敦/新加坡等)不入表(超中国境内校验边界)

### 3. 站点名归一(commit `9a57758`)

- `server/data/recruitment/radar/腾讯.json`:4 条 site.name「腾讯—剩余岗位」→「腾讯·北京/上海/广州/深圳」(按 site.city 裸名派生);address「北京/上海/广州/深圳」原样保留(geocode 输入)
- 全量扫描 radar drops 的 site.name:另 105 条「{公司}—{项目名}」类(如「美团—北斗计划」「三一集团—星辰计划」)是雷达快照的真实项目名,非占位,未动;如需一并归一请 boss 裁决

### 4. radar CITY_TARGETS 扩展(commit `2da8a6e`)

- `crawler/app/domain_map_importer/radar_jobs.py:26-54` CITY_TARGETS 7→10 城(加 南京/苏州/西安),同步 CITY_KEY(nanjing/suzhou/xian,与 ats_feishu.CITY_PINYIN 一致)、CITY_FULL、CITY_PROVINCE(江苏/江苏/陕西)
- `crawler/tests/test_acquisition.py:172-188` 非目标城样例 南京→郑州(原断言「南京 不命中」已不成立);新增 `test_maps_new_target_cities_nanjing_suzhou_xian` 断言 site id/城市/省份契约;:281 注释 7→10 城
- 只改抓取配置,不发起任何抓取

### 5. city_site_id 确定性兜底(commit `2da8a6e`)

- `crawler/app/domain_map_importer/ats_feishu.py:228-246` 派生链:别名归一 → 去「省/市/区」后缀与空白 → CITY_PINYIN 查表 → 仍无拼音保留中文原样(确定性,同城市永远同 id);`unknown` 仅空城市兜底(有文档化理由:空城市无法派生)
- `crawler/tests/test_ats_feishu.py:197-204` 新增 `test_city_site_id_deterministic_derivation`(北京市→beijing、郑州/郑州市同 id、空→unknown)

## 门禁结果

- npm test:`cd server && npm test` → **455 通过 / 453 pass / 2 skip / 0 fail**(与基线一致)
- typecheck:`npm run typecheck` → **0 错误**
- crawler 单测:`cd crawler/app && python3 -m unittest discover -s ../tests` → **66 tests, OK**(等价 make test-unit)
- `make docs-check` → passed;`git diff --check` → 无输出

## 遇到的问题

1. **prompt 事实偏差**:「部分 drops 缺 source」实为全部 704 个缺 source。已按任务意图全量补齐(任务 1a 本就要求「找缺 source 的补上」),范围差异在汇报中说明。若 boss 只想补得物/腾讯,可精简,但不建议(落库逻辑按 source 分桶,数据不带 source 则全部仍落 'seed')。
2. **amend 误伤提交**:修复 import 注释时,amend 落在当时的 tip(crawler commit)上,crawler 变更与注释修正混入同一 commit。已用 `git commit --amend` 改回诚实 message(`2da8a6e` 内容含 crawler + ride-along 注释修正,body 已注明);c799336 保留原始实现+原始注释。**最终树内容与全绿门禁跑过的内容完全一致**(amend 只改 message,最后一次 typecheck/diff 已在最终树上复跑)。
3. **rm 被沙箱拦**:工作树根遗留 4 个一次性扫描脚本(untracked,`fill_source.py` / `normalize_names.py` / `scan_cities.py` / `scan_names.py`),无法删除,请 boss/merger 顺手清理;未入库、不影响任何门禁。
4. 海外站点城市(伦敦/新加坡/东京等)与 <3 站点的中国大陆城市未入 CITY_CENTERS(校验边界 + 数据量权衡),如需可另批。

## 证据

- commit:`9a57758`(data source+腾讯名归一,704+4 处)、`c799336`(import source 落库)、`033ab4e`(city centers +15)、`2da8a6e`(crawler 十城+city_site_id)
- 测试输出摘要:`tests 455 / pass 453 / fail 0 / skipped 2`;crawler `Ran 66 tests ... OK`;`make docs-check` → `Documentation policy check passed.`
- 腾讯.json 归一后样例:`腾讯-site-beijing | 腾讯·北京 | 北京市 | 北京/上海/广州/深圳`(address 未动)

门禁: PASSED
结论: OK
