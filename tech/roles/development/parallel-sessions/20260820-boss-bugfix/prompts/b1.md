# Workstream b1 — 修复 positions 重复(同 external_id 双行)→ 重复 key 警告

## 背景(boss 2026-08-20 实测定位)

- 用户报错:poi-card.tsx:326 `positionsPreview.map(pos => <span key={pos.id}>` 出现
  **同 key 重复警告上百条**(如 `radar-c79bdd7990f2`),根因在数据层:
- DB 实测:positions **21111 行 / 10578 distinct external_id → 10533 重复行**。
  source_id 分布:1=seed 10578 行(8/17-8/19 旧行)、31=9800、33=694、32=39(今日新行)。
- 成因:2026-08-20(w5 + boss import 落库)import 改为尊重 drop 自带 source,同 external_id
  在旧 source(seed)与新 source(xiaozhao-radar 等)下各存一行 —— upsert 唯一键是
  `(source_id, external_id)`,source 变了就插新行,旧行不删。
- positions 表**无任何外键引用**(实测 0);applications 表存 position_id(无 FK 约束,
  冗余了 title/company_name/apply_url,悬空可优雅降级)。
- 契约:authentic 岗位 = external_id `radar-*` / `portal-*`(isAuthenticPositionId)。

## 任务(worktree:`/Users/acccan/dm-wt-b1`,分支 `fix/positions-dedup`)

1. **import 自愈去重 + source 迁移**(`server/src/lib/recruitment-import.ts`,apply 事务内、
   positions 插入循环前或循环内):
   - 对 plan 中每个 authentic position:先 `UPDATE positions SET source_id = $new WHERE external_id = $ext AND source_id <> $new`(迁移旧行 source;同一事务,幂等);
   - 再删除该 external_id 下多余重复行(保留 MIN(id) 一行):`DELETE FROM positions WHERE external_id = $ext AND id <> (SELECT MIN(id) FROM positions WHERE external_id = $ext)`(或等价写法,注意自引用子查询写法);
   - 然后照常 `ON CONFLICT (source_id, external_id) DO UPDATE` upsert(迁移+去重后唯一键不再冲突,DO UPDATE 刷新内容)。
   - 以上顺序不可颠倒:先迁移再删重(删重保留 MIN(id) 即可先删后迁,但迁移必须在 upsert 前)。整体须在现有 BEGIN/COMMIT 事务内,失败回滚。
   - 保持现有「只导入 authentic 岗位」契约;不去动 seed 示例岗位。
2. **组件防御**(`server/src/components/poi-card.tsx` positionsPreview 渲染):map 前按 `pos.id`
   dedup(保序保首个),消除任何残余重复 key 警告(数据修复前的过渡期也不报警)。
3. **测试**:在 server/tests/ 既有 recruitment-import 契约测试(或新建)补:
   - source 迁移用例(同 external_id 旧 source 行迁移到新 source);
   - 重复清理用例(同 external_id 双行 → 保 MIN(id) 一行);
   - poi-card positionsPreview dedup 契约(如该组件有既有契约文件则扩展;无则新建小契约)。
   不需要真实 DB(纯逻辑/SQL 模板断言或注入 mock pool)。
4. 门禁全绿:server npm test / typecheck / make docs-check / git diff --check。

## 文件边界(拥有)

- `server/src/lib/recruitment-import.ts`(仅 positions upsert 区)、`server/tests/`(仅 import/poi-card 契约)、
  `server/src/components/poi-card.tsx`(仅 positionsPreview dedup)

**不碰**:map 相关(map-shell/map-markers/use-poi-map,归 b2)、package.json/升级(b3)、
crawler/、server/data/、其余 tech/。**不要**跑 import/psql(工具已禁;boss 合并后重跑验证)。

## 门禁(全部须绿)

- `cd /Users/acccan/dm-wt-b1/server && npm test`(基线 477 tests / 475 pass / 2 skip,不得减少)
- `cd /Users/acccan/dm-wt-b1/server && npm run typecheck`(0 错误)
- `make docs-check`(worktree 根)、`git diff --check` 无输出

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-bugfix/reports/b1.md`:
改动清单(file:line)、迁移/去重/upsert 顺序说明、测试用例、遇到的问题。小步 commit(Conventional Commits)。
**不要 merge/push**。
末两行必须精确:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```
