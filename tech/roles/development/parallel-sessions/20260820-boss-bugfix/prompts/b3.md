# Workstream b3 — Next.js 16.3.1 + React 19.2.8 升级(清 version-staleness 警告)

## 背景(boss 2026-08-20 查明)

- 用户报错:Next.js version-staleness 警告(https://nextjs.org/docs/messages/version-staleness)。
- 现状:next **15.5.23**(= next@15 最新,必须升大版本)、react/react-dom **19.0.8**;
  最新:next **16.3.1**(Node >=20.9 ✓,本机 26.7)、react/react-dom **19.2.8**。
- devDeps 已是 @types/react 19.2.18 / @types/react-dom 19.2.4;next.config.ts 仅 reactStrictMode(风险面小)。
- **boss 已完成环境准备**(worktree 内,勿重复):package.json 已改为
  `next: ^16.3.1, react: ^19.2.8, react-dom: ^19.2.8`;worktree 的 node_modules 已独立安装
  (symlink 已移除,真实 install),package-lock.json 已随之更新(等你 commit)。
- Next 16 已知变更点(以此线索排查,以实际报错为准):dev/build 默认 Turbopack;
  middleware → proxy 迁移;若干 legacy API 移除;`next lint`/ESLint 版本要求;React 19.2 行为差异。

## 任务(worktree:`/Users/acccan/dm-wt-b3`,分支 `chore/next-16`)

1. **跑门禁找破坏点**:`cd server && npm run typecheck` → `npm test`(基线 477/475/2)→
   `npm run build`(生产构建,重点!)。逐项修复报错:
   - 类型错误(React 19.2 / Next 16 类型变化);
   - 构建错误(Turbopack/webpack 行为差异、移除的 API、config 选项);
   - 测试失败(渲染/契约测试若受升级影响)。
2. 若 `next build` 需要调整 next.config.ts(Turbopack 选项、弃用字段)→ 最小改动修复并说明。
3. **不做的**:不迁移 UI 代码/不改业务逻辑;若某处升级破坏面极大且无解,BLOCKED 回报(boss 裁决回退)。
4. 修复后全绿:typecheck / npm test / npm run build / make docs-check / git diff --check。
5. commit 应包括:package.json、package-lock.json、代码修复文件。

## 文件边界(拥有)

- `server/package.json`、`server/package-lock.json`(已由 boss 改/安装,commit 即可,勿再动版本号)
- `server/src/`(仅升级导致的破坏点修复)、`server/next.config.ts`(仅必需)、`server/tests/`(仅升级导致失败的用例)

**不碰**:crawler/、server/data/、业务逻辑重构、与升级无关的代码。**不要**跑 npm install/ci(工具已禁;boss 已装好)。**不要**动其他 worktree 的 node_modules。

## 门禁(全部须绿)

- `cd /Users/acccan/dm-wt-b3/server && npm run typecheck`(0 错误)
- `cd /Users/acccan/dm-wt-b3/server && npm test`(不得减少 pass)
- `cd /Users/acccan/dm-wt-b3/server && npm run build`(成功产出 .next)
- `make docs-check`(worktree 根)、`git diff --check` 无输出

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-bugfix/reports/b3.md`:
升级后版本、破坏点清单与修复方式(file:line)、build 结果、遇到的问题(尤其 Turbopack/webpack 差异)。
小步 commit(Conventional Commits,`chore:` 前缀)。**不要 merge/push**。
末两行必须精确:
```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```
