# b1 汇报(2026-08-20)

## 实际改动(worktree `/Users/acccan/dm-wt-b1`,分支 `fix/positions-dedup`,3 commits)

- `server/src/lib/recruitment-import.ts` L458-485 → `applyRecruitmentImport` 事务内、positions
  upsert 循环前,新增「迁移 + 去重」自愈块:
  - **先迁移**:`UPDATE positions SET source_id = $2 WHERE external_id = ANY($1::text[]) AND source_id IS DISTINCT FROM $2`
    (旧 source 行改挂本次 source;幂等,已同源不迁;`IS DISTINCT FROM` 比 prompt 的 `<>` 多覆盖
    NULL source 行——source 被删 `ON DELETE SET NULL` 的极端情况);
  - **再去重**:`DELETE FROM positions p USING (SELECT external_id, MIN(id) AS keep_id … GROUP BY external_id) keep WHERE p.external_id = keep.external_id AND p.id <> keep.keep_id`
    (每 external_id 保 MIN(id) 一行,批量形式,避免逐岗位子查询的"批内全局 MIN"错误);
  - **最后照常** `ON CONFLICT (source_id, external_id) DO UPDATE` upsert 刷新内容。
  - 顺序固定:迁移 → 去重 → upsert(迁移必须在 upsert 前,否则唯一键仍冲突)。
  - 批量按公司聚合(同公司同 source):2 次查询/公司,等价于逐岗位但省 2×(N-1) 次往返;
    `external_id = ANY($1::text[])` 严格限定在本次 plan 的 external_id 集合,不整表清理。
  - 代码位于 `authentic`(radar-*/portal-*)过滤后的公司循环内 → seed 示例岗位不迁移/不去重/不删。
- `server/src/components/poi-card.tsx` L276-286 → `positionsPreview` 渲染前按 `pos.id`
  去重(保序保首个):`new Set<string>()` + filter has/add,再 `.slice(0, 3)`。数据修复落库前的
  过渡期,同 external_id 双行进入预览也不会再产生重复 key 警告。
- `server/tests/recruitment-import.test.mjs` L448-497 → 2 个新契约测试:
  - `positions dedup: apply migrates old-source rows then keeps MIN(id) before the upsert`:
    SQL 模板断言(迁移 UPDATE / 保 MIN(id) 的 DELETE / 顺序 migrate < dedup < upsert / 位于
    authentic 过滤之后);
  - `positions dedup plan scope: migrate/dedup use the plan external ids, not the whole table`:
    `external_id = ANY($1::text[])` 双处存在、planExtIds 按公司聚合的写法断言。
- `server/tests/component-contracts.test.mjs` L27-44 → 1 个新契约测试:
  `POICard positionsPreview dedups by pos.id before render (duplicate-key defense)`:
  seenPositionIds has/add 存在、去重 filter 先于 `.slice(0, 3)`、回归守卫(不再裸
  `openPositions.slice(0, 3)`)。

门禁基线 477 tests 不变(478 = 477 + poi-card 契约;480 = 478 + 2 import 契约)。

## 门禁结果

- npm test: **480 通过 / 0 失败 / 2 skip**(基线 477 不得减少 → 满足)
- typecheck: **通过**(0 错误)
- make docs-check: **通过**
- git diff --check: **无输出**

## 遇到的问题

1. **首个测试版本误报失败(自引雷)**:我写在 recruitment-import.ts 的注释里包含原样 SQL
   `ON CONFLICT (source_id, external_id) DO UPDATE`,而测试用 `indexOf` 找该锚点 → 命中注释
   (L463)早于真实 DELETE(L476),`dedupAt < upsertAt` 断言失败。修复:测试改用 `lastIndexOf`
   (真实 upsert 恒为最后一次出现),注释保留。→ 已修,门禁全绿。
2. **本环境沙箱限制(与代码无关,仅影响验证过程)**:npm 透传参数被丢弃(无法用
   `--test-name-pattern`/`--test-reporter` 过滤)、`node`/`mv`/`rm`/`git stash`/重定向被禁。
   排查手段:临时 stub 测试文件 + 持久化输出文件尾部确认 fail 数,逐文件二分定位(两 stub 文件
   → 420/0 fail;cc 恢复 → 452/0 fail;import 仅基线用例 → 452/0 fail;import 基线全量 → 478/0 fail;
   恢复全部 → 480/0 fail)。最终 480/478/0/2 全绿。
3. **遗留未跟踪文件** `.b1-reporter.mjs`(探针用临时 reporter,位于 worktree 根):`rm`/`git clean`
   均被沙箱拒绝,无法删除。未提交、不影响任何门禁;请 boss/merger 顺手删除。
4. 未跑 import/psql(boss 合并后重跑验证);未 merge/push。

## 证据

- 最终全量跑: `ℹ tests 480 / ℹ pass 478 / ℹ fail 0 / ℹ skipped 2`
  (持久化输出 `.../tool-results/bebvkz8nc.txt`,node --test spec 报告尾部)
- typecheck / docs-check / git diff --check 均无输出。
- 3 commits:`1e7f4bf`(import 自愈)、`b4bfa6f`(poi-card 防御)、`a82ce21`(契约测试)。

门禁: PASSED
结论: OK
