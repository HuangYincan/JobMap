# w1 汇报(2026-08-20)

## 实际改动

只改 1 个文件:`server/src/components/map-shell.tsx`(+30 / -7),commit `51c0406`。

- **L58-70(新增)`RAIL_PANEL_MODULES` 模块清单**:`as const satisfies Record<string, () => Promise<unknown>>`,含 7 个 loader(poi-detail / jd-panel / auth-modal / account-panel / recent-panel / saved-panel / layers-panel)。路径与 L48-55 的 `dynamic()` 声明逐一同源;SavedList 与 SavedPanel 同文件(saved-panel)已去重。注释说明用途(把 dev 冷启动「首点按需编译」提前到页面加载,消除 Turbopack dev 首点 `performFullReload` 整页刷新)。
- **L73-90(重构)`prefetchRail(panel)`**:保留原签名与调用点(L2125-2126、2142-2143、2154-2155、2162-2163、2174-2175 附近 `onMouseEnter`/`onFocus` 兜底不变),分支体改为从 `RAIL_PANEL_MODULES` 取值,消灭重复的 import 路径。
- **L92-95(新增)`prefetchAllRail()`**:对清单全部模块 `void load()`。
- **L404(挂载 effect 首行)`prefetchAllRail();`**:追加在现有 mount useEffect(L402-411)内,未新建空 effect;重复调用幂等(后续 import 命中缓存)。

未改:dynamic 声明原样保留(保持代码分割)、未加 `ssr:false`、未加 loading 组件、未动任何视觉/交互语义(按钮、样式、openRail/点击逻辑零改动)。未新增 lib 文件。

## 门禁结果

- npm test: 500 通过 / 0 失败 / 2 skip(基线 486 pass / 2 skip,高于基线)
- typecheck(`tsc --noEmit`): 通过(无输出)
- make docs-check: 通过(Documentation policy check passed)
- git diff --check: 通过(clean)

## 遇到的问题

- **Turbopack dev 无法在 worktree 内启动**:`next dev -p 3999` 秒级 FATAL panic——`Symlink [project]/node_modules is invalid, it points out of the filesystem root`(worktree 的 `server/node_modules` 是指向主树 `/Users/acccan/domain-map/server/node_modules` 的 symlink;Turbopack 拒绝解析出项目根。沙箱内/外均复现,与本次改动无关)。
  → 改用 `next dev --webpack -p 3999`:正常 Ready,无 panic。
- **无浏览器自动化可用**:本会话无 Playwright MCP 工具;`server/package.json` 无 playwright/puppeteer 依赖,`~/.cache/ms-playwright` 不存在;会话内 curl / node fetch 均被权限策略拒绝。
  → 首点不刷新的真实浏览器行为**未能实测**。修复逻辑本身极简(模块级清单 + 挂载 effect 一行调用),已由 typecheck + 500 测试门禁覆盖;boss 若需最终确认,可在主树 dev 或含真实 node_modules 的环境首点验证。
- **webpack dev 下 `next dev --webpack` 的 chunk 预载语义**与 Turbopack 有差异(webpack dev 无 performFullReload 整页刷新问题),故即便跑通 webpack 也无法 1:1 复现原 bug 场景;本次验证仅到「服务可启动、代码在真实编译链路中通过」为止。

## 证据

- commit:`51c0406 fix(map-shell): 挂载时预载 rail 面板 chunk,消除 dev 冷启动首点整页刷新`(1 file changed, +30 / -7;`git status` 干净)
- npm test 输出:`ℹ tests 502 / pass 500 / fail 0 / skipped 2 / duration 6430ms`
- Turbopack panic 日志:`/var/folders/.../T/next-panic-*.log`(错误 ID:Symlink [project]/node_modules is invalid)
- webpack dev Ready 日志:`▲ Next.js 16.3.1 (webpack) / ✓ Ready in 502ms`(后已 kill,lsof 确认 3999 清空;`.next` 未删除)

门禁: PASSED
结论: OK
