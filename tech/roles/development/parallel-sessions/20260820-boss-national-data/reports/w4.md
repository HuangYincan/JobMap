# w4 汇报(2026-08-20)

## 实际改动(分支 fix/geocode-address-strategy;本次新增 commit 2992fb4 + e37cb7d;分支 tip 另有 ca54ce7 20s fetch 超时守卫,为 w4 批次遗留产物)

- `server/src/lib/site-geocode.ts`(commit 2992fb4, +142 行)
  - 新增 `CITY_DISTRICTS`(46 城市 → 区县/功能区清单,覆盖 radar/官方 drops 实际出现的城市)+ 反查索引 `CITY_DISTRICT_INDEX`(同名区县可属多市:西湖区=杭州/南昌、朝阳区=北京/长春)。
  - 新增 `addressConflictsWithCity(address, city)`(L317-329):地址-城市一致性判定。地址含「非目标城市的已知城市名」(`ADDR_CITY_RE` 匹配 XX市,经 `cutCityToken`/`bareCity` 归一)→ 不可信;地址含「非目标城市的已知区/县名」(`ADDR_DISTRICT_RE` 匹配,查 CITY_DISTRICT_INDEX)→ 不可信;无法判定(未收录区名/无区县)→ 放行,由 regeo 区级校验兜底。
  - 新增 `addressConflictsWithRegeoDistrict(address, adname)`(L338-348):regeo 区级校验。仅比对「未收录区名」的区名与落点 adname,不一致 → 拒;已收录区名由前置闸门把关(行政区边界 adname 模糊不可比)、功能区名(高新区/园区/新区/自治区…)与超长 token(省名)跳过。
- `server/scripts/geocode-sites-apply.mjs`(commit e37cb7d, +75/-23)
  - 主循环 L251:地址检索前置闸门 `siteHasStreetAddress(site) && !addressConflictsWithCity(addr, target.city)` —— 杭州地址("西湖区莲花街333号…")在 广州/成都/北京 站点判定不可信 → 跳过地址 geocode,直接公司名检索。
  - 抽出公共 helper `searchCompanyPoi(query, target)`(L155-176):原内联公司名检索逻辑(placeTextSearchRest → pickBestOfficePoi → gradeOfficePoi),供无地址/闸门拒绝/区级校验拒绝三条路径复用,评分与节流语义不变。
  - L297-317:regeo 区级校验 —— 地址检索命中后,落点区(adname)与地址文本区名不符(未收录区名)→ 回退公司名检索,复检 regeo(re2),仍不合格 → `address-district-mismatch` 记 unresolved。两种路径都不写错坐标。
  - 文件头注释(L25-29)记录 w4 闸门语义。
- `server/tests/site-geocode.test.mjs`(commit 2992fb4, +7 用例)
  - 奇安信回归:`西湖区莲花街333号莲花商务中心b座9楼` 在 广州/成都/北京 站点 → 拒(不可信),杭州站点 → 放行。
  - 正确地址+正确城市 → 放行(北京市朝阳区望京东路6号/上海市杨浦区/南山区深圳市软件产业基地 等,含省前缀与「区名先于市名」)。
  - 跨市污染:上城区市民街(宁波银行)-shanghai/-beijing 拒、-hangzhou 放行;市区级联(杭州市西湖区文二西路712号 + 广州)拒。
  - 同名区县不误杀(南昌西湖区站前西路)、县级市不误杀(台州温岭市中华路728号)、功能区不误杀(苏州高新区/成都高新区)。
  - 未收录区名/海外地址 → 放行(兰州市城关区/鄂尔多斯/韩国仁川/横滨),由 regeo 区级校验兜底。
  - `addressConflictsWithRegeoDistrict`:霞山区→赤坎区落点拒、同区通过、adname 空放行;已收录区名(滨江区网商路→西湖区)与功能区(高新区→武侯区/工业园区→虎丘区)跳过,超长省名 token 跳过。

## 门禁结果(提交后终态复跑)

- `cd server && npm test`:**513 跑 / 511 pass / 2 skip / 0 fail**(基线 504 pass/2 skip + 本次 7 用例;0 新增失败)
- `npm run typecheck`:`tsc --noEmit` 通过
- `make docs-check`:通过(Documentation policy check passed)
- `git diff --check`:通过;worktree 无未提交改动

## 遇到的问题

1. **worktree 有上次中断遗留的未提交半成品**(3 文件已实现 + 2 个未跟踪诊断脚本 w4-diag.tmp.mjs / w4-survey.tmp.mjs)。按幂等恢复规则判断:实现完整、与任务规格逐条相符、测试全绿 → 直接验收提交(拆 2 个逻辑 commit),未重做;诊断脚本为临时产物,验证后已删除,未提交。
2. **「地址检索失败 → 公司名检索」的取舍**:原流程中地址 geocode 失败(无结果/超时/网络错)仍按 unresolved 记(不吞错、不烧配额),任务要求"超时/网络错误按 unresolved 记"即此语义;仅「地址不可信(闸门拒)/区级校验不符」两条路径回退公司名检索。若 boss 期望地址检索失败也兜底公司名检索,可后续加,但会双倍 API 调用。
3. **地址-区名对照清单覆盖度**:CITY_DISTRICTS 覆盖 46 个目标城市(radar/官方 drops 实测出现的城市),未收录城市/区名的地址放行 → 由 regeo 区级校验兜底(未收录区名才可比对);已收录但撞名的功能区(高新区等)保守放行 → 依赖 regeo 城市级校验。误杀方向是「回退公司名检索」而非写错坐标,安全。

## 证据

- 真实数据核对:survey(diag 脚本)显示 2018 个站点中 52 个地址文本含跨市区县(如 宁波银行-site-shanghai "上城区市民街69号…"、奇安信类继承杭州地址),本次闸门全部改走公司名检索,不再用杭州地址在广州检索。
- 回归证明:测试 `addressConflictsWithCity rejects the 奇安信 Hangzhou address on non-Hangzhou sites` 通过 —— "西湖区莲花街333号莲花商务中心b座9楼" + 广州市/成都市/北京市 → true(拒,走公司名检索),+ 杭州市 → false(地址检索)。此即实测错配案例(广州"花都区西湖" 113.20/23.38)的防复发路径:闸门在地址检索前拦截,根本不发起跨市地址 geocode。
- 门禁数字见上节;测试输出:511 pass / 0 fail / 2 skipped,typecheck 无输出(通过)。

门禁: PASSED
结论: OK
