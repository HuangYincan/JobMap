# w2 汇报(2026-08-20)

## 实际改动

- `crawler/app/domain_map_importer/ats_zhiye.py`(新)→ zhiye(北森 italent `*.zhiye.com`)适配器,zhiye-ats.md 三步探针固化为**运行时流程**(代码即探针):① 壳 HTML → `parse_bs_global`(BSGlobal 的 PortalId/tenantInfo.Domain)+ `bundle_url`(2022 portal SPA bundle)→ ② 单次礼貌 GET bundle → `extract_api_paths` grep 引号内 `/api/*` 候选(过滤 login/captcha/upload 等)→ ③ `probe_endpoint` 按 job 关键词候选,GET `?portalId=…`(非 JSON 再试 POST `{"portalId":…}`),首个返回可解析岗位列表的端点胜出,`fetch_all_jobs` 按 `page=N&pageSize=M` 分页到 total/max_jobs。全程 PoliteFetcher(robots 门禁、≥2s 节流、诚实 UA),无登录/验证码/限流绕过,带 token 的 referral 链接不请求。输出与现有 drops 同形:继承 radar curated sites + job 城市补 `{slug}-site-{pinyin}` 站点;`externalId = portal-zhiye-{id}`(portal-* 前缀 → `isAuthenticPositionId` 真实岗位);聚合类标题按 radar 校准的 `is_aggregate_title` 标记 `aggregate: true`;`source: "zhiye-ats"`。响应契约带别名(`data.list/jobs/records`、`total/count/totalCount`、行 id/title 别名),校准点已写入文档。
- `crawler/app/domain_map_importer/cli.py` → ① 新增 `zhiye` 子命令(与 feishu 同参数约定:`--dir`(radar drops,租户=139 家 `*.zhiye.com` careerUrl)、`--out-dir`、`--only`、`--interval`、`--page-size`、`--max-jobs`、`--write`(dry-run 默认,仅写有岗位的 drop));② FEISHU_TENANTS 扩充 4 家候选租户(英科医疗 `global-intco`/840753、真格基金 `zhenfund`/356542、原力灵机 `dexmal-inc`/285572(数字段取自同租户 dexmal-* drops)、算秩未来 `acnizrso7ikb`/无 website_path(仅 referral token 链接,留空只爬社招池)),证据 = radar drops careerUrl,路径模式与既有租户一致核对。
- `crawler/tests/test_ats_zhiye.py`(新)+ `crawler/tests/fixtures/zhiye-{iflytek.min.html, bundle.min.js, jobs.sample.json}`(新)→ 37 个 fixture 驱动测试:BSGlobal/bundle 解析、API 候选提取与过滤、探针流程(GET/POST 兜底、错形状跳过、空池)、payload 契约解析(主形/别名/错误码/缺列表)、岗位映射(SourceCompany 形状、family、聚合行标记、applyUrl 相对路径/兜底)、site 城市落点与补齐、crawl_company 端到端(全流程/分页/门户 500/robots 阻断/非 zhiye host/空池/缺 bundle)。与 test_ats_feishu.py 同风格(unittest + FakeGet + PoliteFetcher,零网络)。
- `tech/roles/data/etl/zhiye-ats.md` → finding-only 升级为「适配器已实现(2026-08-20),待采集」:探针实现、响应契约、校准点(bundle 路径/端点 method/字段名/robots 观察,首次实采后回填)。
- `tech/roles/data/etl/feishu-ats.md` → 「后续」记录 4 家租户扩充表(host/website_path/radar drop 证据)+ 路径模式一致性核对 + 剩余 ~40 个未配置 feishu host 的扫描机会。
- `tech/roles/data/data-sources.md` → 台账同步:feishu 行改为 live-validated + 28 租户配置;zhiye 行改为「adapter implemented, live crawl pending」。

## 门禁结果

- npm test(server): 502 测试,500 pass / 2 skip / 0 fail(基线一致,无回归)
- npm run typecheck(server): 通过(tsc --noEmit 无输出)
- make docs-check: Documentation policy check passed
- git diff --check: 通过(工作树干净,3 个 commit 已提交)
- **crawler pytest:由 boss 复验**(worker 无 python 权限)。运行命令与预期:
  ```bash
  cd crawler && PYTHONPATH=app python3 -m unittest discover -s tests -v
  ```
  预期:test_ats_zhiye.py 37 个测试全过;既有 crawler 测试(test_acquisition / test_ats_feishu / test_imports / test_manifest / test_map_access)不受影响(未改其依赖代码;test_ats_feishu 依赖的 ats_feishu.py 未改动,cli.py 无测试引用)。

## 遇到的问题

- **zhiye 端点未知(合规约束)**:zhiye-ats.md 审查结论为「端点不在 HTML,需 bundle 探针」。不写未经验证的端点,改为把三步探针固化为适配器运行时行为:探针+解析器自洽契约(探针只接受解析器能解析的响应),首个实采后锁定真实端点/字段并回填文档校准点。bundle fixture 中的 API 路径为占位(已注明),非声称的已验证端点。
- **FEISHU_TENANTS 实际在 cli.py 而非 ats_feishu.py**:prompt 写「扩充 ats_feishu.py 的 FEISHU_TENANTS」,但租户清单定义在 cli.py(ats_feishu.py 只有适配器函数)。两者都在文件边界内,租户加在 cli.py 实际定义处。
- **原力灵机 website_path 证据**:自身 drop(原力灵机.json)只有短链 `/s/-cbzfrbbx98`,数字段 285572 来自同租户(dexmal-inc)的 dexmal-原力灵机.json / dexmai原力灵机.json drops——已写入 feishu-ats.md 证据表与 cli.py 注释。
- **worker 环境无 python**:无法本地跑 pytest/py_compile,测试质量靠逐行审查(与 test_ats_feishu.py 同 FakeGet/PoliteFetcher 模式,响应队列逐调用手工推演);请 boss 复验。

## 证据

- 测试输出摘要:server `npm test` → `tests 502, pass 500, fail 0, skipped 2`;typecheck 无输出(通过);docs-check 通过。
- crawler 测试:test_ats_zhiye.py 37 tests(unittest,零网络,FakeGet 驱动);运行命令见上。
- commits:`feacd10`(adapter+tests)、`bf68d37`(cli 接线+feishu 租户)、`7d50d21`(docs),分支 `feat/ats-source-extend`,未 merge 未 push。
- 采集未执行(合规:采集属 boss Env E3,按适配器执行)。
- **二次复验(2026-08-20,幂等恢复会话)**:branch tip 已有本 WS 三个 commit,工作树干净;复验人逐项核对适配器/CLI/测试/文档并重跑全部门禁,结果与上文一致(npm test 500 pass / 2 skip / 0 fail、typecheck 通过、docs-check 通过、git diff --check 通过);FEISHU_TENANTS 24→28 家(awk 计数核对);ats_feishu.py / acquire.py / radar_jobs.py 零改动(仅边界内 9 个文件);测试数与文档声明一致(37 tests);pytest 仍待 boss 复验。

门禁: PASSED
结论: OK
