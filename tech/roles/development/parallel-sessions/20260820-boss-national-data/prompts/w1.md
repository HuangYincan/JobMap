# Workstream w1 — fix/sweep-accident-coords(drops 事故坐标清理)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。** 汇报写入批次目录 reports/w1.md(末两行 token,见文末)。

## 背景

**fecef85 事故(2026-08-19)**:refresh-radar 再生(fbc4448)丢失了 8/17 geocode 的坐标,fecef85 修复时把 7d19271 的杭州 office 坐标**复制给了所有城市 site**(如快手 site-beijing/shanghai/guangzhou/shenzhen 全部 = 杭州坐标 120.221266/30.201767)。实测 DB:108 个非杭州站点坐标错误落在杭州框(118.3-120.8 / 29.05-30.75):上海 30/北京 25/深圳 22/成都 18/广州 9/武汉 4。

**后果**:前端串味防御(`server/src/lib/city-cluster.ts:86` `cityLabelMatchesCoordinates`,标签命中参考框但坐标在框外 → 剔除)把这些「上海标签+杭州坐标」的公司从聚合徽章剔除 → 徽章:上海 26(DB 44)、北京 3(28)、广深蓉 0(8)。**防御逻辑正确,数据脏** —— 用户看到「上海才二十几家」「聚合卡片数量不对」。

## 任务

清理 `server/data/recruitment/` 下全部 drops 中的事故坐标:

1. **写清理脚本**(新建 `/Users/acccan/dm-wt-w1/server/scripts/fix-sweep-accident-coords.mjs`):
   - 遍历 `server/data/recruitment/radar/` 与 `official-career/` 全部 JSON drops;
   - 规则:site 的 `city` 存在且**不是杭州**(裸城名 ≠ 「杭州」/「杭州市」,参考 `server/src/lib/spatial-query.ts` 的 `bareCityName` 语义),且 `location.lng/lat` 落在杭州参考框(118.3 ≤ lng ≤ 120.8 且 29.05 ≤ lat ≤ 30.75)→ 将该 site 的 `location.lng/lat` 清为 `null`(保留 `address`,无 address 也保留空结构);
   - **杭州 site(裸城名=杭州/杭州市)的坐标一律保留不动**(7d19271 的真杭州坐标);
   - 幂等:二次运行零改动;输出清理统计(文件数/站点数/城市分布)。
2. **运行脚本**并验证 `git diff`:仅坐标字段变化,无其他字段被误改;抽样人工核对(如快手.json 的 5 个城市 site 坐标应清 4 个、留杭州 1 个)。
   - 若 Bash(node*) 权限被拒无法运行:脚本写完 + 验证方法写清楚,报告 boss 在 Env 阶段运行(仍算完成,但要在汇报「遇到的问题」说明)。
3. **防回归测试**:在 `server/tests/` 新增(或扩展现有)坐标一致性测试:
   - 对 drops 全量断言:任何非杭州 city 站点坐标不得落在杭州框内(与 cityLabelMatchesCoordinates 同源语义);
   - 杭州站点坐标存在性抽查(快手等)。
   - 测试风格对齐 `server/tests/` 现有契约测试(如 component-contracts.test.mjs 的写法)。
4. 若 `npm test` 中已有相关契约测试因 drops 变化失败(如坐标断言),按新事实同步(报备)。

## 文件边界

- **只允许改**:
  - `server/scripts/fix-sweep-accident-coords.mjs`(新)
  - `server/data/recruitment/radar/*.json`、`server/data/recruitment/official-career/*.json`(坐标清理,脚本产物)
  - `server/tests/`(新增坐标一致性测试)
- **不碰**:`server/src/**`(city-cluster/spatial-query 等防御逻辑是对的,不动)、`crawler/`、`db/`、`map-shell.tsx`、tech/ 文档。

## 门禁(必跑)

1. `cd /Users/acccan/dm-wt-w1/server && npm test`(基线 500 pass/2 skip,不得低于)
2. `cd /Users/acccan/dm-wt-w1/server && npm run typecheck`
3. `cd /Users/acccan/dm-wt-w1 && make docs-check`
4. `git diff --check`
5. 清理脚本运行统计 + git diff 抽查(见任务 2)

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-national-data/reports/w1.md`:改动摘要(脚本+清理统计+测试)、门禁结果(真实数字)、遇到的问题。**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

## 约束

- 不 push、不 merge、不切分支。
- Conventional Commits(建议:`fix(data): sweep fecef85 accident coords from non-hangzhou drops` 或类似)。
- 只清事故坐标,不改任何其他数据语义。
