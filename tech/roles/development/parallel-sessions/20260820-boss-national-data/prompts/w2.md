# Workstream w2 — feat/ats-source-extend(合规源扩展代码)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。** 汇报写入批次目录 reports/w2.md(末两行 token,见文末)。

## 背景

用户要求「多爬一点」(上海等大城市公司太少)。合规红线(CLAUDE.md):BOSS 直聘/牛客/小红书/实习僧不得直接抓取;不得绕过登录、验证码、限流;外部采集必须有来源审查记录(tech/roles/data/etl/)。**本 WS 只写代码与文档,不实际采集**(网络采集由 boss Env 阶段执行)。

现有合规源与扩展点(Explore 确认):
- **zhiye(北森)ATS**:138 家 radar 公司域名落在 `*.zhiye.com`,收益最大;审查文档 `tech/roles/data/etl/zhiye-ats.md` 已写好三步探针(**finding only,适配器未实现**)。
- **飞书 ATS**(`crawler/app/domain_map_importer/ats_feishu.py` 已实现,26 个 FEISHU_TENANTS):审查文档 `tech/roles/data/etl/feishu-ats.md`「后续」列出候选租户(英科医疗/算秩未来/真格基金/原力灵机等),补 FEISHU_TENANTS 即可。
- 参考样板:`crawler/app/domain_map_importer/ats_feishu.py`(完整实现)、`official-career/deepseek.json`(官方源样板)、`tech/roles/data/etl/hotjob-ats.md`(可选,30+ 站点)。

## 任务

1. **实现 `crawler/app/domain_map_importer/ats_zhiye.py`**(新):严格按 `tech/roles/data/etl/zhiye-ats.md` 的三步探针与端点语义;对齐 `ats_feishu.py` 的架构(礼貌抓取:robots 门禁、2s 节流、诚实 UA `DomainMapImporter/0.1`,复用 `acquire.py` 的 PoliteFetcher);输出与现有 drops 相同的 `SourceCompany` JSON 形状。**不实际请求网络**(写代码 + 单元测试用 fixture 数据)。
2. **扩充 `ats_feishu.py` 的 FEISHU_TENANTS**:加入 `feishu-ats.md`「后续」列出的候选租户(验证 tenant 路径模式一致性,用文档证据);不采集。
3. **接线**:`cli.py` 增加 zhiye 命令(或并入现有命令结构),与其他采集命令一致的参数与输出约定。
4. **单元测试**:`crawler/tests/` 为 ats_zhiye.py 写测试(fixture 驱动:解析 → SourceCompany 形状、聚合行标记、错误处理)。对齐现有 crawler 测试风格。
5. **文档**:更新 `tech/roles/data/etl/zhiye-ats.md`(实现状态:适配器已实现,待采集)与 `feishu-ats.md`(租户扩充记录);确认 `tech/roles/data/data-sources.md` 台账一致。

## 文件边界

- **只允许改**:`crawler/app/domain_map_importer/ats_zhiye.py`(新)、`ats_feishu.py`、`cli.py`、`crawler/tests/`、`tech/roles/data/etl/zhiye-ats.md`、`tech/roles/data/etl/feishu-ats.md`、`tech/roles/data/data-sources.md`(如台账需同步)。
- **不碰**:`server/`、`db/`、`crawler/app/domain_map_importer/acquire.py`(礼貌抓取基础设施,除非确需小改并报备)、radar_jobs.py。

## 门禁(必跑)

1. `cd /Users/acccan/dm-wt-w2/server && npm test`(确认不回归,基线 500 pass/2 skip)
2. `cd /Users/acccan/dm-wt-w2/server && npm run typecheck`
3. `cd /Users/acccan/dm-wt-w2 && make docs-check`
4. `git diff --check`
5. **crawler pytest 由 boss 复验**(worker 环境无 python 权限):你写完测试后,在汇报里给出运行命令与预期;不要因为跑不了 pytest 而降低测试质量。

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-national-data/reports/w2.md`:改动摘要(适配器/租户/接线/测试/文档)、门禁结果、遇到的问题。**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

## 约束

- 不 push、不 merge、不切分支;不实际采集网络(采集是 boss Env 阶段,会按你的适配器执行)。
- Conventional Commits(建议:`feat(data): zhiye ATS adapter + feishu tenant extend (code only)` 或类似)。
- 严格遵守合规红线:任何端点实现不得绕过登录/验证码/限流;实现前先读 zhiye-ats.md / feishu-ats.md 的审查结论。
