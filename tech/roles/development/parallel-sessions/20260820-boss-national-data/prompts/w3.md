# Workstream w3 — fix/ats-city-normalize(城市文本归一修复)

## 你的身份

boss 派发的 headless 开发 worker。**只在本 worktree 内开发,不 merge、不 push、不碰主树。** 汇报写入批次目录 reports/w3.md(末两行 token,见文末)。

## 背景

w2(zhiye 适配器,已合入 dev)的 crawler 测试有 **3 个失败**(boss 复验 `cd crawler && PYTHONPATH=app python3 -m unittest discover -s tests` 发现,103 跑 / 3 失败)。根因:城市文本归一缺陷。分支已清理,你在新分支修:

1. `PositionMappingTests.test_job_city`:`job_city({"location": "上海市浦东新区"})` 期望 `"上海市"`,实际返回 `"上海市浦东新区"` —— 区级文本未归一到市名。
2. `PositionMappingTests.test_ensure_city_sites_appends_only_new_cities`:site id 期望 `科大讯飞-site-hefei`,实际 `科大讯飞-site-合肥` —— 「合肥」拼音转换失败。
3. `CrawlTests.test_crawl_company_paginates_until_total`:期望 `meta["api_jobs"] == 3`,实际 2 —— 城市过滤/归一副作用,一并修。

## 任务

修 `/Users/acccan/dm-wt-w3/crawler/app/domain_map_importer/ats_zhiye.py`(及其测试)中的城市文本处理:

1. **`job_city` 归一**:「上海市浦东新区」→「上海市」。参考项目已有语义:`crawler/app/domain_map_importer/` 下 imports.py / radar_jobs.py 是否已有城市归一(裸城市名语义,既有 drops 的 site.city 如「上海市」);「北京市朝阳区」→「北京市」同型;区级/县级后缀去除或映射到市名。
2. **site id 拼音**:补「合肥」→ hefei 及同型城市缺失(检查你的 pinyin 映射/转换实现;测试涉及的都核对)。
3. **分页计数**:修完 1/2 后全量 crawler 测试确认 3 个失败清零、其余 100 个不回归。

## 门禁

1. **crawler 测试(必跑,本批 boss 已确认环境可跑 python3)**:
   `cd /Users/acccan/dm-wt-w3/crawler && PYTHONPATH=app python3 -m unittest discover -s tests` → **103 全过,0 失败**
2. `cd /Users/acccan/dm-wt-w3/server && npm test`(基线 504 pass/2 skip,不得低于)
3. typecheck / docs-check / diff --check
4. 新 commit(Conventional Commits,如 `fix(crawler): normalize zhiye job city text + pinyin site ids`)

## 汇报

写 `/Users/acccan/domain-map/tech/roles/development/parallel-sessions/20260820-boss-national-data/reports/w3.md`:3 个失败根因 + 修复方式 + 测试结果(真实数字,含 crawler 103 全过)。**末两行必须精确**:

```
门禁: PASSED | FAILED
结论: OK | BLOCKED: <一句话问题>
```

不 push、不 merge、不切分支。
