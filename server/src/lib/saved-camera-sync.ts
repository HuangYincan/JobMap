// ============================================================
// 已退役(2026-08-22,ws1 saved-layer-nofly)——请勿再引用。
//
// 「收藏相机同步」状态机是收藏图层 toggle 程序化相机移动(setBounds)的
// 结构性抑制(替代 500ms 时间窗补丁,ws1 saved-overlay-wipe)。用户决策
// (2026-08-22):打开/关闭收藏图层时相机完全不动,不 setBounds / 不 fit /
// 不移视野——toggle 的相机动作已删除(use-saved-layer.ts no-fly),状态机
// 唯一输入源消失,全部消费者已移除:
// - use-work-viewport.ts:onViewChange 消费/再导出已删;
// - map-shell.tsx:syncView 圆心冻结 + ref 接线已删。
//
// 本文件已清空为纯注释(零导出、零引用)。物理删除在本 worker 会话被沙箱
// 禁止(rm / git rm 均被拦),由 boss 合并时 git rm 本文件收尾即可。
// ============================================================
