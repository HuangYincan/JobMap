# qqdoc-jobs drops

JSON files for this source live in the private JobMap-data repository
(`recruitment/qqdoc-jobs/`). This public folder is README-only.

One file = one company extracted from the reviewed Tencent Docs share. Override with `QQDOC_JOBS_DIR` or `JOBMAP_DATA_DIR`. Missing, empty, or README-only input is not an authoritative snapshot.
