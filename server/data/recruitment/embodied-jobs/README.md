# embodied-jobs drops

JSON files for this source live in the private JobMap-data repository
(`recruitment/embodied-jobs/`). This public folder is README-only.

Mapped from the reviewed Embodied-AI jobs snapshot. Override with `EMBODIED_JOBS_DIR` or `JOBMAP_DATA_DIR`. Missing, empty, or README-only input is not an authoritative snapshot.
